(require 'cl-lib)

(require 'hydra.core)

(require 'hydra.decode.core)

(require 'hydra.errors)

(require 'hydra.lib.eithers)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.maybes)

(require 'hydra.lib.sets)

(require 'hydra.lib.strings)

(defvar hydra_templates_graph_to_schema (lambda (cx) (lambda (graph) (lambda (els) (let ((to_pair (lambda (el) (let ((name (funcall (lambda (v) (hydra_core_binding-name v)) el))) (funcall (hydra_lib_eithers_bind (funcall (hydra_decode_core_type graph) (funcall (lambda (v) (hydra_core_binding-term v)) el))) (lambda (t_) (list :right (list name t_)))))))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list to_pair) els)) (lambda (pairs) (list :right (hydra_lib_maps_from_list pairs)))))))))

(defvar hydra_templates_instantiate_template (lambda (cx) (lambda (minimal) (lambda (schema) (lambda (tname) (lambda (t_) (let ((inst (lambda (tn) (funcall (funcall (funcall (hydra_templates_instantiate_template cx) minimal) schema) tn)))) (let ((no_poly (list :left (list :extraction (list :unexpected_shape (make-hydra_errors_unexpected_shape_error "non-polymorphic type" "polymorphic or function type")))))) (let ((for_float (lambda (ft) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :bigfloat) (funcall (lambda (_) (list :bigfloat 0.0)) match_value)) ((equal (car match_target) :float32) (funcall (lambda (_) (list :float32 0.0)) match_value)) ((equal (car match_target) :float64) (funcall (lambda (_) (list :float64 0.0)) match_value)))) (cadr match_target))) ft)))) (let ((for_integer (lambda (it) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :bigint) (funcall (lambda (_) (list :bigint 0)) match_value)) ((equal (car match_target) :int8) (funcall (lambda (_) (list :int8 0)) match_value)) ((equal (car match_target) :int16) (funcall (lambda (_) (list :int16 0)) match_value)) ((equal (car match_target) :int32) (funcall (lambda (_) (list :int32 0)) match_value)) ((equal (car match_target) :int64) (funcall (lambda (_) (list :int64 0)) match_value)) ((equal (car match_target) :uint8) (funcall (lambda (_) (list :uint8 0)) match_value)) ((equal (car match_target) :uint16) (funcall (lambda (_) (list :uint16 0)) match_value)) ((equal (car match_target) :uint32) (funcall (lambda (_) (list :uint32 0)) match_value)) ((equal (car match_target) :uint64) (funcall (lambda (_) (list :uint64 0)) match_value)))) (cadr match_target))) it)))) (let ((for_literal (lambda (lt) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :binary) (funcall (lambda (_) (list :string "")) match_value)) ((equal (car match_target) :boolean) (funcall (lambda (_) (list :boolean nil)) match_value)) ((equal (car match_target) :integer) (funcall (lambda (it) (list :integer (for_integer it))) match_value)) ((equal (car match_target) :float) (funcall (lambda (ft) (list :float (for_float ft))) match_value)) ((equal (car match_target) :string) (funcall (lambda (_) (list :string "")) match_value)))) (cadr match_target))) lt)))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (inst tname) (funcall (lambda (v) (hydra_core_annotated_type-body v)) at))) match_value)) ((equal (car match_target) :application) (funcall (lambda (_) no_poly) match_value)) ((equal (car match_target) :function) (funcall (lambda (_) no_poly) match_value)) ((equal (car match_target) :forall) (funcall (lambda (_) no_poly) match_value)) ((equal (car match_target) :list) (funcall (lambda (et) (if minimal (list :right (list :list (list))) (funcall (hydra_lib_eithers_bind (funcall (inst tname) et)) (lambda (e) (list :right (list :list (list e))))))) match_value)) ((equal (car match_target) :literal) (funcall (lambda (lt) (list :right (list :literal (for_literal lt)))) match_value)) ((equal (car match_target) :map) (funcall (lambda (mt) (let ((kt (funcall (lambda (v) (hydra_core_map_type-keys v)) mt))) (let ((vt (funcall (lambda (v) (hydra_core_map_type-values v)) mt))) (if minimal (list :right (list :map hydra_lib_maps_empty)) (funcall (hydra_lib_eithers_bind (funcall (inst tname) kt)) (lambda (ke) (funcall (hydra_lib_eithers_bind (funcall (inst tname) vt)) (lambda (ve) (list :right (list :map (funcall (hydra_lib_maps_singleton ke) ve))))))))))) match_value)) ((equal (car match_target) :maybe) (funcall (lambda (ot) (if minimal (list :right (list :maybe (list :nothing))) (funcall (hydra_lib_eithers_bind (funcall (inst tname) ot)) (lambda (e) (list :right (list :maybe (list :just e))))))) match_value)) ((equal (car match_target) :record) (funcall (lambda (rt) (let ((to_field (lambda (ft) (funcall (hydra_lib_eithers_bind (funcall (inst tname) (funcall (lambda (v) (hydra_core_field_type-type v)) ft))) (lambda (e) (list :right (make-hydra_core_field (funcall (lambda (v) (hydra_core_field_type-name v)) ft) e))))))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list to_field) rt)) (lambda (dfields) (list :right (list :record (make-hydra_core_record tname dfields))))))) match_value)) ((equal (car match_target) :set) (funcall (lambda (et) (if minimal (list :right (list :set hydra_lib_sets_empty)) (funcall (hydra_lib_eithers_bind (funcall (inst tname) et)) (lambda (e) (list :right (list :set (hydra_lib_sets_from_list (list e)))))))) match_value)) ((equal (car match_target) :variable) (funcall (lambda (vname) (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list :left (list :resolution (list :unexpected_shape (make-hydra_errors_unexpected_shape_error "bound type variable" (funcall (hydra_lib_strings_cat2 "unbound variable ") (funcall (lambda (v) v) vname)))))))) (inst vname)) (funcall (hydra_lib_maps_lookup vname) schema))) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (wt) (funcall (hydra_lib_eithers_bind (funcall (inst tname) wt)) (lambda (e) (list :right (list :wrap (make-hydra_core_wrapped_term tname e)))))) match_value)))) (cadr match_target))) t_))))))))))))

(provide 'hydra.templates)
