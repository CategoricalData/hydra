;;; Auto-generated alist-compatible struct accessors
;;; Generated by gen-compat.sh — do not edit manually
(in-package :cl-user)


;; hydra_ast_block_style -> hydra_ast_block_style
(defun make-hydra_ast_block_style (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :indent :newline_before_content :newline_after_content) args)))
(defun hydra_ast_block_style-indent (rec) (cdr (assoc :indent rec)))
(defun hydra_ast_block_style-newline_before_content (rec) (cdr (assoc :newline_before_content rec)))
(defun hydra_ast_block_style-newline_after_content (rec) (cdr (assoc :newline_after_content rec)))
(defun copy-hydra_ast_block_style (x) (copy-alist x))
(defun hydra_ast_block_style-p (x) (listp x))
(setf (symbol-function 'make-block_style) #'make-hydra_ast_block_style)
(setf (symbol-function 'block_style-indent) #'hydra_ast_block_style-indent)
(setf (symbol-function 'block_style-newline_before_content) #'hydra_ast_block_style-newline_before_content)
(setf (symbol-function 'block_style-newline_after_content) #'hydra_ast_block_style-newline_after_content)
(setf (symbol-function 'copy-block_style) #'copy-hydra_ast_block_style)
(setf (symbol-function 'block_style-p) #'hydra_ast_block_style-p)

;; hydra_ast_bracket_expr -> hydra_ast_bracket_expr
(defun make-hydra_ast_bracket_expr (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :brackets :enclosed :style) args)))
(defun hydra_ast_bracket_expr-brackets (rec) (cdr (assoc :brackets rec)))
(defun hydra_ast_bracket_expr-enclosed (rec) (cdr (assoc :enclosed rec)))
(defun hydra_ast_bracket_expr-style (rec) (cdr (assoc :style rec)))
(defun copy-hydra_ast_bracket_expr (x) (copy-alist x))
(defun hydra_ast_bracket_expr-p (x) (listp x))
(setf (symbol-function 'make-bracket_expr) #'make-hydra_ast_bracket_expr)
(setf (symbol-function 'bracket_expr-brackets) #'hydra_ast_bracket_expr-brackets)
(setf (symbol-function 'bracket_expr-enclosed) #'hydra_ast_bracket_expr-enclosed)
(setf (symbol-function 'bracket_expr-style) #'hydra_ast_bracket_expr-style)
(setf (symbol-function 'copy-bracket_expr) #'copy-hydra_ast_bracket_expr)
(setf (symbol-function 'bracket_expr-p) #'hydra_ast_bracket_expr-p)

;; hydra_ast_brackets -> hydra_ast_brackets
(defun make-hydra_ast_brackets (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :open :close) args)))
(defun hydra_ast_brackets-open (rec) (cdr (assoc :open rec)))
(defun hydra_ast_brackets-close (rec) (cdr (assoc :close rec)))
(defun copy-hydra_ast_brackets (x) (copy-alist x))
(defun hydra_ast_brackets-p (x) (listp x))
(setf (symbol-function 'make-brackets) #'make-hydra_ast_brackets)
(setf (symbol-function 'brackets-open) #'hydra_ast_brackets-open)
(setf (symbol-function 'brackets-close) #'hydra_ast_brackets-close)
(setf (symbol-function 'copy-brackets) #'copy-hydra_ast_brackets)
(setf (symbol-function 'brackets-p) #'hydra_ast_brackets-p)

;; hydra_ast_indented_expression -> hydra_ast_indented_expression
(defun make-hydra_ast_indented_expression (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :style :expr) args)))
(defun hydra_ast_indented_expression-style (rec) (cdr (assoc :style rec)))
(defun hydra_ast_indented_expression-expr (rec) (cdr (assoc :expr rec)))
(defun copy-hydra_ast_indented_expression (x) (copy-alist x))
(defun hydra_ast_indented_expression-p (x) (listp x))
(setf (symbol-function 'make-indented_expression) #'make-hydra_ast_indented_expression)
(setf (symbol-function 'indented_expression-style) #'hydra_ast_indented_expression-style)
(setf (symbol-function 'indented_expression-expr) #'hydra_ast_indented_expression-expr)
(setf (symbol-function 'copy-indented_expression) #'copy-hydra_ast_indented_expression)
(setf (symbol-function 'indented_expression-p) #'hydra_ast_indented_expression-p)

;; hydra_ast_op -> hydra_ast_op
(defun make-hydra_ast_op (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :symbol :padding :precedence :associativity) args)))
(defun hydra_ast_op-symbol (rec) (cdr (assoc :symbol rec)))
(defun hydra_ast_op-padding (rec) (cdr (assoc :padding rec)))
(defun hydra_ast_op-precedence (rec) (cdr (assoc :precedence rec)))
(defun hydra_ast_op-associativity (rec) (cdr (assoc :associativity rec)))
(defun copy-hydra_ast_op (x) (copy-alist x))
(defun hydra_ast_op-p (x) (listp x))
(setf (symbol-function 'make-op) #'make-hydra_ast_op)
(setf (symbol-function 'op-symbol) #'hydra_ast_op-symbol)
(setf (symbol-function 'op-padding) #'hydra_ast_op-padding)
(setf (symbol-function 'op-precedence) #'hydra_ast_op-precedence)
(setf (symbol-function 'op-associativity) #'hydra_ast_op-associativity)
(setf (symbol-function 'copy-op) #'copy-hydra_ast_op)
(setf (symbol-function 'op-p) #'hydra_ast_op-p)

;; hydra_ast_op_expr -> hydra_ast_op_expr
(defun make-hydra_ast_op_expr (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :op :lhs :rhs) args)))
(defun hydra_ast_op_expr-op (rec) (cdr (assoc :op rec)))
(defun hydra_ast_op_expr-lhs (rec) (cdr (assoc :lhs rec)))
(defun hydra_ast_op_expr-rhs (rec) (cdr (assoc :rhs rec)))
(defun copy-hydra_ast_op_expr (x) (copy-alist x))
(defun hydra_ast_op_expr-p (x) (listp x))
(setf (symbol-function 'make-op_expr) #'make-hydra_ast_op_expr)
(setf (symbol-function 'op_expr-op) #'hydra_ast_op_expr-op)
(setf (symbol-function 'op_expr-lhs) #'hydra_ast_op_expr-lhs)
(setf (symbol-function 'op_expr-rhs) #'hydra_ast_op_expr-rhs)
(setf (symbol-function 'copy-op_expr) #'copy-hydra_ast_op_expr)
(setf (symbol-function 'op_expr-p) #'hydra_ast_op_expr-p)

;; hydra_ast_padding -> hydra_ast_padding
(defun make-hydra_ast_padding (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :left :right) args)))
(defun hydra_ast_padding-left (rec) (cdr (assoc :left rec)))
(defun hydra_ast_padding-right (rec) (cdr (assoc :right rec)))
(defun copy-hydra_ast_padding (x) (copy-alist x))
(defun hydra_ast_padding-p (x) (listp x))
(setf (symbol-function 'make-padding) #'make-hydra_ast_padding)
(setf (symbol-function 'padding-left) #'hydra_ast_padding-left)
(setf (symbol-function 'padding-right) #'hydra_ast_padding-right)
(setf (symbol-function 'copy-padding) #'copy-hydra_ast_padding)
(setf (symbol-function 'padding-p) #'hydra_ast_padding-p)

;; hydra_ast_precedence -> hydra_ast_precedence
(defun make-hydra_ast_precedence (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_ast_precedence-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_ast_precedence (x) (copy-alist x))
(defun hydra_ast_precedence-p (x) (listp x))
(setf (symbol-function 'make-precedence) #'make-hydra_ast_precedence)
(setf (symbol-function 'precedence-value) #'hydra_ast_precedence-value)
(setf (symbol-function 'copy-precedence) #'copy-hydra_ast_precedence)
(setf (symbol-function 'precedence-p) #'hydra_ast_precedence-p)

;; hydra_ast_seq_expr -> hydra_ast_seq_expr
(defun make-hydra_ast_seq_expr (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :op :elements) args)))
(defun hydra_ast_seq_expr-op (rec) (cdr (assoc :op rec)))
(defun hydra_ast_seq_expr-elements (rec) (cdr (assoc :elements rec)))
(defun copy-hydra_ast_seq_expr (x) (copy-alist x))
(defun hydra_ast_seq_expr-p (x) (listp x))
(setf (symbol-function 'make-seq_expr) #'make-hydra_ast_seq_expr)
(setf (symbol-function 'seq_expr-op) #'hydra_ast_seq_expr-op)
(setf (symbol-function 'seq_expr-elements) #'hydra_ast_seq_expr-elements)
(setf (symbol-function 'copy-seq_expr) #'copy-hydra_ast_seq_expr)
(setf (symbol-function 'seq_expr-p) #'hydra_ast_seq_expr-p)

;; hydra_ast_symbol -> hydra_ast_symbol
(defun make-hydra_ast_symbol (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_ast_symbol-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_ast_symbol (x) (copy-alist x))
(defun hydra_ast_symbol-p (x) (listp x))

;; hydra_coders_adapter -> hydra_coders_adapter
(defun make-hydra_coders_adapter (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :is_lossy :source :target :coder) args)))
(defun hydra_coders_adapter-is_lossy (rec) (cdr (assoc :is_lossy rec)))
(defun hydra_coders_adapter-source (rec) (cdr (assoc :source rec)))
(defun hydra_coders_adapter-target (rec) (cdr (assoc :target rec)))
(defun hydra_coders_adapter-coder (rec) (cdr (assoc :coder rec)))
(defun copy-hydra_coders_adapter (x) (copy-alist x))
(defun hydra_coders_adapter-p (x) (listp x))
(setf (symbol-function 'make-adapter) #'make-hydra_coders_adapter)
(setf (symbol-function 'adapter-is_lossy) #'hydra_coders_adapter-is_lossy)
(setf (symbol-function 'adapter-source) #'hydra_coders_adapter-source)
(setf (symbol-function 'adapter-target) #'hydra_coders_adapter-target)
(setf (symbol-function 'adapter-coder) #'hydra_coders_adapter-coder)
(setf (symbol-function 'copy-adapter) #'copy-hydra_coders_adapter)
(setf (symbol-function 'adapter-p) #'hydra_coders_adapter-p)

;; hydra_coders_adapter_context -> hydra_coders_adapter_context
(defun make-hydra_coders_adapter_context (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :graph :language :adapters) args)))
(defun hydra_coders_adapter_context-graph (rec) (cdr (assoc :graph rec)))
(defun hydra_coders_adapter_context-language (rec) (cdr (assoc :language rec)))
(defun hydra_coders_adapter_context-adapters (rec) (cdr (assoc :adapters rec)))
(defun copy-hydra_coders_adapter_context (x) (copy-alist x))
(defun hydra_coders_adapter_context-p (x) (listp x))
(setf (symbol-function 'make-adapter_context) #'make-hydra_coders_adapter_context)
(setf (symbol-function 'adapter_context-graph) #'hydra_coders_adapter_context-graph)
(setf (symbol-function 'adapter_context-language) #'hydra_coders_adapter_context-language)
(setf (symbol-function 'adapter_context-adapters) #'hydra_coders_adapter_context-adapters)
(setf (symbol-function 'copy-adapter_context) #'copy-hydra_coders_adapter_context)
(setf (symbol-function 'adapter_context-p) #'hydra_coders_adapter_context-p)

;; hydra_coders_bicoder -> hydra_coders_bicoder
(defun make-hydra_coders_bicoder (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :encode :decode) args)))
(defun hydra_coders_bicoder-encode (rec) (cdr (assoc :encode rec)))
(defun hydra_coders_bicoder-decode (rec) (cdr (assoc :decode rec)))
(defun copy-hydra_coders_bicoder (x) (copy-alist x))
(defun hydra_coders_bicoder-p (x) (listp x))
(setf (symbol-function 'make-bicoder) #'make-hydra_coders_bicoder)
(setf (symbol-function 'bicoder-encode) #'hydra_coders_bicoder-encode)
(setf (symbol-function 'bicoder-decode) #'hydra_coders_bicoder-decode)
(setf (symbol-function 'copy-bicoder) #'copy-hydra_coders_bicoder)
(setf (symbol-function 'bicoder-p) #'hydra_coders_bicoder-p)

;; hydra_coders_coder -> hydra_coders_coder
(defun make-hydra_coders_coder (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :encode :decode) args)))
(defun hydra_coders_coder-encode (rec) (cdr (assoc :encode rec)))
(defun hydra_coders_coder-decode (rec) (cdr (assoc :decode rec)))
(defun copy-hydra_coders_coder (x) (copy-alist x))
(defun hydra_coders_coder-p (x) (listp x))
(setf (symbol-function 'make-coder) #'make-hydra_coders_coder)
(setf (symbol-function 'coder-encode) #'hydra_coders_coder-encode)
(setf (symbol-function 'coder-decode) #'hydra_coders_coder-decode)
(setf (symbol-function 'copy-coder) #'copy-hydra_coders_coder)
(setf (symbol-function 'coder-p) #'hydra_coders_coder-p)

;; hydra_coders_language -> hydra_coders_language
(defun make-hydra_coders_language (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :constraints) args)))
(defun hydra_coders_language-name (rec) (cdr (assoc :name rec)))
(defun hydra_coders_language-constraints (rec) (cdr (assoc :constraints rec)))
(defun copy-hydra_coders_language (x) (copy-alist x))
(defun hydra_coders_language-p (x) (listp x))
(setf (symbol-function 'make-language) #'make-hydra_coders_language)
(setf (symbol-function 'language-name) #'hydra_coders_language-name)
(setf (symbol-function 'language-constraints) #'hydra_coders_language-constraints)
(setf (symbol-function 'copy-language) #'copy-hydra_coders_language)
(setf (symbol-function 'language-p) #'hydra_coders_language-p)

;; hydra_coders_language_name -> hydra_coders_language_name
(defun make-hydra_coders_language_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_coders_language_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_coders_language_name (x) (copy-alist x))
(defun hydra_coders_language_name-p (x) (listp x))
(setf (symbol-function 'make-language_name) #'make-hydra_coders_language_name)
(setf (symbol-function 'language_name-value) #'hydra_coders_language_name-value)
(setf (symbol-function 'copy-language_name) #'copy-hydra_coders_language_name)
(setf (symbol-function 'language_name-p) #'hydra_coders_language_name-p)

;; hydra_core_annotated_term -> hydra_core_annotated_term
(defun make-hydra_core_annotated_term (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :body :annotation) args)))
(defun hydra_core_annotated_term-body (rec) (cdr (assoc :body rec)))
(defun hydra_core_annotated_term-annotation (rec) (cdr (assoc :annotation rec)))
(defun copy-hydra_core_annotated_term (x) (copy-alist x))
(defun hydra_core_annotated_term-p (x) (listp x))
(setf (symbol-function 'make-annotated_term) #'make-hydra_core_annotated_term)
(setf (symbol-function 'annotated_term-body) #'hydra_core_annotated_term-body)
(setf (symbol-function 'annotated_term-annotation) #'hydra_core_annotated_term-annotation)
(setf (symbol-function 'copy-annotated_term) #'copy-hydra_core_annotated_term)
(setf (symbol-function 'annotated_term-p) #'hydra_core_annotated_term-p)

;; hydra_core_annotated_type -> hydra_core_annotated_type
(defun make-hydra_core_annotated_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :body :annotation) args)))
(defun hydra_core_annotated_type-body (rec) (cdr (assoc :body rec)))
(defun hydra_core_annotated_type-annotation (rec) (cdr (assoc :annotation rec)))
(defun copy-hydra_core_annotated_type (x) (copy-alist x))
(defun hydra_core_annotated_type-p (x) (listp x))
(setf (symbol-function 'make-annotated_type) #'make-hydra_core_annotated_type)
(setf (symbol-function 'annotated_type-body) #'hydra_core_annotated_type-body)
(setf (symbol-function 'annotated_type-annotation) #'hydra_core_annotated_type-annotation)
(setf (symbol-function 'copy-annotated_type) #'copy-hydra_core_annotated_type)
(setf (symbol-function 'annotated_type-p) #'hydra_core_annotated_type-p)

;; hydra_core_application -> hydra_core_application
(defun make-hydra_core_application (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :function :argument) args)))
(defun hydra_core_application-function (rec) (cdr (assoc :function rec)))
(defun hydra_core_application-argument (rec) (cdr (assoc :argument rec)))
(defun copy-hydra_core_application (x) (copy-alist x))
(defun hydra_core_application-p (x) (listp x))
(setf (symbol-function 'make-application) #'make-hydra_core_application)
(setf (symbol-function 'application-function) #'hydra_core_application-function)
(setf (symbol-function 'application-argument) #'hydra_core_application-argument)
(setf (symbol-function 'copy-application) #'copy-hydra_core_application)
(setf (symbol-function 'application-p) #'hydra_core_application-p)

;; hydra_core_application_type -> hydra_core_application_type
(defun make-hydra_core_application_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :function :argument) args)))
(defun hydra_core_application_type-function (rec) (cdr (assoc :function rec)))
(defun hydra_core_application_type-argument (rec) (cdr (assoc :argument rec)))
(defun copy-hydra_core_application_type (x) (copy-alist x))
(defun hydra_core_application_type-p (x) (listp x))
(setf (symbol-function 'make-application_type) #'make-hydra_core_application_type)
(setf (symbol-function 'application_type-function) #'hydra_core_application_type-function)
(setf (symbol-function 'application_type-argument) #'hydra_core_application_type-argument)
(setf (symbol-function 'copy-application_type) #'copy-hydra_core_application_type)
(setf (symbol-function 'application_type-p) #'hydra_core_application_type-p)

;; hydra_core_binding -> hydra_core_binding
(defun make-hydra_core_binding (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :term :type_scheme) args)))
(defun hydra_core_binding-name (rec) (cdr (assoc :name rec)))
(defun hydra_core_binding-term (rec) (cdr (assoc :term rec)))
(defun hydra_core_binding-type_scheme (rec) (cdr (assoc :type_scheme rec)))
(defun copy-hydra_core_binding (x) (copy-alist x))
(defun hydra_core_binding-p (x) (listp x))
(setf (symbol-function 'make-binding) #'make-hydra_core_binding)
(setf (symbol-function 'binding-name) #'hydra_core_binding-name)
(setf (symbol-function 'binding-term) #'hydra_core_binding-term)
(setf (symbol-function 'binding-type_scheme) #'hydra_core_binding-type_scheme)
(setf (symbol-function 'copy-binding) #'copy-hydra_core_binding)
(setf (symbol-function 'binding-p) #'hydra_core_binding-p)

;; hydra_core_case_statement -> hydra_core_case_statement
(defun make-hydra_core_case_statement (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_name :default :cases) args)))
(defun hydra_core_case_statement-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_core_case_statement-default (rec) (cdr (assoc :default rec)))
(defun hydra_core_case_statement-cases (rec) (cdr (assoc :cases rec)))
(defun copy-hydra_core_case_statement (x) (copy-alist x))
(defun hydra_core_case_statement-p (x) (listp x))
(setf (symbol-function 'make-case_statement) #'make-hydra_core_case_statement)
(setf (symbol-function 'case_statement-type_name) #'hydra_core_case_statement-type_name)
(setf (symbol-function 'case_statement-default) #'hydra_core_case_statement-default)
(setf (symbol-function 'case_statement-cases) #'hydra_core_case_statement-cases)
(setf (symbol-function 'copy-case_statement) #'copy-hydra_core_case_statement)
(setf (symbol-function 'case_statement-p) #'hydra_core_case_statement-p)

;; hydra_core_either_type -> hydra_core_either_type
(defun make-hydra_core_either_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :left :right) args)))
(defun hydra_core_either_type-left (rec) (cdr (assoc :left rec)))
(defun hydra_core_either_type-right (rec) (cdr (assoc :right rec)))
(defun copy-hydra_core_either_type (x) (copy-alist x))
(defun hydra_core_either_type-p (x) (listp x))
(setf (symbol-function 'make-either_type) #'make-hydra_core_either_type)
(setf (symbol-function 'either_type-left) #'hydra_core_either_type-left)
(setf (symbol-function 'either_type-right) #'hydra_core_either_type-right)
(setf (symbol-function 'copy-either_type) #'copy-hydra_core_either_type)
(setf (symbol-function 'either_type-p) #'hydra_core_either_type-p)

;; hydra_core_field -> hydra_core_field
(defun make-hydra_core_field (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :term) args)))
(defun hydra_core_field-name (rec) (cdr (assoc :name rec)))
(defun hydra_core_field-term (rec) (cdr (assoc :term rec)))
(defun copy-hydra_core_field (x) (copy-alist x))
(defun hydra_core_field-p (x) (listp x))
(setf (symbol-function 'make-field) #'make-hydra_core_field)
(setf (symbol-function 'field-name) #'hydra_core_field-name)
(setf (symbol-function 'field-term) #'hydra_core_field-term)
(setf (symbol-function 'copy-field) #'copy-hydra_core_field)
(setf (symbol-function 'field-p) #'hydra_core_field-p)

;; hydra_core_field_type -> hydra_core_field_type
(defun make-hydra_core_field_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :type) args)))
(defun hydra_core_field_type-name (rec) (cdr (assoc :name rec)))
(defun hydra_core_field_type-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_core_field_type (x) (copy-alist x))
(defun hydra_core_field_type-p (x) (listp x))
(setf (symbol-function 'make-field_type) #'make-hydra_core_field_type)
(setf (symbol-function 'field_type-name) #'hydra_core_field_type-name)
(setf (symbol-function 'field_type-type) #'hydra_core_field_type-type)
(setf (symbol-function 'copy-field_type) #'copy-hydra_core_field_type)
(setf (symbol-function 'field_type-p) #'hydra_core_field_type-p)

;; hydra_core_forall_type -> hydra_core_forall_type
(defun make-hydra_core_forall_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :parameter :body) args)))
(defun hydra_core_forall_type-parameter (rec) (cdr (assoc :parameter rec)))
(defun hydra_core_forall_type-body (rec) (cdr (assoc :body rec)))
(defun copy-hydra_core_forall_type (x) (copy-alist x))
(defun hydra_core_forall_type-p (x) (listp x))
(setf (symbol-function 'make-forall_type) #'make-hydra_core_forall_type)
(setf (symbol-function 'forall_type-parameter) #'hydra_core_forall_type-parameter)
(setf (symbol-function 'forall_type-body) #'hydra_core_forall_type-body)
(setf (symbol-function 'copy-forall_type) #'copy-hydra_core_forall_type)
(setf (symbol-function 'forall_type-p) #'hydra_core_forall_type-p)

;; hydra_core_function_type -> hydra_core_function_type
(defun make-hydra_core_function_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :domain :codomain) args)))
(defun hydra_core_function_type-domain (rec) (cdr (assoc :domain rec)))
(defun hydra_core_function_type-codomain (rec) (cdr (assoc :codomain rec)))
(defun copy-hydra_core_function_type (x) (copy-alist x))
(defun hydra_core_function_type-p (x) (listp x))
(setf (symbol-function 'make-function_type) #'make-hydra_core_function_type)
(setf (symbol-function 'function_type-domain) #'hydra_core_function_type-domain)
(setf (symbol-function 'function_type-codomain) #'hydra_core_function_type-codomain)
(setf (symbol-function 'copy-function_type) #'copy-hydra_core_function_type)
(setf (symbol-function 'function_type-p) #'hydra_core_function_type-p)

;; hydra_core_injection -> hydra_core_injection
(defun make-hydra_core_injection (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_name :field) args)))
(defun hydra_core_injection-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_core_injection-field (rec) (cdr (assoc :field rec)))
(defun copy-hydra_core_injection (x) (copy-alist x))
(defun hydra_core_injection-p (x) (listp x))
(setf (symbol-function 'make-injection) #'make-hydra_core_injection)
(setf (symbol-function 'injection-type_name) #'hydra_core_injection-type_name)
(setf (symbol-function 'injection-field) #'hydra_core_injection-field)
(setf (symbol-function 'copy-injection) #'copy-hydra_core_injection)
(setf (symbol-function 'injection-p) #'hydra_core_injection-p)

;; hydra_core_lambda -> hydra_core_lambda
(defun make-hydra_core_lambda (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :parameter :domain :body) args)))
(defun hydra_core_lambda-parameter (rec) (cdr (assoc :parameter rec)))
(defun hydra_core_lambda-domain (rec) (cdr (assoc :domain rec)))
(defun hydra_core_lambda-body (rec) (cdr (assoc :body rec)))
(defun copy-hydra_core_lambda (x) (copy-alist x))
(defun hydra_core_lambda-p (x) (listp x))
(setf (symbol-function 'make-lambda) #'make-hydra_core_lambda)
(setf (symbol-function 'lambda-parameter) #'hydra_core_lambda-parameter)
(setf (symbol-function 'lambda-domain) #'hydra_core_lambda-domain)
(setf (symbol-function 'lambda-body) #'hydra_core_lambda-body)
(setf (symbol-function 'copy-lambda) #'copy-hydra_core_lambda)
(setf (symbol-function 'lambda-p) #'hydra_core_lambda-p)

;; hydra_core_let -> hydra_core_let
(defun make-hydra_core_let (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :bindings :body) args)))
(defun hydra_core_let-bindings (rec) (cdr (assoc :bindings rec)))
(defun hydra_core_let-body (rec) (cdr (assoc :body rec)))
(defun copy-hydra_core_let (x) (copy-alist x))
(defun hydra_core_let-p (x) (listp x))
(setf (symbol-function 'make-let) #'make-hydra_core_let)
(setf (symbol-function 'let-bindings) #'hydra_core_let-bindings)
(setf (symbol-function 'let-body) #'hydra_core_let-body)
(setf (symbol-function 'copy-let) #'copy-hydra_core_let)
(setf (symbol-function 'let-p) #'hydra_core_let-p)

;; hydra_core_map_type -> hydra_core_map_type
(defun make-hydra_core_map_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :keys :values) args)))
(defun hydra_core_map_type-keys (rec) (cdr (assoc :keys rec)))
(defun hydra_core_map_type-values (rec) (cdr (assoc :values rec)))
(defun copy-hydra_core_map_type (x) (copy-alist x))
(defun hydra_core_map_type-p (x) (listp x))
(setf (symbol-function 'make-map_type) #'make-hydra_core_map_type)
(setf (symbol-function 'map_type-keys) #'hydra_core_map_type-keys)
(setf (symbol-function 'map_type-values) #'hydra_core_map_type-values)
(setf (symbol-function 'copy-map_type) #'copy-hydra_core_map_type)
(setf (symbol-function 'map_type-p) #'hydra_core_map_type-p)

;; hydra_core_name -> hydra_core_name
(defun make-hydra_core_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_core_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_core_name (x) (copy-alist x))
(defun hydra_core_name-p (x) (listp x))
(setf (symbol-function 'make-name) #'make-hydra_core_name)
(setf (symbol-function 'name-value) #'hydra_core_name-value)
(setf (symbol-function 'copy-name) #'copy-hydra_core_name)
(setf (symbol-function 'name-p) #'hydra_core_name-p)

;; hydra_core_pair_type -> hydra_core_pair_type
(defun make-hydra_core_pair_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :first :second) args)))
(defun hydra_core_pair_type-first (rec) (cdr (assoc :first rec)))
(defun hydra_core_pair_type-second (rec) (cdr (assoc :second rec)))
(defun copy-hydra_core_pair_type (x) (copy-alist x))
(defun hydra_core_pair_type-p (x) (listp x))
(setf (symbol-function 'make-pair_type) #'make-hydra_core_pair_type)
(setf (symbol-function 'pair_type-first) #'hydra_core_pair_type-first)
(setf (symbol-function 'pair_type-second) #'hydra_core_pair_type-second)
(setf (symbol-function 'copy-pair_type) #'copy-hydra_core_pair_type)
(setf (symbol-function 'pair_type-p) #'hydra_core_pair_type-p)

;; hydra_core_projection -> hydra_core_projection
(defun make-hydra_core_projection (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_name :field_name) args)))
(defun hydra_core_projection-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_core_projection-field_name (rec) (cdr (assoc :field_name rec)))
(defun copy-hydra_core_projection (x) (copy-alist x))
(defun hydra_core_projection-p (x) (listp x))
(setf (symbol-function 'make-projection) #'make-hydra_core_projection)
(setf (symbol-function 'projection-type_name) #'hydra_core_projection-type_name)
(setf (symbol-function 'projection-field_name) #'hydra_core_projection-field_name)
(setf (symbol-function 'copy-projection) #'copy-hydra_core_projection)
(setf (symbol-function 'projection-p) #'hydra_core_projection-p)

;; hydra_core_record -> hydra_core_record
(defun make-hydra_core_record (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_name :fields) args)))
(defun hydra_core_record-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_core_record-fields (rec) (cdr (assoc :fields rec)))
(defun copy-hydra_core_record (x) (copy-alist x))
(defun hydra_core_record-p (x) (listp x))
(setf (symbol-function 'make-record) #'make-hydra_core_record)
(setf (symbol-function 'record-type_name) #'hydra_core_record-type_name)
(setf (symbol-function 'record-fields) #'hydra_core_record-fields)
(setf (symbol-function 'copy-record) #'copy-hydra_core_record)
(setf (symbol-function 'record-p) #'hydra_core_record-p)

;; hydra_core_type_application_term -> hydra_core_type_application_term
(defun make-hydra_core_type_application_term (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :body :type) args)))
(defun hydra_core_type_application_term-body (rec) (cdr (assoc :body rec)))
(defun hydra_core_type_application_term-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_core_type_application_term (x) (copy-alist x))
(defun hydra_core_type_application_term-p (x) (listp x))
(setf (symbol-function 'make-type_application_term) #'make-hydra_core_type_application_term)
(setf (symbol-function 'type_application_term-body) #'hydra_core_type_application_term-body)
(setf (symbol-function 'type_application_term-type) #'hydra_core_type_application_term-type)
(setf (symbol-function 'copy-type_application_term) #'copy-hydra_core_type_application_term)
(setf (symbol-function 'type_application_term-p) #'hydra_core_type_application_term-p)

;; hydra_core_type_lambda -> hydra_core_type_lambda
(defun make-hydra_core_type_lambda (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :parameter :body) args)))
(defun hydra_core_type_lambda-parameter (rec) (cdr (assoc :parameter rec)))
(defun hydra_core_type_lambda-body (rec) (cdr (assoc :body rec)))
(defun copy-hydra_core_type_lambda (x) (copy-alist x))
(defun hydra_core_type_lambda-p (x) (listp x))
(setf (symbol-function 'make-type_lambda) #'make-hydra_core_type_lambda)
(setf (symbol-function 'type_lambda-parameter) #'hydra_core_type_lambda-parameter)
(setf (symbol-function 'type_lambda-body) #'hydra_core_type_lambda-body)
(setf (symbol-function 'copy-type_lambda) #'copy-hydra_core_type_lambda)
(setf (symbol-function 'type_lambda-p) #'hydra_core_type_lambda-p)

;; hydra_core_type_scheme -> hydra_core_type_scheme
(defun make-hydra_core_type_scheme (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :variables :body :constraints) args)))
(defun hydra_core_type_scheme-variables (rec) (cdr (assoc :variables rec)))
(defun hydra_core_type_scheme-body (rec) (cdr (assoc :body rec)))
(defun hydra_core_type_scheme-constraints (rec) (cdr (assoc :constraints rec)))
(defun copy-hydra_core_type_scheme (x) (copy-alist x))
(defun hydra_core_type_scheme-p (x) (listp x))
(setf (symbol-function 'make-type_scheme) #'make-hydra_core_type_scheme)
(setf (symbol-function 'type_scheme-variables) #'hydra_core_type_scheme-variables)
(setf (symbol-function 'type_scheme-body) #'hydra_core_type_scheme-body)
(setf (symbol-function 'type_scheme-constraints) #'hydra_core_type_scheme-constraints)
(setf (symbol-function 'copy-type_scheme) #'copy-hydra_core_type_scheme)
(setf (symbol-function 'type_scheme-p) #'hydra_core_type_scheme-p)

;; hydra_core_type_variable_metadata -> hydra_core_type_variable_metadata
(defun make-hydra_core_type_variable_metadata (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :classes) args)))
(defun hydra_core_type_variable_metadata-classes (rec) (cdr (assoc :classes rec)))
(defun copy-hydra_core_type_variable_metadata (x) (copy-alist x))
(defun hydra_core_type_variable_metadata-p (x) (listp x))
(setf (symbol-function 'make-type_variable_metadata) #'make-hydra_core_type_variable_metadata)
(setf (symbol-function 'type_variable_metadata-classes) #'hydra_core_type_variable_metadata-classes)
(setf (symbol-function 'copy-type_variable_metadata) #'copy-hydra_core_type_variable_metadata)
(setf (symbol-function 'type_variable_metadata-p) #'hydra_core_type_variable_metadata-p)

;; hydra_core_wrapped_term -> hydra_core_wrapped_term
(defun make-hydra_core_wrapped_term (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_name :body) args)))
(defun hydra_core_wrapped_term-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_core_wrapped_term-body (rec) (cdr (assoc :body rec)))
(defun copy-hydra_core_wrapped_term (x) (copy-alist x))
(defun hydra_core_wrapped_term-p (x) (listp x))
(setf (symbol-function 'make-wrapped_term) #'make-hydra_core_wrapped_term)
(setf (symbol-function 'wrapped_term-type_name) #'hydra_core_wrapped_term-type_name)
(setf (symbol-function 'wrapped_term-body) #'hydra_core_wrapped_term-body)
(setf (symbol-function 'copy-wrapped_term) #'copy-hydra_core_wrapped_term)
(setf (symbol-function 'wrapped_term-p) #'hydra_core_wrapped_term-p)

;; hydra_error_checking_incorrect_unification_error -> hydra_error_checking_incorrect_unification_error
(defun make-hydra_error_checking_incorrect_unification_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :substitution) args)))
(defun hydra_error_checking_incorrect_unification_error-substitution (rec) (cdr (assoc :substitution rec)))
(defun copy-hydra_error_checking_incorrect_unification_error (x) (copy-alist x))
(defun hydra_error_checking_incorrect_unification_error-p (x) (listp x))
(setf (symbol-function 'make-checking_incorrect_unification_error) #'make-hydra_error_checking_incorrect_unification_error)
(setf (symbol-function 'checking_incorrect_unification_error-substitution) #'hydra_error_checking_incorrect_unification_error-substitution)
(setf (symbol-function 'copy-checking_incorrect_unification_error) #'copy-hydra_error_checking_incorrect_unification_error)
(setf (symbol-function 'checking_incorrect_unification_error-p) #'hydra_error_checking_incorrect_unification_error-p)

;; hydra_error_checking_not_a_forall_type_error -> hydra_error_checking_not_a_forall_type_error
(defun make-hydra_error_checking_not_a_forall_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type :type_arguments) args)))
(defun hydra_error_checking_not_a_forall_type_error-type (rec) (cdr (assoc :type rec)))
(defun hydra_error_checking_not_a_forall_type_error-type_arguments (rec) (cdr (assoc :type_arguments rec)))
(defun copy-hydra_error_checking_not_a_forall_type_error (x) (copy-alist x))
(defun hydra_error_checking_not_a_forall_type_error-p (x) (listp x))
(setf (symbol-function 'make-checking_not_a_forall_type_error) #'make-hydra_error_checking_not_a_forall_type_error)
(setf (symbol-function 'checking_not_a_forall_type_error-type) #'hydra_error_checking_not_a_forall_type_error-type)
(setf (symbol-function 'checking_not_a_forall_type_error-type_arguments) #'hydra_error_checking_not_a_forall_type_error-type_arguments)
(setf (symbol-function 'copy-checking_not_a_forall_type_error) #'copy-hydra_error_checking_not_a_forall_type_error)
(setf (symbol-function 'checking_not_a_forall_type_error-p) #'hydra_error_checking_not_a_forall_type_error-p)

;; hydra_error_checking_not_a_function_type_error -> hydra_error_checking_not_a_function_type_error
(defun make-hydra_error_checking_not_a_function_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type) args)))
(defun hydra_error_checking_not_a_function_type_error-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_error_checking_not_a_function_type_error (x) (copy-alist x))
(defun hydra_error_checking_not_a_function_type_error-p (x) (listp x))
(setf (symbol-function 'make-checking_not_a_function_type_error) #'make-hydra_error_checking_not_a_function_type_error)
(setf (symbol-function 'checking_not_a_function_type_error-type) #'hydra_error_checking_not_a_function_type_error-type)
(setf (symbol-function 'copy-checking_not_a_function_type_error) #'copy-hydra_error_checking_not_a_function_type_error)
(setf (symbol-function 'checking_not_a_function_type_error-p) #'hydra_error_checking_not_a_function_type_error-p)

;; hydra_error_checking_other_checking_error -> hydra_error_checking_other_checking_error
(defun make-hydra_error_checking_other_checking_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :message) args)))
(defun hydra_error_checking_other_checking_error-path (rec) (cdr (assoc :path rec)))
(defun hydra_error_checking_other_checking_error-message (rec) (cdr (assoc :message rec)))
(defun copy-hydra_error_checking_other_checking_error (x) (copy-alist x))
(defun hydra_error_checking_other_checking_error-p (x) (listp x))
(setf (symbol-function 'make-checking_other_checking_error) #'make-hydra_error_checking_other_checking_error)
(setf (symbol-function 'checking_other_checking_error-path) #'hydra_error_checking_other_checking_error-path)
(setf (symbol-function 'checking_other_checking_error-message) #'hydra_error_checking_other_checking_error-message)
(setf (symbol-function 'copy-checking_other_checking_error) #'copy-hydra_error_checking_other_checking_error)
(setf (symbol-function 'checking_other_checking_error-p) #'hydra_error_checking_other_checking_error-p)

;; hydra_error_checking_type_arity_mismatch_error -> hydra_error_checking_type_arity_mismatch_error
(defun make-hydra_error_checking_type_arity_mismatch_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type :expected_arity :actual_arity :type_arguments) args)))
(defun hydra_error_checking_type_arity_mismatch_error-type (rec) (cdr (assoc :type rec)))
(defun hydra_error_checking_type_arity_mismatch_error-expected_arity (rec) (cdr (assoc :expected_arity rec)))
(defun hydra_error_checking_type_arity_mismatch_error-actual_arity (rec) (cdr (assoc :actual_arity rec)))
(defun hydra_error_checking_type_arity_mismatch_error-type_arguments (rec) (cdr (assoc :type_arguments rec)))
(defun copy-hydra_error_checking_type_arity_mismatch_error (x) (copy-alist x))
(defun hydra_error_checking_type_arity_mismatch_error-p (x) (listp x))
(setf (symbol-function 'make-checking_type_arity_mismatch_error) #'make-hydra_error_checking_type_arity_mismatch_error)
(setf (symbol-function 'checking_type_arity_mismatch_error-type) #'hydra_error_checking_type_arity_mismatch_error-type)
(setf (symbol-function 'checking_type_arity_mismatch_error-expected_arity) #'hydra_error_checking_type_arity_mismatch_error-expected_arity)
(setf (symbol-function 'checking_type_arity_mismatch_error-actual_arity) #'hydra_error_checking_type_arity_mismatch_error-actual_arity)
(setf (symbol-function 'checking_type_arity_mismatch_error-type_arguments) #'hydra_error_checking_type_arity_mismatch_error-type_arguments)
(setf (symbol-function 'copy-checking_type_arity_mismatch_error) #'copy-hydra_error_checking_type_arity_mismatch_error)
(setf (symbol-function 'checking_type_arity_mismatch_error-p) #'hydra_error_checking_type_arity_mismatch_error-p)

;; hydra_error_checking_type_mismatch_error -> hydra_error_checking_type_mismatch_error
(defun make-hydra_error_checking_type_mismatch_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :expected_type :actual_type) args)))
(defun hydra_error_checking_type_mismatch_error-expected_type (rec) (cdr (assoc :expected_type rec)))
(defun hydra_error_checking_type_mismatch_error-actual_type (rec) (cdr (assoc :actual_type rec)))
(defun copy-hydra_error_checking_type_mismatch_error (x) (copy-alist x))
(defun hydra_error_checking_type_mismatch_error-p (x) (listp x))
(setf (symbol-function 'make-checking_type_mismatch_error) #'make-hydra_error_checking_type_mismatch_error)
(setf (symbol-function 'checking_type_mismatch_error-expected_type) #'hydra_error_checking_type_mismatch_error-expected_type)
(setf (symbol-function 'checking_type_mismatch_error-actual_type) #'hydra_error_checking_type_mismatch_error-actual_type)
(setf (symbol-function 'copy-checking_type_mismatch_error) #'copy-hydra_error_checking_type_mismatch_error)
(setf (symbol-function 'checking_type_mismatch_error-p) #'hydra_error_checking_type_mismatch_error-p)

;; hydra_error_checking_unbound_type_variables_error -> hydra_error_checking_unbound_type_variables_error
(defun make-hydra_error_checking_unbound_type_variables_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :variables :type) args)))
(defun hydra_error_checking_unbound_type_variables_error-variables (rec) (cdr (assoc :variables rec)))
(defun hydra_error_checking_unbound_type_variables_error-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_error_checking_unbound_type_variables_error (x) (copy-alist x))
(defun hydra_error_checking_unbound_type_variables_error-p (x) (listp x))
(setf (symbol-function 'make-checking_unbound_type_variables_error) #'make-hydra_error_checking_unbound_type_variables_error)
(setf (symbol-function 'checking_unbound_type_variables_error-variables) #'hydra_error_checking_unbound_type_variables_error-variables)
(setf (symbol-function 'checking_unbound_type_variables_error-type) #'hydra_error_checking_unbound_type_variables_error-type)
(setf (symbol-function 'copy-checking_unbound_type_variables_error) #'copy-hydra_error_checking_unbound_type_variables_error)
(setf (symbol-function 'checking_unbound_type_variables_error-p) #'hydra_error_checking_unbound_type_variables_error-p)

;; hydra_error_checking_undefined_term_variable_checking_error -> hydra_error_checking_undefined_term_variable_checking_error
(defun make-hydra_error_checking_undefined_term_variable_checking_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :name) args)))
(defun hydra_error_checking_undefined_term_variable_checking_error-path (rec) (cdr (assoc :path rec)))
(defun hydra_error_checking_undefined_term_variable_checking_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_checking_undefined_term_variable_checking_error (x) (copy-alist x))
(defun hydra_error_checking_undefined_term_variable_checking_error-p (x) (listp x))
(setf (symbol-function 'make-checking_undefined_term_variable_checking_error) #'make-hydra_error_checking_undefined_term_variable_checking_error)
(setf (symbol-function 'checking_undefined_term_variable_checking_error-path) #'hydra_error_checking_undefined_term_variable_checking_error-path)
(setf (symbol-function 'checking_undefined_term_variable_checking_error-name) #'hydra_error_checking_undefined_term_variable_checking_error-name)
(setf (symbol-function 'copy-checking_undefined_term_variable_checking_error) #'copy-hydra_error_checking_undefined_term_variable_checking_error)
(setf (symbol-function 'checking_undefined_term_variable_checking_error-p) #'hydra_error_checking_undefined_term_variable_checking_error-p)

;; hydra_error_checking_unequal_types_error -> hydra_error_checking_unequal_types_error
(defun make-hydra_error_checking_unequal_types_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :types :description) args)))
(defun hydra_error_checking_unequal_types_error-types (rec) (cdr (assoc :types rec)))
(defun hydra_error_checking_unequal_types_error-description (rec) (cdr (assoc :description rec)))
(defun copy-hydra_error_checking_unequal_types_error (x) (copy-alist x))
(defun hydra_error_checking_unequal_types_error-p (x) (listp x))
(setf (symbol-function 'make-checking_unequal_types_error) #'make-hydra_error_checking_unequal_types_error)
(setf (symbol-function 'checking_unequal_types_error-types) #'hydra_error_checking_unequal_types_error-types)
(setf (symbol-function 'checking_unequal_types_error-description) #'hydra_error_checking_unequal_types_error-description)
(setf (symbol-function 'copy-checking_unequal_types_error) #'copy-hydra_error_checking_unequal_types_error)
(setf (symbol-function 'checking_unequal_types_error-p) #'hydra_error_checking_unequal_types_error-p)

;; hydra_error_checking_unsupported_term_variant_error -> hydra_error_checking_unsupported_term_variant_error
(defun make-hydra_error_checking_unsupported_term_variant_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :term_variant) args)))
(defun hydra_error_checking_unsupported_term_variant_error-term_variant (rec) (cdr (assoc :term_variant rec)))
(defun copy-hydra_error_checking_unsupported_term_variant_error (x) (copy-alist x))
(defun hydra_error_checking_unsupported_term_variant_error-p (x) (listp x))
(setf (symbol-function 'make-checking_unsupported_term_variant_error) #'make-hydra_error_checking_unsupported_term_variant_error)
(setf (symbol-function 'checking_unsupported_term_variant_error-term_variant) #'hydra_error_checking_unsupported_term_variant_error-term_variant)
(setf (symbol-function 'copy-checking_unsupported_term_variant_error) #'copy-hydra_error_checking_unsupported_term_variant_error)
(setf (symbol-function 'checking_unsupported_term_variant_error-p) #'hydra_error_checking_unsupported_term_variant_error-p)

;; hydra_error_checking_untyped_lambda_error -> hydra_error_checking_untyped_lambda_error
(defun make-hydra_error_checking_untyped_lambda_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '() args)))
(defun copy-hydra_error_checking_untyped_lambda_error (x) (copy-alist x))
(defun hydra_error_checking_untyped_lambda_error-p (x) (listp x))
(setf (symbol-function 'make-checking_untyped_lambda_error) #'make-hydra_error_checking_untyped_lambda_error)
(setf (symbol-function 'copy-checking_untyped_lambda_error) #'copy-hydra_error_checking_untyped_lambda_error)
(setf (symbol-function 'checking_untyped_lambda_error-p) #'hydra_error_checking_untyped_lambda_error-p)

;; hydra_error_checking_untyped_let_binding_error -> hydra_error_checking_untyped_let_binding_error
(defun make-hydra_error_checking_untyped_let_binding_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :binding) args)))
(defun hydra_error_checking_untyped_let_binding_error-binding (rec) (cdr (assoc :binding rec)))
(defun copy-hydra_error_checking_untyped_let_binding_error (x) (copy-alist x))
(defun hydra_error_checking_untyped_let_binding_error-p (x) (listp x))
(setf (symbol-function 'make-checking_untyped_let_binding_error) #'make-hydra_error_checking_untyped_let_binding_error)
(setf (symbol-function 'checking_untyped_let_binding_error-binding) #'hydra_error_checking_untyped_let_binding_error-binding)
(setf (symbol-function 'copy-checking_untyped_let_binding_error) #'copy-hydra_error_checking_untyped_let_binding_error)
(setf (symbol-function 'checking_untyped_let_binding_error-p) #'hydra_error_checking_untyped_let_binding_error-p)

;; hydra_error_checking_untyped_term_variable_checking_error -> hydra_error_checking_untyped_term_variable_checking_error
(defun make-hydra_error_checking_untyped_term_variable_checking_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :name) args)))
(defun hydra_error_checking_untyped_term_variable_checking_error-path (rec) (cdr (assoc :path rec)))
(defun hydra_error_checking_untyped_term_variable_checking_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_checking_untyped_term_variable_checking_error (x) (copy-alist x))
(defun hydra_error_checking_untyped_term_variable_checking_error-p (x) (listp x))
(setf (symbol-function 'make-checking_untyped_term_variable_checking_error) #'make-hydra_error_checking_untyped_term_variable_checking_error)
(setf (symbol-function 'checking_untyped_term_variable_checking_error-path) #'hydra_error_checking_untyped_term_variable_checking_error-path)
(setf (symbol-function 'checking_untyped_term_variable_checking_error-name) #'hydra_error_checking_untyped_term_variable_checking_error-name)
(setf (symbol-function 'copy-checking_untyped_term_variable_checking_error) #'copy-hydra_error_checking_untyped_term_variable_checking_error)
(setf (symbol-function 'checking_untyped_term_variable_checking_error-p) #'hydra_error_checking_untyped_term_variable_checking_error-p)

;; hydra_error_core_constant_condition_error -> hydra_error_core_constant_condition_error
(defun make-hydra_error_core_constant_condition_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :value) args)))
(defun hydra_error_core_constant_condition_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_constant_condition_error-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_error_core_constant_condition_error (x) (copy-alist x))
(defun hydra_error_core_constant_condition_error-p (x) (listp x))
(setf (symbol-function 'make-core_constant_condition_error) #'make-hydra_error_core_constant_condition_error)
(setf (symbol-function 'core_constant_condition_error-location) #'hydra_error_core_constant_condition_error-location)
(setf (symbol-function 'core_constant_condition_error-value) #'hydra_error_core_constant_condition_error-value)
(setf (symbol-function 'copy-core_constant_condition_error) #'copy-hydra_error_core_constant_condition_error)
(setf (symbol-function 'core_constant_condition_error-p) #'hydra_error_core_constant_condition_error-p)

;; hydra_error_core_duplicate_binding_error -> hydra_error_core_duplicate_binding_error
(defun make-hydra_error_core_duplicate_binding_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_duplicate_binding_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_duplicate_binding_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_duplicate_binding_error (x) (copy-alist x))
(defun hydra_error_core_duplicate_binding_error-p (x) (listp x))
(setf (symbol-function 'make-core_duplicate_binding_error) #'make-hydra_error_core_duplicate_binding_error)
(setf (symbol-function 'core_duplicate_binding_error-location) #'hydra_error_core_duplicate_binding_error-location)
(setf (symbol-function 'core_duplicate_binding_error-name) #'hydra_error_core_duplicate_binding_error-name)
(setf (symbol-function 'copy-core_duplicate_binding_error) #'copy-hydra_error_core_duplicate_binding_error)
(setf (symbol-function 'core_duplicate_binding_error-p) #'hydra_error_core_duplicate_binding_error-p)

;; hydra_error_core_duplicate_field_error -> hydra_error_core_duplicate_field_error
(defun make-hydra_error_core_duplicate_field_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_duplicate_field_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_duplicate_field_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_duplicate_field_error (x) (copy-alist x))
(defun hydra_error_core_duplicate_field_error-p (x) (listp x))
(setf (symbol-function 'make-core_duplicate_field_error) #'make-hydra_error_core_duplicate_field_error)
(setf (symbol-function 'core_duplicate_field_error-location) #'hydra_error_core_duplicate_field_error-location)
(setf (symbol-function 'core_duplicate_field_error-name) #'hydra_error_core_duplicate_field_error-name)
(setf (symbol-function 'copy-core_duplicate_field_error) #'copy-hydra_error_core_duplicate_field_error)
(setf (symbol-function 'core_duplicate_field_error-p) #'hydra_error_core_duplicate_field_error-p)

;; hydra_error_core_duplicate_record_type_field_names_error -> hydra_error_core_duplicate_record_type_field_names_error
(defun make-hydra_error_core_duplicate_record_type_field_names_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_duplicate_record_type_field_names_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_duplicate_record_type_field_names_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_duplicate_record_type_field_names_error (x) (copy-alist x))
(defun hydra_error_core_duplicate_record_type_field_names_error-p (x) (listp x))
(setf (symbol-function 'make-core_duplicate_record_type_field_names_error) #'make-hydra_error_core_duplicate_record_type_field_names_error)
(setf (symbol-function 'core_duplicate_record_type_field_names_error-location) #'hydra_error_core_duplicate_record_type_field_names_error-location)
(setf (symbol-function 'core_duplicate_record_type_field_names_error-name) #'hydra_error_core_duplicate_record_type_field_names_error-name)
(setf (symbol-function 'copy-core_duplicate_record_type_field_names_error) #'copy-hydra_error_core_duplicate_record_type_field_names_error)
(setf (symbol-function 'core_duplicate_record_type_field_names_error-p) #'hydra_error_core_duplicate_record_type_field_names_error-p)

;; hydra_error_core_duplicate_union_type_field_names_error -> hydra_error_core_duplicate_union_type_field_names_error
(defun make-hydra_error_core_duplicate_union_type_field_names_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_duplicate_union_type_field_names_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_duplicate_union_type_field_names_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_duplicate_union_type_field_names_error (x) (copy-alist x))
(defun hydra_error_core_duplicate_union_type_field_names_error-p (x) (listp x))
(setf (symbol-function 'make-core_duplicate_union_type_field_names_error) #'make-hydra_error_core_duplicate_union_type_field_names_error)
(setf (symbol-function 'core_duplicate_union_type_field_names_error-location) #'hydra_error_core_duplicate_union_type_field_names_error-location)
(setf (symbol-function 'core_duplicate_union_type_field_names_error-name) #'hydra_error_core_duplicate_union_type_field_names_error-name)
(setf (symbol-function 'copy-core_duplicate_union_type_field_names_error) #'copy-hydra_error_core_duplicate_union_type_field_names_error)
(setf (symbol-function 'core_duplicate_union_type_field_names_error-p) #'hydra_error_core_duplicate_union_type_field_names_error-p)

;; hydra_error_core_empty_case_statement_error -> hydra_error_core_empty_case_statement_error
(defun make-hydra_error_core_empty_case_statement_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :type_name) args)))
(defun hydra_error_core_empty_case_statement_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_empty_case_statement_error-type_name (rec) (cdr (assoc :type_name rec)))
(defun copy-hydra_error_core_empty_case_statement_error (x) (copy-alist x))
(defun hydra_error_core_empty_case_statement_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_case_statement_error) #'make-hydra_error_core_empty_case_statement_error)
(setf (symbol-function 'core_empty_case_statement_error-location) #'hydra_error_core_empty_case_statement_error-location)
(setf (symbol-function 'core_empty_case_statement_error-type_name) #'hydra_error_core_empty_case_statement_error-type_name)
(setf (symbol-function 'copy-core_empty_case_statement_error) #'copy-hydra_error_core_empty_case_statement_error)
(setf (symbol-function 'core_empty_case_statement_error-p) #'hydra_error_core_empty_case_statement_error-p)

;; hydra_error_core_empty_let_bindings_error -> hydra_error_core_empty_let_bindings_error
(defun make-hydra_error_core_empty_let_bindings_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_let_bindings_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_let_bindings_error (x) (copy-alist x))
(defun hydra_error_core_empty_let_bindings_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_let_bindings_error) #'make-hydra_error_core_empty_let_bindings_error)
(setf (symbol-function 'core_empty_let_bindings_error-location) #'hydra_error_core_empty_let_bindings_error-location)
(setf (symbol-function 'copy-core_empty_let_bindings_error) #'copy-hydra_error_core_empty_let_bindings_error)
(setf (symbol-function 'core_empty_let_bindings_error-p) #'hydra_error_core_empty_let_bindings_error-p)

;; hydra_error_core_empty_record_type_error -> hydra_error_core_empty_record_type_error
(defun make-hydra_error_core_empty_record_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_record_type_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_record_type_error (x) (copy-alist x))
(defun hydra_error_core_empty_record_type_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_record_type_error) #'make-hydra_error_core_empty_record_type_error)
(setf (symbol-function 'core_empty_record_type_error-location) #'hydra_error_core_empty_record_type_error-location)
(setf (symbol-function 'copy-core_empty_record_type_error) #'copy-hydra_error_core_empty_record_type_error)
(setf (symbol-function 'core_empty_record_type_error-p) #'hydra_error_core_empty_record_type_error-p)

;; hydra_error_core_empty_term_annotation_error -> hydra_error_core_empty_term_annotation_error
(defun make-hydra_error_core_empty_term_annotation_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_term_annotation_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_term_annotation_error (x) (copy-alist x))
(defun hydra_error_core_empty_term_annotation_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_term_annotation_error) #'make-hydra_error_core_empty_term_annotation_error)
(setf (symbol-function 'core_empty_term_annotation_error-location) #'hydra_error_core_empty_term_annotation_error-location)
(setf (symbol-function 'copy-core_empty_term_annotation_error) #'copy-hydra_error_core_empty_term_annotation_error)
(setf (symbol-function 'core_empty_term_annotation_error-p) #'hydra_error_core_empty_term_annotation_error-p)

;; hydra_error_core_empty_type_annotation_error -> hydra_error_core_empty_type_annotation_error
(defun make-hydra_error_core_empty_type_annotation_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_type_annotation_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_type_annotation_error (x) (copy-alist x))
(defun hydra_error_core_empty_type_annotation_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_type_annotation_error) #'make-hydra_error_core_empty_type_annotation_error)
(setf (symbol-function 'core_empty_type_annotation_error-location) #'hydra_error_core_empty_type_annotation_error-location)
(setf (symbol-function 'copy-core_empty_type_annotation_error) #'copy-hydra_error_core_empty_type_annotation_error)
(setf (symbol-function 'core_empty_type_annotation_error-p) #'hydra_error_core_empty_type_annotation_error-p)

;; hydra_error_core_empty_type_name_in_term_error -> hydra_error_core_empty_type_name_in_term_error
(defun make-hydra_error_core_empty_type_name_in_term_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_type_name_in_term_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_type_name_in_term_error (x) (copy-alist x))
(defun hydra_error_core_empty_type_name_in_term_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_type_name_in_term_error) #'make-hydra_error_core_empty_type_name_in_term_error)
(setf (symbol-function 'core_empty_type_name_in_term_error-location) #'hydra_error_core_empty_type_name_in_term_error-location)
(setf (symbol-function 'copy-core_empty_type_name_in_term_error) #'copy-hydra_error_core_empty_type_name_in_term_error)
(setf (symbol-function 'core_empty_type_name_in_term_error-p) #'hydra_error_core_empty_type_name_in_term_error-p)

;; hydra_error_core_empty_union_type_error -> hydra_error_core_empty_union_type_error
(defun make-hydra_error_core_empty_union_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_empty_union_type_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_empty_union_type_error (x) (copy-alist x))
(defun hydra_error_core_empty_union_type_error-p (x) (listp x))
(setf (symbol-function 'make-core_empty_union_type_error) #'make-hydra_error_core_empty_union_type_error)
(setf (symbol-function 'core_empty_union_type_error-location) #'hydra_error_core_empty_union_type_error-location)
(setf (symbol-function 'copy-core_empty_union_type_error) #'copy-hydra_error_core_empty_union_type_error)
(setf (symbol-function 'core_empty_union_type_error-p) #'hydra_error_core_empty_union_type_error-p)

;; hydra_error_core_invalid_forall_parameter_name_error -> hydra_error_core_invalid_forall_parameter_name_error
(defun make-hydra_error_core_invalid_forall_parameter_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_invalid_forall_parameter_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_invalid_forall_parameter_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_invalid_forall_parameter_name_error (x) (copy-alist x))
(defun hydra_error_core_invalid_forall_parameter_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_invalid_forall_parameter_name_error) #'make-hydra_error_core_invalid_forall_parameter_name_error)
(setf (symbol-function 'core_invalid_forall_parameter_name_error-location) #'hydra_error_core_invalid_forall_parameter_name_error-location)
(setf (symbol-function 'core_invalid_forall_parameter_name_error-name) #'hydra_error_core_invalid_forall_parameter_name_error-name)
(setf (symbol-function 'copy-core_invalid_forall_parameter_name_error) #'copy-hydra_error_core_invalid_forall_parameter_name_error)
(setf (symbol-function 'core_invalid_forall_parameter_name_error-p) #'hydra_error_core_invalid_forall_parameter_name_error-p)

;; hydra_error_core_invalid_lambda_parameter_name_error -> hydra_error_core_invalid_lambda_parameter_name_error
(defun make-hydra_error_core_invalid_lambda_parameter_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_invalid_lambda_parameter_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_invalid_lambda_parameter_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_invalid_lambda_parameter_name_error (x) (copy-alist x))
(defun hydra_error_core_invalid_lambda_parameter_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_invalid_lambda_parameter_name_error) #'make-hydra_error_core_invalid_lambda_parameter_name_error)
(setf (symbol-function 'core_invalid_lambda_parameter_name_error-location) #'hydra_error_core_invalid_lambda_parameter_name_error-location)
(setf (symbol-function 'core_invalid_lambda_parameter_name_error-name) #'hydra_error_core_invalid_lambda_parameter_name_error-name)
(setf (symbol-function 'copy-core_invalid_lambda_parameter_name_error) #'copy-hydra_error_core_invalid_lambda_parameter_name_error)
(setf (symbol-function 'core_invalid_lambda_parameter_name_error-p) #'hydra_error_core_invalid_lambda_parameter_name_error-p)

;; hydra_error_core_invalid_let_binding_name_error -> hydra_error_core_invalid_let_binding_name_error
(defun make-hydra_error_core_invalid_let_binding_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_invalid_let_binding_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_invalid_let_binding_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_invalid_let_binding_name_error (x) (copy-alist x))
(defun hydra_error_core_invalid_let_binding_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_invalid_let_binding_name_error) #'make-hydra_error_core_invalid_let_binding_name_error)
(setf (symbol-function 'core_invalid_let_binding_name_error-location) #'hydra_error_core_invalid_let_binding_name_error-location)
(setf (symbol-function 'core_invalid_let_binding_name_error-name) #'hydra_error_core_invalid_let_binding_name_error-name)
(setf (symbol-function 'copy-core_invalid_let_binding_name_error) #'copy-hydra_error_core_invalid_let_binding_name_error)
(setf (symbol-function 'core_invalid_let_binding_name_error-p) #'hydra_error_core_invalid_let_binding_name_error-p)

;; hydra_error_core_invalid_type_lambda_parameter_name_error -> hydra_error_core_invalid_type_lambda_parameter_name_error
(defun make-hydra_error_core_invalid_type_lambda_parameter_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_invalid_type_lambda_parameter_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_invalid_type_lambda_parameter_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_invalid_type_lambda_parameter_name_error (x) (copy-alist x))
(defun hydra_error_core_invalid_type_lambda_parameter_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_invalid_type_lambda_parameter_name_error) #'make-hydra_error_core_invalid_type_lambda_parameter_name_error)
(setf (symbol-function 'core_invalid_type_lambda_parameter_name_error-location) #'hydra_error_core_invalid_type_lambda_parameter_name_error-location)
(setf (symbol-function 'core_invalid_type_lambda_parameter_name_error-name) #'hydra_error_core_invalid_type_lambda_parameter_name_error-name)
(setf (symbol-function 'copy-core_invalid_type_lambda_parameter_name_error) #'copy-hydra_error_core_invalid_type_lambda_parameter_name_error)
(setf (symbol-function 'core_invalid_type_lambda_parameter_name_error-p) #'hydra_error_core_invalid_type_lambda_parameter_name_error-p)

;; hydra_error_core_invalid_type_scheme_variable_name_error -> hydra_error_core_invalid_type_scheme_variable_name_error
(defun make-hydra_error_core_invalid_type_scheme_variable_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_invalid_type_scheme_variable_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_invalid_type_scheme_variable_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_invalid_type_scheme_variable_name_error (x) (copy-alist x))
(defun hydra_error_core_invalid_type_scheme_variable_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_invalid_type_scheme_variable_name_error) #'make-hydra_error_core_invalid_type_scheme_variable_name_error)
(setf (symbol-function 'core_invalid_type_scheme_variable_name_error-location) #'hydra_error_core_invalid_type_scheme_variable_name_error-location)
(setf (symbol-function 'core_invalid_type_scheme_variable_name_error-name) #'hydra_error_core_invalid_type_scheme_variable_name_error-name)
(setf (symbol-function 'copy-core_invalid_type_scheme_variable_name_error) #'copy-hydra_error_core_invalid_type_scheme_variable_name_error)
(setf (symbol-function 'core_invalid_type_scheme_variable_name_error-p) #'hydra_error_core_invalid_type_scheme_variable_name_error-p)

;; hydra_error_core_literal_type_mismatch_error -> hydra_error_core_literal_type_mismatch_error
(defun make-hydra_error_core_literal_type_mismatch_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :expected_type :actual_type) args)))
(defun hydra_error_core_literal_type_mismatch_error-expected_type (rec) (cdr (assoc :expected_type rec)))
(defun hydra_error_core_literal_type_mismatch_error-actual_type (rec) (cdr (assoc :actual_type rec)))
(defun copy-hydra_error_core_literal_type_mismatch_error (x) (copy-alist x))
(defun hydra_error_core_literal_type_mismatch_error-p (x) (listp x))
(setf (symbol-function 'make-core_literal_type_mismatch_error) #'make-hydra_error_core_literal_type_mismatch_error)
(setf (symbol-function 'core_literal_type_mismatch_error-expected_type) #'hydra_error_core_literal_type_mismatch_error-expected_type)
(setf (symbol-function 'core_literal_type_mismatch_error-actual_type) #'hydra_error_core_literal_type_mismatch_error-actual_type)
(setf (symbol-function 'copy-core_literal_type_mismatch_error) #'copy-hydra_error_core_literal_type_mismatch_error)
(setf (symbol-function 'core_literal_type_mismatch_error-p) #'hydra_error_core_literal_type_mismatch_error-p)

;; hydra_error_core_nested_term_annotation_error -> hydra_error_core_nested_term_annotation_error
(defun make-hydra_error_core_nested_term_annotation_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_nested_term_annotation_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_nested_term_annotation_error (x) (copy-alist x))
(defun hydra_error_core_nested_term_annotation_error-p (x) (listp x))
(setf (symbol-function 'make-core_nested_term_annotation_error) #'make-hydra_error_core_nested_term_annotation_error)
(setf (symbol-function 'core_nested_term_annotation_error-location) #'hydra_error_core_nested_term_annotation_error-location)
(setf (symbol-function 'copy-core_nested_term_annotation_error) #'copy-hydra_error_core_nested_term_annotation_error)
(setf (symbol-function 'core_nested_term_annotation_error-p) #'hydra_error_core_nested_term_annotation_error-p)

;; hydra_error_core_nested_type_annotation_error -> hydra_error_core_nested_type_annotation_error
(defun make-hydra_error_core_nested_type_annotation_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_nested_type_annotation_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_nested_type_annotation_error (x) (copy-alist x))
(defun hydra_error_core_nested_type_annotation_error-p (x) (listp x))
(setf (symbol-function 'make-core_nested_type_annotation_error) #'make-hydra_error_core_nested_type_annotation_error)
(setf (symbol-function 'core_nested_type_annotation_error-location) #'hydra_error_core_nested_type_annotation_error-location)
(setf (symbol-function 'copy-core_nested_type_annotation_error) #'copy-hydra_error_core_nested_type_annotation_error)
(setf (symbol-function 'core_nested_type_annotation_error-p) #'hydra_error_core_nested_type_annotation_error-p)

;; hydra_error_core_non_comparable_map_key_type_error -> hydra_error_core_non_comparable_map_key_type_error
(defun make-hydra_error_core_non_comparable_map_key_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :key_type) args)))
(defun hydra_error_core_non_comparable_map_key_type_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_non_comparable_map_key_type_error-key_type (rec) (cdr (assoc :key_type rec)))
(defun copy-hydra_error_core_non_comparable_map_key_type_error (x) (copy-alist x))
(defun hydra_error_core_non_comparable_map_key_type_error-p (x) (listp x))
(setf (symbol-function 'make-core_non_comparable_map_key_type_error) #'make-hydra_error_core_non_comparable_map_key_type_error)
(setf (symbol-function 'core_non_comparable_map_key_type_error-location) #'hydra_error_core_non_comparable_map_key_type_error-location)
(setf (symbol-function 'core_non_comparable_map_key_type_error-key_type) #'hydra_error_core_non_comparable_map_key_type_error-key_type)
(setf (symbol-function 'copy-core_non_comparable_map_key_type_error) #'copy-hydra_error_core_non_comparable_map_key_type_error)
(setf (symbol-function 'core_non_comparable_map_key_type_error-p) #'hydra_error_core_non_comparable_map_key_type_error-p)

;; hydra_error_core_non_comparable_set_element_type_error -> hydra_error_core_non_comparable_set_element_type_error
(defun make-hydra_error_core_non_comparable_set_element_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :element_type) args)))
(defun hydra_error_core_non_comparable_set_element_type_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_non_comparable_set_element_type_error-element_type (rec) (cdr (assoc :element_type rec)))
(defun copy-hydra_error_core_non_comparable_set_element_type_error (x) (copy-alist x))
(defun hydra_error_core_non_comparable_set_element_type_error-p (x) (listp x))
(setf (symbol-function 'make-core_non_comparable_set_element_type_error) #'make-hydra_error_core_non_comparable_set_element_type_error)
(setf (symbol-function 'core_non_comparable_set_element_type_error-location) #'hydra_error_core_non_comparable_set_element_type_error-location)
(setf (symbol-function 'core_non_comparable_set_element_type_error-element_type) #'hydra_error_core_non_comparable_set_element_type_error-element_type)
(setf (symbol-function 'copy-core_non_comparable_set_element_type_error) #'copy-hydra_error_core_non_comparable_set_element_type_error)
(setf (symbol-function 'core_non_comparable_set_element_type_error-p) #'hydra_error_core_non_comparable_set_element_type_error-p)

;; hydra_error_core_redundant_wrap_unwrap_error -> hydra_error_core_redundant_wrap_unwrap_error
(defun make-hydra_error_core_redundant_wrap_unwrap_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :type_name) args)))
(defun hydra_error_core_redundant_wrap_unwrap_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_redundant_wrap_unwrap_error-type_name (rec) (cdr (assoc :type_name rec)))
(defun copy-hydra_error_core_redundant_wrap_unwrap_error (x) (copy-alist x))
(defun hydra_error_core_redundant_wrap_unwrap_error-p (x) (listp x))
(setf (symbol-function 'make-core_redundant_wrap_unwrap_error) #'make-hydra_error_core_redundant_wrap_unwrap_error)
(setf (symbol-function 'core_redundant_wrap_unwrap_error-location) #'hydra_error_core_redundant_wrap_unwrap_error-location)
(setf (symbol-function 'core_redundant_wrap_unwrap_error-type_name) #'hydra_error_core_redundant_wrap_unwrap_error-type_name)
(setf (symbol-function 'copy-core_redundant_wrap_unwrap_error) #'copy-hydra_error_core_redundant_wrap_unwrap_error)
(setf (symbol-function 'core_redundant_wrap_unwrap_error-p) #'hydra_error_core_redundant_wrap_unwrap_error-p)

;; hydra_error_core_self_application_error -> hydra_error_core_self_application_error
(defun make-hydra_error_core_self_application_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_self_application_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_self_application_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_self_application_error (x) (copy-alist x))
(defun hydra_error_core_self_application_error-p (x) (listp x))
(setf (symbol-function 'make-core_self_application_error) #'make-hydra_error_core_self_application_error)
(setf (symbol-function 'core_self_application_error-location) #'hydra_error_core_self_application_error-location)
(setf (symbol-function 'core_self_application_error-name) #'hydra_error_core_self_application_error-name)
(setf (symbol-function 'copy-core_self_application_error) #'copy-hydra_error_core_self_application_error)
(setf (symbol-function 'core_self_application_error-p) #'hydra_error_core_self_application_error-p)

;; hydra_error_core_single_variant_union_error -> hydra_error_core_single_variant_union_error
(defun make-hydra_error_core_single_variant_union_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :field_name) args)))
(defun hydra_error_core_single_variant_union_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_single_variant_union_error-field_name (rec) (cdr (assoc :field_name rec)))
(defun copy-hydra_error_core_single_variant_union_error (x) (copy-alist x))
(defun hydra_error_core_single_variant_union_error-p (x) (listp x))
(setf (symbol-function 'make-core_single_variant_union_error) #'make-hydra_error_core_single_variant_union_error)
(setf (symbol-function 'core_single_variant_union_error-location) #'hydra_error_core_single_variant_union_error-location)
(setf (symbol-function 'core_single_variant_union_error-field_name) #'hydra_error_core_single_variant_union_error-field_name)
(setf (symbol-function 'copy-core_single_variant_union_error) #'copy-hydra_error_core_single_variant_union_error)
(setf (symbol-function 'core_single_variant_union_error-p) #'hydra_error_core_single_variant_union_error-p)

;; hydra_error_core_term_variable_shadowing_error -> hydra_error_core_term_variable_shadowing_error
(defun make-hydra_error_core_term_variable_shadowing_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_term_variable_shadowing_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_term_variable_shadowing_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_term_variable_shadowing_error (x) (copy-alist x))
(defun hydra_error_core_term_variable_shadowing_error-p (x) (listp x))
(setf (symbol-function 'make-core_term_variable_shadowing_error) #'make-hydra_error_core_term_variable_shadowing_error)
(setf (symbol-function 'core_term_variable_shadowing_error-location) #'hydra_error_core_term_variable_shadowing_error-location)
(setf (symbol-function 'core_term_variable_shadowing_error-name) #'hydra_error_core_term_variable_shadowing_error-name)
(setf (symbol-function 'copy-core_term_variable_shadowing_error) #'copy-hydra_error_core_term_variable_shadowing_error)
(setf (symbol-function 'core_term_variable_shadowing_error-p) #'hydra_error_core_term_variable_shadowing_error-p)

;; hydra_error_core_type_variable_shadowing_in_forall_error -> hydra_error_core_type_variable_shadowing_in_forall_error
(defun make-hydra_error_core_type_variable_shadowing_in_forall_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_type_variable_shadowing_in_forall_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_type_variable_shadowing_in_forall_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_type_variable_shadowing_in_forall_error (x) (copy-alist x))
(defun hydra_error_core_type_variable_shadowing_in_forall_error-p (x) (listp x))
(setf (symbol-function 'make-core_type_variable_shadowing_in_forall_error) #'make-hydra_error_core_type_variable_shadowing_in_forall_error)
(setf (symbol-function 'core_type_variable_shadowing_in_forall_error-location) #'hydra_error_core_type_variable_shadowing_in_forall_error-location)
(setf (symbol-function 'core_type_variable_shadowing_in_forall_error-name) #'hydra_error_core_type_variable_shadowing_in_forall_error-name)
(setf (symbol-function 'copy-core_type_variable_shadowing_in_forall_error) #'copy-hydra_error_core_type_variable_shadowing_in_forall_error)
(setf (symbol-function 'core_type_variable_shadowing_in_forall_error-p) #'hydra_error_core_type_variable_shadowing_in_forall_error-p)

;; hydra_error_core_type_variable_shadowing_in_type_lambda_error -> hydra_error_core_type_variable_shadowing_in_type_lambda_error
(defun make-hydra_error_core_type_variable_shadowing_in_type_lambda_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_type_variable_shadowing_in_type_lambda_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_type_variable_shadowing_in_type_lambda_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_type_variable_shadowing_in_type_lambda_error (x) (copy-alist x))
(defun hydra_error_core_type_variable_shadowing_in_type_lambda_error-p (x) (listp x))
(setf (symbol-function 'make-core_type_variable_shadowing_in_type_lambda_error) #'make-hydra_error_core_type_variable_shadowing_in_type_lambda_error)
(setf (symbol-function 'core_type_variable_shadowing_in_type_lambda_error-location) #'hydra_error_core_type_variable_shadowing_in_type_lambda_error-location)
(setf (symbol-function 'core_type_variable_shadowing_in_type_lambda_error-name) #'hydra_error_core_type_variable_shadowing_in_type_lambda_error-name)
(setf (symbol-function 'copy-core_type_variable_shadowing_in_type_lambda_error) #'copy-hydra_error_core_type_variable_shadowing_in_type_lambda_error)
(setf (symbol-function 'core_type_variable_shadowing_in_type_lambda_error-p) #'hydra_error_core_type_variable_shadowing_in_type_lambda_error-p)

;; hydra_error_core_undefined_field_error -> hydra_error_core_undefined_field_error
(defun make-hydra_error_core_undefined_field_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :field_name :type_name) args)))
(defun hydra_error_core_undefined_field_error-field_name (rec) (cdr (assoc :field_name rec)))
(defun hydra_error_core_undefined_field_error-type_name (rec) (cdr (assoc :type_name rec)))
(defun copy-hydra_error_core_undefined_field_error (x) (copy-alist x))
(defun hydra_error_core_undefined_field_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_field_error) #'make-hydra_error_core_undefined_field_error)
(setf (symbol-function 'core_undefined_field_error-field_name) #'hydra_error_core_undefined_field_error-field_name)
(setf (symbol-function 'core_undefined_field_error-type_name) #'hydra_error_core_undefined_field_error-type_name)
(setf (symbol-function 'copy-core_undefined_field_error) #'copy-hydra_error_core_undefined_field_error)
(setf (symbol-function 'core_undefined_field_error-p) #'hydra_error_core_undefined_field_error-p)

;; hydra_error_core_undefined_term_variable_error -> hydra_error_core_undefined_term_variable_error
(defun make-hydra_error_core_undefined_term_variable_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_undefined_term_variable_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_undefined_term_variable_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_undefined_term_variable_error (x) (copy-alist x))
(defun hydra_error_core_undefined_term_variable_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_term_variable_error) #'make-hydra_error_core_undefined_term_variable_error)
(setf (symbol-function 'core_undefined_term_variable_error-location) #'hydra_error_core_undefined_term_variable_error-location)
(setf (symbol-function 'core_undefined_term_variable_error-name) #'hydra_error_core_undefined_term_variable_error-name)
(setf (symbol-function 'copy-core_undefined_term_variable_error) #'copy-hydra_error_core_undefined_term_variable_error)
(setf (symbol-function 'core_undefined_term_variable_error-p) #'hydra_error_core_undefined_term_variable_error-p)

;; hydra_error_core_undefined_type_variable_error -> hydra_error_core_undefined_type_variable_error
(defun make-hydra_error_core_undefined_type_variable_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_undefined_type_variable_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_undefined_type_variable_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_undefined_type_variable_error (x) (copy-alist x))
(defun hydra_error_core_undefined_type_variable_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_type_variable_error) #'make-hydra_error_core_undefined_type_variable_error)
(setf (symbol-function 'core_undefined_type_variable_error-location) #'hydra_error_core_undefined_type_variable_error-location)
(setf (symbol-function 'core_undefined_type_variable_error-name) #'hydra_error_core_undefined_type_variable_error-name)
(setf (symbol-function 'copy-core_undefined_type_variable_error) #'copy-hydra_error_core_undefined_type_variable_error)
(setf (symbol-function 'core_undefined_type_variable_error-p) #'hydra_error_core_undefined_type_variable_error-p)

;; hydra_error_core_undefined_type_variable_in_binding_type_error -> hydra_error_core_undefined_type_variable_in_binding_type_error
(defun make-hydra_error_core_undefined_type_variable_in_binding_type_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_undefined_type_variable_in_binding_type_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_undefined_type_variable_in_binding_type_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_undefined_type_variable_in_binding_type_error (x) (copy-alist x))
(defun hydra_error_core_undefined_type_variable_in_binding_type_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_type_variable_in_binding_type_error) #'make-hydra_error_core_undefined_type_variable_in_binding_type_error)
(setf (symbol-function 'core_undefined_type_variable_in_binding_type_error-location) #'hydra_error_core_undefined_type_variable_in_binding_type_error-location)
(setf (symbol-function 'core_undefined_type_variable_in_binding_type_error-name) #'hydra_error_core_undefined_type_variable_in_binding_type_error-name)
(setf (symbol-function 'copy-core_undefined_type_variable_in_binding_type_error) #'copy-hydra_error_core_undefined_type_variable_in_binding_type_error)
(setf (symbol-function 'core_undefined_type_variable_in_binding_type_error-p) #'hydra_error_core_undefined_type_variable_in_binding_type_error-p)

;; hydra_error_core_undefined_type_variable_in_lambda_domain_error -> hydra_error_core_undefined_type_variable_in_lambda_domain_error
(defun make-hydra_error_core_undefined_type_variable_in_lambda_domain_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_undefined_type_variable_in_lambda_domain_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_undefined_type_variable_in_lambda_domain_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_undefined_type_variable_in_lambda_domain_error (x) (copy-alist x))
(defun hydra_error_core_undefined_type_variable_in_lambda_domain_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_type_variable_in_lambda_domain_error) #'make-hydra_error_core_undefined_type_variable_in_lambda_domain_error)
(setf (symbol-function 'core_undefined_type_variable_in_lambda_domain_error-location) #'hydra_error_core_undefined_type_variable_in_lambda_domain_error-location)
(setf (symbol-function 'core_undefined_type_variable_in_lambda_domain_error-name) #'hydra_error_core_undefined_type_variable_in_lambda_domain_error-name)
(setf (symbol-function 'copy-core_undefined_type_variable_in_lambda_domain_error) #'copy-hydra_error_core_undefined_type_variable_in_lambda_domain_error)
(setf (symbol-function 'core_undefined_type_variable_in_lambda_domain_error-p) #'hydra_error_core_undefined_type_variable_in_lambda_domain_error-p)

;; hydra_error_core_undefined_type_variable_in_type_application_error -> hydra_error_core_undefined_type_variable_in_type_application_error
(defun make-hydra_error_core_undefined_type_variable_in_type_application_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_undefined_type_variable_in_type_application_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_undefined_type_variable_in_type_application_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_undefined_type_variable_in_type_application_error (x) (copy-alist x))
(defun hydra_error_core_undefined_type_variable_in_type_application_error-p (x) (listp x))
(setf (symbol-function 'make-core_undefined_type_variable_in_type_application_error) #'make-hydra_error_core_undefined_type_variable_in_type_application_error)
(setf (symbol-function 'core_undefined_type_variable_in_type_application_error-location) #'hydra_error_core_undefined_type_variable_in_type_application_error-location)
(setf (symbol-function 'core_undefined_type_variable_in_type_application_error-name) #'hydra_error_core_undefined_type_variable_in_type_application_error-name)
(setf (symbol-function 'copy-core_undefined_type_variable_in_type_application_error) #'copy-hydra_error_core_undefined_type_variable_in_type_application_error)
(setf (symbol-function 'core_undefined_type_variable_in_type_application_error-p) #'hydra_error_core_undefined_type_variable_in_type_application_error-p)

;; hydra_error_core_unexpected_term_variant_error -> hydra_error_core_unexpected_term_variant_error
(defun make-hydra_error_core_unexpected_term_variant_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :expected_variant :actual_term) args)))
(defun hydra_error_core_unexpected_term_variant_error-expected_variant (rec) (cdr (assoc :expected_variant rec)))
(defun hydra_error_core_unexpected_term_variant_error-actual_term (rec) (cdr (assoc :actual_term rec)))
(defun copy-hydra_error_core_unexpected_term_variant_error (x) (copy-alist x))
(defun hydra_error_core_unexpected_term_variant_error-p (x) (listp x))
(setf (symbol-function 'make-core_unexpected_term_variant_error) #'make-hydra_error_core_unexpected_term_variant_error)
(setf (symbol-function 'core_unexpected_term_variant_error-expected_variant) #'hydra_error_core_unexpected_term_variant_error-expected_variant)
(setf (symbol-function 'core_unexpected_term_variant_error-actual_term) #'hydra_error_core_unexpected_term_variant_error-actual_term)
(setf (symbol-function 'copy-core_unexpected_term_variant_error) #'copy-hydra_error_core_unexpected_term_variant_error)
(setf (symbol-function 'core_unexpected_term_variant_error-p) #'hydra_error_core_unexpected_term_variant_error-p)

;; hydra_error_core_unexpected_type_variant_error -> hydra_error_core_unexpected_type_variant_error
(defun make-hydra_error_core_unexpected_type_variant_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :expected_variant :actual_type) args)))
(defun hydra_error_core_unexpected_type_variant_error-expected_variant (rec) (cdr (assoc :expected_variant rec)))
(defun hydra_error_core_unexpected_type_variant_error-actual_type (rec) (cdr (assoc :actual_type rec)))
(defun copy-hydra_error_core_unexpected_type_variant_error (x) (copy-alist x))
(defun hydra_error_core_unexpected_type_variant_error-p (x) (listp x))
(setf (symbol-function 'make-core_unexpected_type_variant_error) #'make-hydra_error_core_unexpected_type_variant_error)
(setf (symbol-function 'core_unexpected_type_variant_error-expected_variant) #'hydra_error_core_unexpected_type_variant_error-expected_variant)
(setf (symbol-function 'core_unexpected_type_variant_error-actual_type) #'hydra_error_core_unexpected_type_variant_error-actual_type)
(setf (symbol-function 'copy-core_unexpected_type_variant_error) #'copy-hydra_error_core_unexpected_type_variant_error)
(setf (symbol-function 'core_unexpected_type_variant_error-p) #'hydra_error_core_unexpected_type_variant_error-p)

;; hydra_error_core_unknown_primitive_name_error -> hydra_error_core_unknown_primitive_name_error
(defun make-hydra_error_core_unknown_primitive_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_unknown_primitive_name_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_unknown_primitive_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_unknown_primitive_name_error (x) (copy-alist x))
(defun hydra_error_core_unknown_primitive_name_error-p (x) (listp x))
(setf (symbol-function 'make-core_unknown_primitive_name_error) #'make-hydra_error_core_unknown_primitive_name_error)
(setf (symbol-function 'core_unknown_primitive_name_error-location) #'hydra_error_core_unknown_primitive_name_error-location)
(setf (symbol-function 'core_unknown_primitive_name_error-name) #'hydra_error_core_unknown_primitive_name_error-name)
(setf (symbol-function 'copy-core_unknown_primitive_name_error) #'copy-hydra_error_core_unknown_primitive_name_error)
(setf (symbol-function 'core_unknown_primitive_name_error-p) #'hydra_error_core_unknown_primitive_name_error-p)

;; hydra_error_core_unnecessary_identity_application_error -> hydra_error_core_unnecessary_identity_application_error
(defun make-hydra_error_core_unnecessary_identity_application_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_unnecessary_identity_application_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_unnecessary_identity_application_error (x) (copy-alist x))
(defun hydra_error_core_unnecessary_identity_application_error-p (x) (listp x))
(setf (symbol-function 'make-core_unnecessary_identity_application_error) #'make-hydra_error_core_unnecessary_identity_application_error)
(setf (symbol-function 'core_unnecessary_identity_application_error-location) #'hydra_error_core_unnecessary_identity_application_error-location)
(setf (symbol-function 'copy-core_unnecessary_identity_application_error) #'copy-hydra_error_core_unnecessary_identity_application_error)
(setf (symbol-function 'core_unnecessary_identity_application_error-p) #'hydra_error_core_unnecessary_identity_application_error-p)

;; hydra_error_core_untyped_term_variable_error -> hydra_error_core_untyped_term_variable_error
(defun make-hydra_error_core_untyped_term_variable_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location :name) args)))
(defun hydra_error_core_untyped_term_variable_error-location (rec) (cdr (assoc :location rec)))
(defun hydra_error_core_untyped_term_variable_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_core_untyped_term_variable_error (x) (copy-alist x))
(defun hydra_error_core_untyped_term_variable_error-p (x) (listp x))
(setf (symbol-function 'make-core_untyped_term_variable_error) #'make-hydra_error_core_untyped_term_variable_error)
(setf (symbol-function 'core_untyped_term_variable_error-location) #'hydra_error_core_untyped_term_variable_error-location)
(setf (symbol-function 'core_untyped_term_variable_error-name) #'hydra_error_core_untyped_term_variable_error-name)
(setf (symbol-function 'copy-core_untyped_term_variable_error) #'copy-hydra_error_core_untyped_term_variable_error)
(setf (symbol-function 'core_untyped_term_variable_error-p) #'hydra_error_core_untyped_term_variable_error-p)

;; hydra_error_core_void_in_non_bottom_position_error -> hydra_error_core_void_in_non_bottom_position_error
(defun make-hydra_error_core_void_in_non_bottom_position_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :location) args)))
(defun hydra_error_core_void_in_non_bottom_position_error-location (rec) (cdr (assoc :location rec)))
(defun copy-hydra_error_core_void_in_non_bottom_position_error (x) (copy-alist x))
(defun hydra_error_core_void_in_non_bottom_position_error-p (x) (listp x))
(setf (symbol-function 'make-core_void_in_non_bottom_position_error) #'make-hydra_error_core_void_in_non_bottom_position_error)
(setf (symbol-function 'core_void_in_non_bottom_position_error-location) #'hydra_error_core_void_in_non_bottom_position_error-location)
(setf (symbol-function 'copy-core_void_in_non_bottom_position_error) #'copy-hydra_error_core_void_in_non_bottom_position_error)
(setf (symbol-function 'core_void_in_non_bottom_position_error-p) #'hydra_error_core_void_in_non_bottom_position_error-p)

;; hydra_error_packaging_conflicting_module_name_error -> hydra_error_packaging_conflicting_module_name_error
(defun make-hydra_error_packaging_conflicting_module_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :first :second) args)))
(defun hydra_error_packaging_conflicting_module_name_error-first (rec) (cdr (assoc :first rec)))
(defun hydra_error_packaging_conflicting_module_name_error-second (rec) (cdr (assoc :second rec)))
(defun copy-hydra_error_packaging_conflicting_module_name_error (x) (copy-alist x))
(defun hydra_error_packaging_conflicting_module_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_conflicting_module_name_error) #'make-hydra_error_packaging_conflicting_module_name_error)
(setf (symbol-function 'packaging_conflicting_module_name_error-first) #'hydra_error_packaging_conflicting_module_name_error-first)
(setf (symbol-function 'packaging_conflicting_module_name_error-second) #'hydra_error_packaging_conflicting_module_name_error-second)
(setf (symbol-function 'copy-packaging_conflicting_module_name_error) #'copy-hydra_error_packaging_conflicting_module_name_error)
(setf (symbol-function 'packaging_conflicting_module_name_error-p) #'hydra_error_packaging_conflicting_module_name_error-p)

;; hydra_error_packaging_conflicting_variant_name_error -> hydra_error_packaging_conflicting_variant_name_error
(defun make-hydra_error_packaging_conflicting_variant_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :type_name :variant_name :conflicting_name) args)))
(defun hydra_error_packaging_conflicting_variant_name_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_conflicting_variant_name_error-type_name (rec) (cdr (assoc :type_name rec)))
(defun hydra_error_packaging_conflicting_variant_name_error-variant_name (rec) (cdr (assoc :variant_name rec)))
(defun hydra_error_packaging_conflicting_variant_name_error-conflicting_name (rec) (cdr (assoc :conflicting_name rec)))
(defun copy-hydra_error_packaging_conflicting_variant_name_error (x) (copy-alist x))
(defun hydra_error_packaging_conflicting_variant_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_conflicting_variant_name_error) #'make-hydra_error_packaging_conflicting_variant_name_error)
(setf (symbol-function 'packaging_conflicting_variant_name_error-module_name) #'hydra_error_packaging_conflicting_variant_name_error-module_name)
(setf (symbol-function 'packaging_conflicting_variant_name_error-type_name) #'hydra_error_packaging_conflicting_variant_name_error-type_name)
(setf (symbol-function 'packaging_conflicting_variant_name_error-variant_name) #'hydra_error_packaging_conflicting_variant_name_error-variant_name)
(setf (symbol-function 'packaging_conflicting_variant_name_error-conflicting_name) #'hydra_error_packaging_conflicting_variant_name_error-conflicting_name)
(setf (symbol-function 'copy-packaging_conflicting_variant_name_error) #'copy-hydra_error_packaging_conflicting_variant_name_error)
(setf (symbol-function 'packaging_conflicting_variant_name_error-p) #'hydra_error_packaging_conflicting_variant_name_error-p)

;; hydra_error_packaging_definition_not_in_module_name_error -> hydra_error_packaging_definition_not_in_module_name_error
(defun make-hydra_error_packaging_definition_not_in_module_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :name) args)))
(defun hydra_error_packaging_definition_not_in_module_name_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_definition_not_in_module_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_packaging_definition_not_in_module_name_error (x) (copy-alist x))
(defun hydra_error_packaging_definition_not_in_module_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_definition_not_in_module_name_error) #'make-hydra_error_packaging_definition_not_in_module_name_error)
(setf (symbol-function 'packaging_definition_not_in_module_name_error-module_name) #'hydra_error_packaging_definition_not_in_module_name_error-module_name)
(setf (symbol-function 'packaging_definition_not_in_module_name_error-name) #'hydra_error_packaging_definition_not_in_module_name_error-name)
(setf (symbol-function 'copy-packaging_definition_not_in_module_name_error) #'copy-hydra_error_packaging_definition_not_in_module_name_error)
(setf (symbol-function 'packaging_definition_not_in_module_name_error-p) #'hydra_error_packaging_definition_not_in_module_name_error-p)

;; hydra_error_packaging_definitions_out_of_order_error -> hydra_error_packaging_definitions_out_of_order_error
(defun make-hydra_error_packaging_definitions_out_of_order_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :preceding_name :following_name) args)))
(defun hydra_error_packaging_definitions_out_of_order_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_definitions_out_of_order_error-preceding_name (rec) (cdr (assoc :preceding_name rec)))
(defun hydra_error_packaging_definitions_out_of_order_error-following_name (rec) (cdr (assoc :following_name rec)))
(defun copy-hydra_error_packaging_definitions_out_of_order_error (x) (copy-alist x))
(defun hydra_error_packaging_definitions_out_of_order_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_definitions_out_of_order_error) #'make-hydra_error_packaging_definitions_out_of_order_error)
(setf (symbol-function 'packaging_definitions_out_of_order_error-module_name) #'hydra_error_packaging_definitions_out_of_order_error-module_name)
(setf (symbol-function 'packaging_definitions_out_of_order_error-preceding_name) #'hydra_error_packaging_definitions_out_of_order_error-preceding_name)
(setf (symbol-function 'packaging_definitions_out_of_order_error-following_name) #'hydra_error_packaging_definitions_out_of_order_error-following_name)
(setf (symbol-function 'copy-packaging_definitions_out_of_order_error) #'copy-hydra_error_packaging_definitions_out_of_order_error)
(setf (symbol-function 'packaging_definitions_out_of_order_error-p) #'hydra_error_packaging_definitions_out_of_order_error-p)

;; hydra_error_packaging_duplicate_definition_name_error -> hydra_error_packaging_duplicate_definition_name_error
(defun make-hydra_error_packaging_duplicate_definition_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :name) args)))
(defun hydra_error_packaging_duplicate_definition_name_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_duplicate_definition_name_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_packaging_duplicate_definition_name_error (x) (copy-alist x))
(defun hydra_error_packaging_duplicate_definition_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_duplicate_definition_name_error) #'make-hydra_error_packaging_duplicate_definition_name_error)
(setf (symbol-function 'packaging_duplicate_definition_name_error-module_name) #'hydra_error_packaging_duplicate_definition_name_error-module_name)
(setf (symbol-function 'packaging_duplicate_definition_name_error-name) #'hydra_error_packaging_duplicate_definition_name_error-name)
(setf (symbol-function 'copy-packaging_duplicate_definition_name_error) #'copy-hydra_error_packaging_duplicate_definition_name_error)
(setf (symbol-function 'packaging_duplicate_definition_name_error-p) #'hydra_error_packaging_duplicate_definition_name_error-p)

;; hydra_error_packaging_duplicate_module_name_error -> hydra_error_packaging_duplicate_module_name_error
(defun make-hydra_error_packaging_duplicate_module_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name) args)))
(defun hydra_error_packaging_duplicate_module_name_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun copy-hydra_error_packaging_duplicate_module_name_error (x) (copy-alist x))
(defun hydra_error_packaging_duplicate_module_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_duplicate_module_name_error) #'make-hydra_error_packaging_duplicate_module_name_error)
(setf (symbol-function 'packaging_duplicate_module_name_error-module_name) #'hydra_error_packaging_duplicate_module_name_error-module_name)
(setf (symbol-function 'copy-packaging_duplicate_module_name_error) #'copy-hydra_error_packaging_duplicate_module_name_error)
(setf (symbol-function 'packaging_duplicate_module_name_error-p) #'hydra_error_packaging_duplicate_module_name_error-p)

;; hydra_error_packaging_invalid_definition_name_error -> hydra_error_packaging_invalid_definition_name_error
(defun make-hydra_error_packaging_invalid_definition_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :name :expected_convention) args)))
(defun hydra_error_packaging_invalid_definition_name_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_invalid_definition_name_error-name (rec) (cdr (assoc :name rec)))
(defun hydra_error_packaging_invalid_definition_name_error-expected_convention (rec) (cdr (assoc :expected_convention rec)))
(defun copy-hydra_error_packaging_invalid_definition_name_error (x) (copy-alist x))
(defun hydra_error_packaging_invalid_definition_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_invalid_definition_name_error) #'make-hydra_error_packaging_invalid_definition_name_error)
(setf (symbol-function 'packaging_invalid_definition_name_error-module_name) #'hydra_error_packaging_invalid_definition_name_error-module_name)
(setf (symbol-function 'packaging_invalid_definition_name_error-name) #'hydra_error_packaging_invalid_definition_name_error-name)
(setf (symbol-function 'packaging_invalid_definition_name_error-expected_convention) #'hydra_error_packaging_invalid_definition_name_error-expected_convention)
(setf (symbol-function 'copy-packaging_invalid_definition_name_error) #'copy-hydra_error_packaging_invalid_definition_name_error)
(setf (symbol-function 'packaging_invalid_definition_name_error-p) #'hydra_error_packaging_invalid_definition_name_error-p)

;; hydra_error_packaging_invalid_module_name_convention_error -> hydra_error_packaging_invalid_module_name_convention_error
(defun make-hydra_error_packaging_invalid_module_name_convention_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name) args)))
(defun hydra_error_packaging_invalid_module_name_convention_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun copy-hydra_error_packaging_invalid_module_name_convention_error (x) (copy-alist x))
(defun hydra_error_packaging_invalid_module_name_convention_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_invalid_module_name_convention_error) #'make-hydra_error_packaging_invalid_module_name_convention_error)
(setf (symbol-function 'packaging_invalid_module_name_convention_error-module_name) #'hydra_error_packaging_invalid_module_name_convention_error-module_name)
(setf (symbol-function 'copy-packaging_invalid_module_name_convention_error) #'copy-hydra_error_packaging_invalid_module_name_convention_error)
(setf (symbol-function 'packaging_invalid_module_name_convention_error-p) #'hydra_error_packaging_invalid_module_name_convention_error-p)

;; hydra_error_packaging_invalid_package_name_error -> hydra_error_packaging_invalid_package_name_error
(defun make-hydra_error_packaging_invalid_package_name_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :package_name) args)))
(defun hydra_error_packaging_invalid_package_name_error-package_name (rec) (cdr (assoc :package_name rec)))
(defun copy-hydra_error_packaging_invalid_package_name_error (x) (copy-alist x))
(defun hydra_error_packaging_invalid_package_name_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_invalid_package_name_error) #'make-hydra_error_packaging_invalid_package_name_error)
(setf (symbol-function 'packaging_invalid_package_name_error-package_name) #'hydra_error_packaging_invalid_package_name_error-package_name)
(setf (symbol-function 'copy-packaging_invalid_package_name_error) #'copy-hydra_error_packaging_invalid_package_name_error)
(setf (symbol-function 'packaging_invalid_package_name_error-p) #'hydra_error_packaging_invalid_package_name_error-p)

;; hydra_error_packaging_missing_documentation_error -> hydra_error_packaging_missing_documentation_error
(defun make-hydra_error_packaging_missing_documentation_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :name) args)))
(defun hydra_error_packaging_missing_documentation_error-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_error_packaging_missing_documentation_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_error_packaging_missing_documentation_error (x) (copy-alist x))
(defun hydra_error_packaging_missing_documentation_error-p (x) (listp x))
(setf (symbol-function 'make-packaging_missing_documentation_error) #'make-hydra_error_packaging_missing_documentation_error)
(setf (symbol-function 'packaging_missing_documentation_error-module_name) #'hydra_error_packaging_missing_documentation_error-module_name)
(setf (symbol-function 'packaging_missing_documentation_error-name) #'hydra_error_packaging_missing_documentation_error-name)
(setf (symbol-function 'copy-packaging_missing_documentation_error) #'copy-hydra_error_packaging_missing_documentation_error)
(setf (symbol-function 'packaging_missing_documentation_error-p) #'hydra_error_packaging_missing_documentation_error-p)

;; hydra_errors_decoding_error -> hydra_errors_decoding_error
(defun make-hydra_errors_decoding_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_errors_decoding_error-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_errors_decoding_error (x) (copy-alist x))
(defun hydra_errors_decoding_error-p (x) (listp x))
(setf (symbol-function 'make-decoding_error) #'make-hydra_errors_decoding_error)
(setf (symbol-function 'decoding_error-value) #'hydra_errors_decoding_error-value)
(setf (symbol-function 'copy-decoding_error) #'copy-hydra_errors_decoding_error)
(setf (symbol-function 'decoding_error-p) #'hydra_errors_decoding_error-p)

;; hydra_errors_multiple_bindings_error -> hydra_errors_multiple_bindings_error
(defun make-hydra_errors_multiple_bindings_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name) args)))
(defun hydra_errors_multiple_bindings_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_errors_multiple_bindings_error (x) (copy-alist x))
(defun hydra_errors_multiple_bindings_error-p (x) (listp x))
(setf (symbol-function 'make-multiple_bindings_error) #'make-hydra_errors_multiple_bindings_error)
(setf (symbol-function 'multiple_bindings_error-name) #'hydra_errors_multiple_bindings_error-name)
(setf (symbol-function 'copy-multiple_bindings_error) #'copy-hydra_errors_multiple_bindings_error)
(setf (symbol-function 'multiple_bindings_error-p) #'hydra_errors_multiple_bindings_error-p)

;; hydra_errors_multiple_fields_error -> hydra_errors_multiple_fields_error
(defun make-hydra_errors_multiple_fields_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :field_name) args)))
(defun hydra_errors_multiple_fields_error-field_name (rec) (cdr (assoc :field_name rec)))
(defun copy-hydra_errors_multiple_fields_error (x) (copy-alist x))
(defun hydra_errors_multiple_fields_error-p (x) (listp x))
(setf (symbol-function 'make-multiple_fields_error) #'make-hydra_errors_multiple_fields_error)
(setf (symbol-function 'multiple_fields_error-field_name) #'hydra_errors_multiple_fields_error-field_name)
(setf (symbol-function 'copy-multiple_fields_error) #'copy-hydra_errors_multiple_fields_error)
(setf (symbol-function 'multiple_fields_error-p) #'hydra_errors_multiple_fields_error-p)

;; hydra_errors_no_matching_field_error -> hydra_errors_no_matching_field_error
(defun make-hydra_errors_no_matching_field_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :field_name) args)))
(defun hydra_errors_no_matching_field_error-field_name (rec) (cdr (assoc :field_name rec)))
(defun copy-hydra_errors_no_matching_field_error (x) (copy-alist x))
(defun hydra_errors_no_matching_field_error-p (x) (listp x))
(setf (symbol-function 'make-no_matching_field_error) #'make-hydra_errors_no_matching_field_error)
(setf (symbol-function 'no_matching_field_error-field_name) #'hydra_errors_no_matching_field_error-field_name)
(setf (symbol-function 'copy-no_matching_field_error) #'copy-hydra_errors_no_matching_field_error)
(setf (symbol-function 'no_matching_field_error-p) #'hydra_errors_no_matching_field_error-p)

;; hydra_errors_no_such_binding_error -> hydra_errors_no_such_binding_error
(defun make-hydra_errors_no_such_binding_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name) args)))
(defun hydra_errors_no_such_binding_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_errors_no_such_binding_error (x) (copy-alist x))
(defun hydra_errors_no_such_binding_error-p (x) (listp x))
(setf (symbol-function 'make-no_such_binding_error) #'make-hydra_errors_no_such_binding_error)
(setf (symbol-function 'no_such_binding_error-name) #'hydra_errors_no_such_binding_error-name)
(setf (symbol-function 'copy-no_such_binding_error) #'copy-hydra_errors_no_such_binding_error)
(setf (symbol-function 'no_such_binding_error-p) #'hydra_errors_no_such_binding_error-p)

;; hydra_errors_no_such_primitive_error -> hydra_errors_no_such_primitive_error
(defun make-hydra_errors_no_such_primitive_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name) args)))
(defun hydra_errors_no_such_primitive_error-name (rec) (cdr (assoc :name rec)))
(defun copy-hydra_errors_no_such_primitive_error (x) (copy-alist x))
(defun hydra_errors_no_such_primitive_error-p (x) (listp x))
(setf (symbol-function 'make-no_such_primitive_error) #'make-hydra_errors_no_such_primitive_error)
(setf (symbol-function 'no_such_primitive_error-name) #'hydra_errors_no_such_primitive_error-name)
(setf (symbol-function 'copy-no_such_primitive_error) #'copy-hydra_errors_no_such_primitive_error)
(setf (symbol-function 'no_such_primitive_error-p) #'hydra_errors_no_such_primitive_error-p)

;; hydra_errors_other_error -> hydra_errors_other_error
(defun make-hydra_errors_other_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_errors_other_error-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_errors_other_error (x) (copy-alist x))
(defun hydra_errors_other_error-p (x) (listp x))
(setf (symbol-function 'make-other_error) #'make-hydra_errors_other_error)
(setf (symbol-function 'other_error-value) #'hydra_errors_other_error-value)
(setf (symbol-function 'copy-other_error) #'copy-hydra_errors_other_error)
(setf (symbol-function 'other_error-p) #'hydra_errors_other_error-p)

;; hydra_errors_other_inference_error -> hydra_errors_other_inference_error
(defun make-hydra_errors_other_inference_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :message) args)))
(defun hydra_errors_other_inference_error-path (rec) (cdr (assoc :path rec)))
(defun hydra_errors_other_inference_error-message (rec) (cdr (assoc :message rec)))
(defun copy-hydra_errors_other_inference_error (x) (copy-alist x))
(defun hydra_errors_other_inference_error-p (x) (listp x))
(setf (symbol-function 'make-other_inference_error) #'make-hydra_errors_other_inference_error)
(setf (symbol-function 'other_inference_error-path) #'hydra_errors_other_inference_error-path)
(setf (symbol-function 'other_inference_error-message) #'hydra_errors_other_inference_error-message)
(setf (symbol-function 'copy-other_inference_error) #'copy-hydra_errors_other_inference_error)
(setf (symbol-function 'other_inference_error-p) #'hydra_errors_other_inference_error-p)

;; hydra_errors_other_resolution_error -> hydra_errors_other_resolution_error
(defun make-hydra_errors_other_resolution_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_errors_other_resolution_error-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_errors_other_resolution_error (x) (copy-alist x))
(defun hydra_errors_other_resolution_error-p (x) (listp x))
(setf (symbol-function 'make-other_resolution_error) #'make-hydra_errors_other_resolution_error)
(setf (symbol-function 'other_resolution_error-value) #'hydra_errors_other_resolution_error-value)
(setf (symbol-function 'copy-other_resolution_error) #'copy-hydra_errors_other_resolution_error)
(setf (symbol-function 'other_resolution_error-p) #'hydra_errors_other_resolution_error-p)

;; hydra_errors_unexpected_shape_error -> hydra_errors_unexpected_shape_error
(defun make-hydra_errors_unexpected_shape_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :expected :actual) args)))
(defun hydra_errors_unexpected_shape_error-expected (rec) (cdr (assoc :expected rec)))
(defun hydra_errors_unexpected_shape_error-actual (rec) (cdr (assoc :actual rec)))
(defun copy-hydra_errors_unexpected_shape_error (x) (copy-alist x))
(defun hydra_errors_unexpected_shape_error-p (x) (listp x))
(setf (symbol-function 'make-unexpected_shape_error) #'make-hydra_errors_unexpected_shape_error)
(setf (symbol-function 'unexpected_shape_error-expected) #'hydra_errors_unexpected_shape_error-expected)
(setf (symbol-function 'unexpected_shape_error-actual) #'hydra_errors_unexpected_shape_error-actual)
(setf (symbol-function 'copy-unexpected_shape_error) #'copy-hydra_errors_unexpected_shape_error)
(setf (symbol-function 'unexpected_shape_error-p) #'hydra_errors_unexpected_shape_error-p)

;; hydra_errors_unification_error -> hydra_errors_unification_error
(defun make-hydra_errors_unification_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :left_type :right_type :message) args)))
(defun hydra_errors_unification_error-left_type (rec) (cdr (assoc :left_type rec)))
(defun hydra_errors_unification_error-right_type (rec) (cdr (assoc :right_type rec)))
(defun hydra_errors_unification_error-message (rec) (cdr (assoc :message rec)))
(defun copy-hydra_errors_unification_error (x) (copy-alist x))
(defun hydra_errors_unification_error-p (x) (listp x))
(setf (symbol-function 'make-unification_error) #'make-hydra_errors_unification_error)
(setf (symbol-function 'unification_error-left_type) #'hydra_errors_unification_error-left_type)
(setf (symbol-function 'unification_error-right_type) #'hydra_errors_unification_error-right_type)
(setf (symbol-function 'unification_error-message) #'hydra_errors_unification_error-message)
(setf (symbol-function 'copy-unification_error) #'copy-hydra_errors_unification_error)
(setf (symbol-function 'unification_error-p) #'hydra_errors_unification_error-p)

;; hydra_errors_unification_inference_error -> hydra_errors_unification_inference_error
(defun make-hydra_errors_unification_inference_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :cause) args)))
(defun hydra_errors_unification_inference_error-path (rec) (cdr (assoc :path rec)))
(defun hydra_errors_unification_inference_error-cause (rec) (cdr (assoc :cause rec)))
(defun copy-hydra_errors_unification_inference_error (x) (copy-alist x))
(defun hydra_errors_unification_inference_error-p (x) (listp x))
(setf (symbol-function 'make-unification_inference_error) #'make-hydra_errors_unification_inference_error)
(setf (symbol-function 'unification_inference_error-path) #'hydra_errors_unification_inference_error-path)
(setf (symbol-function 'unification_inference_error-cause) #'hydra_errors_unification_inference_error-cause)
(setf (symbol-function 'copy-unification_inference_error) #'copy-hydra_errors_unification_inference_error)
(setf (symbol-function 'unification_inference_error-p) #'hydra_errors_unification_inference_error-p)

;; hydra_graph_library -> hydra_graph_library
(defun make-hydra_graph_library (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :namespace :prefix :primitives) args)))
(defun hydra_graph_library-namespace (rec) (cdr (assoc :namespace rec)))
(defun hydra_graph_library-prefix (rec) (cdr (assoc :prefix rec)))
(defun hydra_graph_library-primitives (rec) (cdr (assoc :primitives rec)))
(defun copy-hydra_graph_library (x) (copy-alist x))
(defun hydra_graph_library-p (x) (listp x))
(setf (symbol-function 'make-library) #'make-hydra_graph_library)
(setf (symbol-function 'library-namespace) #'hydra_graph_library-namespace)
(setf (symbol-function 'library-prefix) #'hydra_graph_library-prefix)
(setf (symbol-function 'library-primitives) #'hydra_graph_library-primitives)
(setf (symbol-function 'copy-library) #'copy-hydra_graph_library)
(setf (symbol-function 'library-p) #'hydra_graph_library-p)

;; hydra_graph_primitive -> hydra_graph_primitive
(defun make-hydra_graph_primitive (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :definition :implementation) args)))
(defun hydra_graph_primitive-definition (rec) (cdr (assoc :definition rec)))
(defun hydra_graph_primitive-implementation (rec) (cdr (assoc :implementation rec)))
(defun copy-hydra_graph_primitive (x) (copy-alist x))
(defun hydra_graph_primitive-p (x) (listp x))
(setf (symbol-function 'make-primitive) #'make-hydra_graph_primitive)
(setf (symbol-function 'primitive-definition) #'hydra_graph_primitive-definition)
(setf (symbol-function 'primitive-implementation) #'hydra_graph_primitive-implementation)
(setf (symbol-function 'copy-primitive) #'copy-hydra_graph_primitive)
(setf (symbol-function 'primitive-p) #'hydra_graph_primitive-p)

;; hydra_graph_term_coder -> hydra_graph_term_coder
(defun make-hydra_graph_term_coder (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type :encode :decode) args)))
(defun hydra_graph_term_coder-type (rec) (cdr (assoc :type rec)))
(defun hydra_graph_term_coder-encode (rec) (cdr (assoc :encode rec)))
(defun hydra_graph_term_coder-decode (rec) (cdr (assoc :decode rec)))
(defun copy-hydra_graph_term_coder (x) (copy-alist x))
(defun hydra_graph_term_coder-p (x) (listp x))
(setf (symbol-function 'make-term_coder) #'make-hydra_graph_term_coder)
(setf (symbol-function 'term_coder-type) #'hydra_graph_term_coder-type)
(setf (symbol-function 'term_coder-encode) #'hydra_graph_term_coder-encode)
(setf (symbol-function 'term_coder-decode) #'hydra_graph_term_coder-decode)
(setf (symbol-function 'copy-term_coder) #'copy-hydra_graph_term_coder)
(setf (symbol-function 'term_coder-p) #'hydra_graph_term_coder-p)

;; hydra_packaging_file_extension -> hydra_packaging_file_extension
(defun make-hydra_packaging_file_extension (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_packaging_file_extension-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_packaging_file_extension (x) (copy-alist x))
(defun hydra_packaging_file_extension-p (x) (listp x))
(setf (symbol-function 'make-file_extension) #'make-hydra_packaging_file_extension)
(setf (symbol-function 'file_extension-value) #'hydra_packaging_file_extension-value)
(setf (symbol-function 'copy-file_extension) #'copy-hydra_packaging_file_extension)
(setf (symbol-function 'file_extension-p) #'hydra_packaging_file_extension-p)

;; hydra_packaging_module -> hydra_packaging_module
(defun make-hydra_packaging_module (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :description :name :dependencies :definitions) args)))
(defun hydra_packaging_module-description (rec) (cdr (assoc :description rec)))
(defun hydra_packaging_module-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_module-dependencies (rec) (cdr (assoc :dependencies rec)))
(defun hydra_packaging_module-definitions (rec) (cdr (assoc :definitions rec)))
(defun copy-hydra_packaging_module (x) (copy-alist x))
(defun hydra_packaging_module-p (x) (listp x))
(setf (symbol-function 'make-module) #'make-hydra_packaging_module)
(setf (symbol-function 'module-description) #'hydra_packaging_module-description)
(setf (symbol-function 'module-name) #'hydra_packaging_module-name)
(setf (symbol-function 'module-dependencies) #'hydra_packaging_module-dependencies)
(setf (symbol-function 'module-definitions) #'hydra_packaging_module-definitions)
(setf (symbol-function 'copy-module) #'copy-hydra_packaging_module)
(setf (symbol-function 'module-p) #'hydra_packaging_module-p)

;; hydra_packaging_module_dependency -> hydra_packaging_module_dependency
(defun make-hydra_packaging_module_dependency (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module :package) args)))
(defun hydra_packaging_module_dependency-module (rec) (cdr (assoc :module rec)))
(defun hydra_packaging_module_dependency-package (rec) (cdr (assoc :package rec)))
(defun copy-hydra_packaging_module_dependency (x) (copy-alist x))
(defun hydra_packaging_module_dependency-p (x) (listp x))
(setf (symbol-function 'make-module_dependency) #'make-hydra_packaging_module_dependency)
(setf (symbol-function 'module_dependency-module) #'hydra_packaging_module_dependency-module)
(setf (symbol-function 'module_dependency-package) #'hydra_packaging_module_dependency-package)
(setf (symbol-function 'copy-module_dependency) #'copy-hydra_packaging_module_dependency)
(setf (symbol-function 'module_dependency-p) #'hydra_packaging_module_dependency-p)

;; hydra_packaging_module_name -> hydra_packaging_module_name
(defun make-hydra_packaging_module_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_packaging_module_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_packaging_module_name (x) (copy-alist x))
(defun hydra_packaging_module_name-p (x) (listp x))
(setf (symbol-function 'make-module_name) #'make-hydra_packaging_module_name)
(setf (symbol-function 'module_name-value) #'hydra_packaging_module_name-value)
(setf (symbol-function 'copy-module_name) #'copy-hydra_packaging_module_name)
(setf (symbol-function 'module_name-p) #'hydra_packaging_module_name-p)

;; hydra_packaging_package -> hydra_packaging_package
(defun make-hydra_packaging_package (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :modules :dependencies :description) args)))
(defun hydra_packaging_package-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_package-modules (rec) (cdr (assoc :modules rec)))
(defun hydra_packaging_package-dependencies (rec) (cdr (assoc :dependencies rec)))
(defun hydra_packaging_package-description (rec) (cdr (assoc :description rec)))
(defun copy-hydra_packaging_package (x) (copy-alist x))
(defun hydra_packaging_package-p (x) (listp x))

;; hydra_packaging_package_dependency -> hydra_packaging_package_dependency
(defun make-hydra_packaging_package_dependency (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :version) args)))
(defun hydra_packaging_package_dependency-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_package_dependency-version (rec) (cdr (assoc :version rec)))
(defun copy-hydra_packaging_package_dependency (x) (copy-alist x))
(defun hydra_packaging_package_dependency-p (x) (listp x))
(setf (symbol-function 'make-package_dependency) #'make-hydra_packaging_package_dependency)
(setf (symbol-function 'package_dependency-name) #'hydra_packaging_package_dependency-name)
(setf (symbol-function 'package_dependency-version) #'hydra_packaging_package_dependency-version)
(setf (symbol-function 'copy-package_dependency) #'copy-hydra_packaging_package_dependency)
(setf (symbol-function 'package_dependency-p) #'hydra_packaging_package_dependency-p)

;; hydra_packaging_package_name -> hydra_packaging_package_name
(defun make-hydra_packaging_package_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_packaging_package_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_packaging_package_name (x) (copy-alist x))
(defun hydra_packaging_package_name-p (x) (listp x))
(setf (symbol-function 'make-package_name) #'make-hydra_packaging_package_name)
(setf (symbol-function 'package_name-value) #'hydra_packaging_package_name-value)
(setf (symbol-function 'copy-package_name) #'copy-hydra_packaging_package_name)
(setf (symbol-function 'package_name-p) #'hydra_packaging_package_name-p)

;; hydra_packaging_primitive_definition -> hydra_packaging_primitive_definition
;; Fields (#319, 10 total): name, signature, description, comments, see_also,
;; is_pure, is_total, available_since, deprecated_since, default_implementation.
(defun make-hydra_packaging_primitive_definition (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '(:name :signature :description :comments :see_also
                     :is_pure :is_total :available_since :deprecated_since
                     :default_implementation)
                   args)))
(defun hydra_packaging_primitive_definition-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_primitive_definition-signature (rec) (cdr (assoc :signature rec)))
(defun hydra_packaging_primitive_definition-description (rec) (cdr (assoc :description rec)))
(defun hydra_packaging_primitive_definition-comments (rec) (cdr (assoc :comments rec)))
(defun hydra_packaging_primitive_definition-see_also (rec) (cdr (assoc :see_also rec)))
(defun hydra_packaging_primitive_definition-is_pure (rec) (cdr (assoc :is_pure rec)))
(defun hydra_packaging_primitive_definition-is_total (rec) (cdr (assoc :is_total rec)))
(defun hydra_packaging_primitive_definition-available_since (rec) (cdr (assoc :available_since rec)))
(defun hydra_packaging_primitive_definition-deprecated_since (rec) (cdr (assoc :deprecated_since rec)))
(defun hydra_packaging_primitive_definition-default_implementation (rec) (cdr (assoc :default_implementation rec)))
(defun copy-hydra_packaging_primitive_definition (x) (copy-alist x))
(defun hydra_packaging_primitive_definition-p (x) (listp x))
(setf (symbol-function 'make-primitive_definition) #'make-hydra_packaging_primitive_definition)
(setf (symbol-function 'primitive_definition-name) #'hydra_packaging_primitive_definition-name)
(setf (symbol-function 'primitive_definition-signature) #'hydra_packaging_primitive_definition-signature)
(setf (symbol-function 'primitive_definition-description) #'hydra_packaging_primitive_definition-description)
(setf (symbol-function 'primitive_definition-comments) #'hydra_packaging_primitive_definition-comments)
(setf (symbol-function 'primitive_definition-see_also) #'hydra_packaging_primitive_definition-see_also)
(setf (symbol-function 'primitive_definition-is_pure) #'hydra_packaging_primitive_definition-is_pure)
(setf (symbol-function 'primitive_definition-is_total) #'hydra_packaging_primitive_definition-is_total)
(setf (symbol-function 'primitive_definition-available_since) #'hydra_packaging_primitive_definition-available_since)
(setf (symbol-function 'primitive_definition-deprecated_since) #'hydra_packaging_primitive_definition-deprecated_since)
(setf (symbol-function 'primitive_definition-default_implementation) #'hydra_packaging_primitive_definition-default_implementation)
(setf (symbol-function 'copy-primitive_definition) #'copy-hydra_packaging_primitive_definition)
(setf (symbol-function 'primitive_definition-p) #'hydra_packaging_primitive_definition-p)

;; hydra_packaging_qualified_name -> hydra_packaging_qualified_name
(defun make-hydra_packaging_qualified_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :module_name :local) args)))
(defun hydra_packaging_qualified_name-module_name (rec) (cdr (assoc :module_name rec)))
(defun hydra_packaging_qualified_name-local (rec) (cdr (assoc :local rec)))
(defun copy-hydra_packaging_qualified_name (x) (copy-alist x))
(defun hydra_packaging_qualified_name-p (x) (listp x))
(setf (symbol-function 'make-qualified_name) #'make-hydra_packaging_qualified_name)
(setf (symbol-function 'qualified_name-module_name) #'hydra_packaging_qualified_name-module_name)
(setf (symbol-function 'qualified_name-local) #'hydra_packaging_qualified_name-local)
(setf (symbol-function 'copy-qualified_name) #'copy-hydra_packaging_qualified_name)
(setf (symbol-function 'qualified_name-p) #'hydra_packaging_qualified_name-p)

;; hydra_packaging_term_definition -> hydra_packaging_term_definition
(defun make-hydra_packaging_term_definition (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :term :signature) args)))
(defun hydra_packaging_term_definition-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_term_definition-term (rec) (cdr (assoc :term rec)))
(defun hydra_packaging_term_definition-signature (rec) (cdr (assoc :signature rec)))
(defun copy-hydra_packaging_term_definition (x) (copy-alist x))
(defun hydra_packaging_term_definition-p (x) (listp x))
(setf (symbol-function 'make-term_definition) #'make-hydra_packaging_term_definition)
(setf (symbol-function 'term_definition-name) #'hydra_packaging_term_definition-name)
(setf (symbol-function 'term_definition-term) #'hydra_packaging_term_definition-term)
(setf (symbol-function 'term_definition-signature) #'hydra_packaging_term_definition-signature)
(setf (symbol-function 'copy-term_definition) #'copy-hydra_packaging_term_definition)
(setf (symbol-function 'term_definition-p) #'hydra_packaging_term_definition-p)

;; hydra_packaging_type_definition -> hydra_packaging_type_definition
(defun make-hydra_packaging_type_definition (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :type_scheme) args)))
(defun hydra_packaging_type_definition-name (rec) (cdr (assoc :name rec)))
(defun hydra_packaging_type_definition-type_scheme (rec) (cdr (assoc :type_scheme rec)))
(defun copy-hydra_packaging_type_definition (x) (copy-alist x))
(defun hydra_packaging_type_definition-p (x) (listp x))
(setf (symbol-function 'make-type_definition) #'make-hydra_packaging_type_definition)
(setf (symbol-function 'type_definition-name) #'hydra_packaging_type_definition-name)
(setf (symbol-function 'type_definition-type_scheme) #'hydra_packaging_type_definition-type_scheme)
(setf (symbol-function 'copy-type_definition) #'copy-hydra_packaging_type_definition)
(setf (symbol-function 'type_definition-p) #'hydra_packaging_type_definition-p)

;; hydra_parsing_parse_error -> hydra_parsing_parse_error
(defun make-hydra_parsing_parse_error (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :message :remainder) args)))
(defun hydra_parsing_parse_error-message (rec) (cdr (assoc :message rec)))
(defun hydra_parsing_parse_error-remainder (rec) (cdr (assoc :remainder rec)))
(defun copy-hydra_parsing_parse_error (x) (copy-alist x))
(defun hydra_parsing_parse_error-p (x) (listp x))
(setf (symbol-function 'make-parse_error) #'make-hydra_parsing_parse_error)
(setf (symbol-function 'parse_error-message) #'hydra_parsing_parse_error-message)
(setf (symbol-function 'parse_error-remainder) #'hydra_parsing_parse_error-remainder)
(setf (symbol-function 'copy-parse_error) #'copy-hydra_parsing_parse_error)
(setf (symbol-function 'parse_error-p) #'hydra_parsing_parse_error-p)

;; hydra_parsing_parse_success -> hydra_parsing_parse_success
(defun make-hydra_parsing_parse_success (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value :remainder) args)))
(defun hydra_parsing_parse_success-value (rec) (cdr (assoc :value rec)))
(defun hydra_parsing_parse_success-remainder (rec) (cdr (assoc :remainder rec)))
(defun copy-hydra_parsing_parse_success (x) (copy-alist x))
(defun hydra_parsing_parse_success-p (x) (listp x))
(setf (symbol-function 'make-parse_success) #'make-hydra_parsing_parse_success)
(setf (symbol-function 'parse_success-value) #'hydra_parsing_parse_success-value)
(setf (symbol-function 'parse_success-remainder) #'hydra_parsing_parse_success-remainder)
(setf (symbol-function 'copy-parse_success) #'copy-hydra_parsing_parse_success)
(setf (symbol-function 'parse_success-p) #'hydra_parsing_parse_success-p)

;; hydra_parsing_parser -> hydra_parsing_parser
(defun make-hydra_parsing_parser (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_parsing_parser-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_parsing_parser (x) (copy-alist x))
(defun hydra_parsing_parser-p (x) (listp x))
(setf (symbol-function 'make-parser) #'make-hydra_parsing_parser)
(setf (symbol-function 'parser-value) #'hydra_parsing_parser-value)
(setf (symbol-function 'copy-parser) #'copy-hydra_parsing_parser)
(setf (symbol-function 'parser-p) #'hydra_parsing_parser-p)

;; hydra_paths_subterm_edge -> hydra_paths_subterm_edge
(defun make-hydra_paths_subterm_edge (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :source :path :target) args)))
(defun hydra_paths_subterm_edge-source (rec) (cdr (assoc :source rec)))
(defun hydra_paths_subterm_edge-path (rec) (cdr (assoc :path rec)))
(defun hydra_paths_subterm_edge-target (rec) (cdr (assoc :target rec)))
(defun copy-hydra_paths_subterm_edge (x) (copy-alist x))
(defun hydra_paths_subterm_edge-p (x) (listp x))
(setf (symbol-function 'make-subterm_edge) #'make-hydra_paths_subterm_edge)
(setf (symbol-function 'subterm_edge-source) #'hydra_paths_subterm_edge-source)
(setf (symbol-function 'subterm_edge-path) #'hydra_paths_subterm_edge-path)
(setf (symbol-function 'subterm_edge-target) #'hydra_paths_subterm_edge-target)
(setf (symbol-function 'copy-subterm_edge) #'copy-hydra_paths_subterm_edge)
(setf (symbol-function 'subterm_edge-p) #'hydra_paths_subterm_edge-p)

;; hydra_paths_subterm_graph -> hydra_paths_subterm_graph
(defun make-hydra_paths_subterm_graph (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :nodes :edges) args)))
(defun hydra_paths_subterm_graph-nodes (rec) (cdr (assoc :nodes rec)))
(defun hydra_paths_subterm_graph-edges (rec) (cdr (assoc :edges rec)))
(defun copy-hydra_paths_subterm_graph (x) (copy-alist x))
(defun hydra_paths_subterm_graph-p (x) (listp x))
(setf (symbol-function 'make-subterm_graph) #'make-hydra_paths_subterm_graph)
(setf (symbol-function 'subterm_graph-nodes) #'hydra_paths_subterm_graph-nodes)
(setf (symbol-function 'subterm_graph-edges) #'hydra_paths_subterm_graph-edges)
(setf (symbol-function 'copy-subterm_graph) #'copy-hydra_paths_subterm_graph)
(setf (symbol-function 'subterm_graph-p) #'hydra_paths_subterm_graph-p)

;; hydra_paths_subterm_node -> hydra_paths_subterm_node
(defun make-hydra_paths_subterm_node (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :label :id) args)))
(defun hydra_paths_subterm_node-name (rec) (cdr (assoc :name rec)))
(defun hydra_paths_subterm_node-label (rec) (cdr (assoc :label rec)))
(defun hydra_paths_subterm_node-id (rec) (cdr (assoc :id rec)))
(defun copy-hydra_paths_subterm_node (x) (copy-alist x))
(defun hydra_paths_subterm_node-p (x) (listp x))
(setf (symbol-function 'make-subterm_node) #'make-hydra_paths_subterm_node)
(setf (symbol-function 'subterm_node-name) #'hydra_paths_subterm_node-name)
(setf (symbol-function 'subterm_node-label) #'hydra_paths_subterm_node-label)
(setf (symbol-function 'subterm_node-id) #'hydra_paths_subterm_node-id)
(setf (symbol-function 'copy-subterm_node) #'copy-hydra_paths_subterm_node)
(setf (symbol-function 'subterm_node-p) #'hydra_paths_subterm_node-p)

;; hydra_paths_subterm_path -> hydra_paths_subterm_path
(defun make-hydra_paths_subterm_path (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_paths_subterm_path-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_paths_subterm_path (x) (copy-alist x))
(defun hydra_paths_subterm_path-p (x) (listp x))
(setf (symbol-function 'make-subterm_path) #'make-hydra_paths_subterm_path)
(setf (symbol-function 'subterm_path-value) #'hydra_paths_subterm_path-value)
(setf (symbol-function 'copy-subterm_path) #'copy-hydra_paths_subterm_path)
(setf (symbol-function 'subterm_path-p) #'hydra_paths_subterm_path-p)

;; hydra_paths_subtype_edge -> hydra_paths_subtype_edge
(defun make-hydra_paths_subtype_edge (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :source :path :target) args)))
(defun hydra_paths_subtype_edge-source (rec) (cdr (assoc :source rec)))
(defun hydra_paths_subtype_edge-path (rec) (cdr (assoc :path rec)))
(defun hydra_paths_subtype_edge-target (rec) (cdr (assoc :target rec)))
(defun copy-hydra_paths_subtype_edge (x) (copy-alist x))
(defun hydra_paths_subtype_edge-p (x) (listp x))
(setf (symbol-function 'make-subtype_edge) #'make-hydra_paths_subtype_edge)
(setf (symbol-function 'subtype_edge-source) #'hydra_paths_subtype_edge-source)
(setf (symbol-function 'subtype_edge-path) #'hydra_paths_subtype_edge-path)
(setf (symbol-function 'subtype_edge-target) #'hydra_paths_subtype_edge-target)
(setf (symbol-function 'copy-subtype_edge) #'copy-hydra_paths_subtype_edge)
(setf (symbol-function 'subtype_edge-p) #'hydra_paths_subtype_edge-p)

;; hydra_paths_subtype_graph -> hydra_paths_subtype_graph
(defun make-hydra_paths_subtype_graph (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :nodes :edges) args)))
(defun hydra_paths_subtype_graph-nodes (rec) (cdr (assoc :nodes rec)))
(defun hydra_paths_subtype_graph-edges (rec) (cdr (assoc :edges rec)))
(defun copy-hydra_paths_subtype_graph (x) (copy-alist x))
(defun hydra_paths_subtype_graph-p (x) (listp x))
(setf (symbol-function 'make-subtype_graph) #'make-hydra_paths_subtype_graph)
(setf (symbol-function 'subtype_graph-nodes) #'hydra_paths_subtype_graph-nodes)
(setf (symbol-function 'subtype_graph-edges) #'hydra_paths_subtype_graph-edges)
(setf (symbol-function 'copy-subtype_graph) #'copy-hydra_paths_subtype_graph)
(setf (symbol-function 'subtype_graph-p) #'hydra_paths_subtype_graph-p)

;; hydra_paths_subtype_node -> hydra_paths_subtype_node
(defun make-hydra_paths_subtype_node (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :label :id) args)))
(defun hydra_paths_subtype_node-name (rec) (cdr (assoc :name rec)))
(defun hydra_paths_subtype_node-label (rec) (cdr (assoc :label rec)))
(defun hydra_paths_subtype_node-id (rec) (cdr (assoc :id rec)))
(defun copy-hydra_paths_subtype_node (x) (copy-alist x))
(defun hydra_paths_subtype_node-p (x) (listp x))
(setf (symbol-function 'make-subtype_node) #'make-hydra_paths_subtype_node)
(setf (symbol-function 'subtype_node-name) #'hydra_paths_subtype_node-name)
(setf (symbol-function 'subtype_node-label) #'hydra_paths_subtype_node-label)
(setf (symbol-function 'subtype_node-id) #'hydra_paths_subtype_node-id)
(setf (symbol-function 'copy-subtype_node) #'copy-hydra_paths_subtype_node)
(setf (symbol-function 'subtype_node-p) #'hydra_paths_subtype_node-p)

;; hydra_paths_subtype_path -> hydra_paths_subtype_path
(defun make-hydra_paths_subtype_path (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_paths_subtype_path-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_paths_subtype_path (x) (copy-alist x))
(defun hydra_paths_subtype_path-p (x) (listp x))
(setf (symbol-function 'make-subtype_path) #'make-hydra_paths_subtype_path)
(setf (symbol-function 'subtype_path-value) #'hydra_paths_subtype_path-value)
(setf (symbol-function 'copy-subtype_path) #'copy-hydra_paths_subtype_path)
(setf (symbol-function 'subtype_path-p) #'hydra_paths_subtype_path-p)

;; hydra_phantoms_t_binding -> hydra_phantoms_t_binding
(defun make-hydra_phantoms_t_binding (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :term) args)))
(defun hydra_phantoms_t_binding-name (rec) (cdr (assoc :name rec)))
(defun hydra_phantoms_t_binding-term (rec) (cdr (assoc :term rec)))
(defun copy-hydra_phantoms_t_binding (x) (copy-alist x))
(defun hydra_phantoms_t_binding-p (x) (listp x))
(setf (symbol-function 'make-t_binding) #'make-hydra_phantoms_t_binding)
(setf (symbol-function 't_binding-name) #'hydra_phantoms_t_binding-name)
(setf (symbol-function 't_binding-term) #'hydra_phantoms_t_binding-term)
(setf (symbol-function 'copy-t_binding) #'copy-hydra_phantoms_t_binding)
(setf (symbol-function 't_binding-p) #'hydra_phantoms_t_binding-p)

;; hydra_phantoms_t_term -> hydra_phantoms_t_term
(defun make-hydra_phantoms_t_term (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_phantoms_t_term-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_phantoms_t_term (x) (copy-alist x))
(defun hydra_phantoms_t_term-p (x) (listp x))
(setf (symbol-function 'make-t_term) #'make-hydra_phantoms_t_term)
(setf (symbol-function 't_term-value) #'hydra_phantoms_t_term-value)
(setf (symbol-function 'copy-t_term) #'copy-hydra_phantoms_t_term)
(setf (symbol-function 't_term-p) #'hydra_phantoms_t_term-p)

;; hydra_phantoms_t_term_definition -> hydra_phantoms_t_term_definition
(defun make-hydra_phantoms_t_term_definition (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :term) args)))
(defun hydra_phantoms_t_term_definition-name (rec) (cdr (assoc :name rec)))
(defun hydra_phantoms_t_term_definition-term (rec) (cdr (assoc :term rec)))
(defun copy-hydra_phantoms_t_term_definition (x) (copy-alist x))
(defun hydra_phantoms_t_term_definition-p (x) (listp x))
(setf (symbol-function 'make-t_term_definition) #'make-hydra_phantoms_t_term_definition)
(setf (symbol-function 't_term_definition-name) #'hydra_phantoms_t_term_definition-name)
(setf (symbol-function 't_term_definition-term) #'hydra_phantoms_t_term_definition-term)
(setf (symbol-function 'copy-t_term_definition) #'copy-hydra_phantoms_t_term_definition)
(setf (symbol-function 't_term_definition-p) #'hydra_phantoms_t_term_definition-p)

;; hydra_query_edge -> hydra_query_edge
(defun make-hydra_query_edge (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type :out :in) args)))
(defun hydra_query_edge-type (rec) (cdr (assoc :type rec)))
(defun hydra_query_edge-out (rec) (cdr (assoc :out rec)))
(defun hydra_query_edge-in (rec) (cdr (assoc :in rec)))
(defun copy-hydra_query_edge (x) (copy-alist x))
(defun hydra_query_edge-p (x) (listp x))
(setf (symbol-function 'make-edge) #'make-hydra_query_edge)
(setf (symbol-function 'edge-type) #'hydra_query_edge-type)
(setf (symbol-function 'edge-out) #'hydra_query_edge-out)
(setf (symbol-function 'edge-in) #'hydra_query_edge-in)
(setf (symbol-function 'copy-edge) #'copy-hydra_query_edge)
(setf (symbol-function 'edge-p) #'hydra_query_edge-p)

;; hydra_query_graph_pattern -> hydra_query_graph_pattern
(defun make-hydra_query_graph_pattern (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :graph :patterns) args)))
(defun hydra_query_graph_pattern-graph (rec) (cdr (assoc :graph rec)))
(defun hydra_query_graph_pattern-patterns (rec) (cdr (assoc :patterns rec)))
(defun copy-hydra_query_graph_pattern (x) (copy-alist x))
(defun hydra_query_graph_pattern-p (x) (listp x))
(setf (symbol-function 'make-graph_pattern) #'make-hydra_query_graph_pattern)
(setf (symbol-function 'graph_pattern-graph) #'hydra_query_graph_pattern-graph)
(setf (symbol-function 'graph_pattern-patterns) #'hydra_query_graph_pattern-patterns)
(setf (symbol-function 'copy-graph_pattern) #'copy-hydra_query_graph_pattern)
(setf (symbol-function 'graph_pattern-p) #'hydra_query_graph_pattern-p)

;; hydra_query_path_equation -> hydra_query_path_equation
(defun make-hydra_query_path_equation (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :left :right) args)))
(defun hydra_query_path_equation-left (rec) (cdr (assoc :left rec)))
(defun hydra_query_path_equation-right (rec) (cdr (assoc :right rec)))
(defun copy-hydra_query_path_equation (x) (copy-alist x))
(defun hydra_query_path_equation-p (x) (listp x))
(setf (symbol-function 'make-path_equation) #'make-hydra_query_path_equation)
(setf (symbol-function 'path_equation-left) #'hydra_query_path_equation-left)
(setf (symbol-function 'path_equation-right) #'hydra_query_path_equation-right)
(setf (symbol-function 'copy-path_equation) #'copy-hydra_query_path_equation)
(setf (symbol-function 'path_equation-p) #'hydra_query_path_equation-p)

;; hydra_query_pattern_implication -> hydra_query_pattern_implication
(defun make-hydra_query_pattern_implication (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :antecedent :consequent) args)))
(defun hydra_query_pattern_implication-antecedent (rec) (cdr (assoc :antecedent rec)))
(defun hydra_query_pattern_implication-consequent (rec) (cdr (assoc :consequent rec)))
(defun copy-hydra_query_pattern_implication (x) (copy-alist x))
(defun hydra_query_pattern_implication-p (x) (listp x))
(setf (symbol-function 'make-pattern_implication) #'make-hydra_query_pattern_implication)
(setf (symbol-function 'pattern_implication-antecedent) #'hydra_query_pattern_implication-antecedent)
(setf (symbol-function 'pattern_implication-consequent) #'hydra_query_pattern_implication-consequent)
(setf (symbol-function 'copy-pattern_implication) #'copy-hydra_query_pattern_implication)
(setf (symbol-function 'pattern_implication-p) #'hydra_query_pattern_implication-p)

;; hydra_query_query -> hydra_query_query
(defun make-hydra_query_query (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :variables :patterns) args)))
(defun hydra_query_query-variables (rec) (cdr (assoc :variables rec)))
(defun hydra_query_query-patterns (rec) (cdr (assoc :patterns rec)))
(defun copy-hydra_query_query (x) (copy-alist x))
(defun hydra_query_query-p (x) (listp x))
(setf (symbol-function 'make-query) #'make-hydra_query_query)
(setf (symbol-function 'query-variables) #'hydra_query_query-variables)
(setf (symbol-function 'query-patterns) #'hydra_query_query-patterns)
(setf (symbol-function 'copy-query) #'copy-hydra_query_query)
(setf (symbol-function 'query-p) #'hydra_query_query-p)

;; hydra_query_range -> hydra_query_range
(defun make-hydra_query_range (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :min :max) args)))
(defun hydra_query_range-min (rec) (cdr (assoc :min rec)))
(defun hydra_query_range-max (rec) (cdr (assoc :max rec)))
(defun copy-hydra_query_range (x) (copy-alist x))
(defun hydra_query_range-p (x) (listp x))
(setf (symbol-function 'make-range) #'make-hydra_query_range)
(setf (symbol-function 'range-min) #'hydra_query_range-min)
(setf (symbol-function 'range-max) #'hydra_query_range-max)
(setf (symbol-function 'copy-range) #'copy-hydra_query_range)
(setf (symbol-function 'range-p) #'hydra_query_range-p)

;; hydra_query_regex_sequence -> hydra_query_regex_sequence
(defun make-hydra_query_regex_sequence (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :path :quantifier) args)))
(defun hydra_query_regex_sequence-path (rec) (cdr (assoc :path rec)))
(defun hydra_query_regex_sequence-quantifier (rec) (cdr (assoc :quantifier rec)))
(defun copy-hydra_query_regex_sequence (x) (copy-alist x))
(defun hydra_query_regex_sequence-p (x) (listp x))
(setf (symbol-function 'make-regex_sequence) #'make-hydra_query_regex_sequence)
(setf (symbol-function 'regex_sequence-path) #'hydra_query_regex_sequence-path)
(setf (symbol-function 'regex_sequence-quantifier) #'hydra_query_regex_sequence-quantifier)
(setf (symbol-function 'copy-regex_sequence) #'copy-hydra_query_regex_sequence)
(setf (symbol-function 'regex_sequence-p) #'hydra_query_regex_sequence-p)

;; hydra_query_triple_pattern -> hydra_query_triple_pattern
(defun make-hydra_query_triple_pattern (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :subject :predicate :object) args)))
(defun hydra_query_triple_pattern-subject (rec) (cdr (assoc :subject rec)))
(defun hydra_query_triple_pattern-predicate (rec) (cdr (assoc :predicate rec)))
(defun hydra_query_triple_pattern-object (rec) (cdr (assoc :object rec)))
(defun copy-hydra_query_triple_pattern (x) (copy-alist x))
(defun hydra_query_triple_pattern-p (x) (listp x))
(setf (symbol-function 'make-triple_pattern) #'make-hydra_query_triple_pattern)
(setf (symbol-function 'triple_pattern-subject) #'hydra_query_triple_pattern-subject)
(setf (symbol-function 'triple_pattern-predicate) #'hydra_query_triple_pattern-predicate)
(setf (symbol-function 'triple_pattern-object) #'hydra_query_triple_pattern-object)
(setf (symbol-function 'copy-triple_pattern) #'copy-hydra_query_triple_pattern)
(setf (symbol-function 'triple_pattern-p) #'hydra_query_triple_pattern-p)

;; hydra_query_variable -> hydra_query_variable
(defun make-hydra_query_variable (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_query_variable-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_query_variable (x) (copy-alist x))
(defun hydra_query_variable-p (x) (listp x))
(setf (symbol-function 'make-variable) #'make-hydra_query_variable)
(setf (symbol-function 'variable-value) #'hydra_query_variable-value)
(setf (symbol-function 'copy-variable) #'copy-hydra_query_variable)
(setf (symbol-function 'variable-p) #'hydra_query_variable-p)

;; hydra_relational_column_name -> hydra_relational_column_name
(defun make-hydra_relational_column_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_column_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_column_name (x) (copy-alist x))
(defun hydra_relational_column_name-p (x) (listp x))
(setf (symbol-function 'make-column_name) #'make-hydra_relational_column_name)
(setf (symbol-function 'column_name-value) #'hydra_relational_column_name-value)
(setf (symbol-function 'copy-column_name) #'copy-hydra_relational_column_name)
(setf (symbol-function 'column_name-p) #'hydra_relational_column_name-p)

;; hydra_relational_column_schema -> hydra_relational_column_schema
(defun make-hydra_relational_column_schema (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :domain) args)))
(defun hydra_relational_column_schema-name (rec) (cdr (assoc :name rec)))
(defun hydra_relational_column_schema-domain (rec) (cdr (assoc :domain rec)))
(defun copy-hydra_relational_column_schema (x) (copy-alist x))
(defun hydra_relational_column_schema-p (x) (listp x))
(setf (symbol-function 'make-column_schema) #'make-hydra_relational_column_schema)
(setf (symbol-function 'column_schema-name) #'hydra_relational_column_schema-name)
(setf (symbol-function 'column_schema-domain) #'hydra_relational_column_schema-domain)
(setf (symbol-function 'copy-column_schema) #'copy-hydra_relational_column_schema)
(setf (symbol-function 'column_schema-p) #'hydra_relational_column_schema-p)

;; hydra_relational_foreign_key -> hydra_relational_foreign_key
(defun make-hydra_relational_foreign_key (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :foreign_relation :keys) args)))
(defun hydra_relational_foreign_key-foreign_relation (rec) (cdr (assoc :foreign_relation rec)))
(defun hydra_relational_foreign_key-keys (rec) (cdr (assoc :keys rec)))
(defun copy-hydra_relational_foreign_key (x) (copy-alist x))
(defun hydra_relational_foreign_key-p (x) (listp x))
(setf (symbol-function 'make-foreign_key) #'make-hydra_relational_foreign_key)
(setf (symbol-function 'foreign_key-foreign_relation) #'hydra_relational_foreign_key-foreign_relation)
(setf (symbol-function 'foreign_key-keys) #'hydra_relational_foreign_key-keys)
(setf (symbol-function 'copy-foreign_key) #'copy-hydra_relational_foreign_key)
(setf (symbol-function 'foreign_key-p) #'hydra_relational_foreign_key-p)

;; hydra_relational_primary_key -> hydra_relational_primary_key
(defun make-hydra_relational_primary_key (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_primary_key-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_primary_key (x) (copy-alist x))
(defun hydra_relational_primary_key-p (x) (listp x))
(setf (symbol-function 'make-primary_key) #'make-hydra_relational_primary_key)
(setf (symbol-function 'primary_key-value) #'hydra_relational_primary_key-value)
(setf (symbol-function 'copy-primary_key) #'copy-hydra_relational_primary_key)
(setf (symbol-function 'primary_key-p) #'hydra_relational_primary_key-p)

;; hydra_relational_relation -> hydra_relational_relation
(defun make-hydra_relational_relation (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_relation-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_relation (x) (copy-alist x))
(defun hydra_relational_relation-p (x) (listp x))
(setf (symbol-function 'make-relation) #'make-hydra_relational_relation)
(setf (symbol-function 'relation-value) #'hydra_relational_relation-value)
(setf (symbol-function 'copy-relation) #'copy-hydra_relational_relation)
(setf (symbol-function 'relation-p) #'hydra_relational_relation-p)

;; hydra_relational_relation_name -> hydra_relational_relation_name
(defun make-hydra_relational_relation_name (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_relation_name-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_relation_name (x) (copy-alist x))
(defun hydra_relational_relation_name-p (x) (listp x))
(setf (symbol-function 'make-relation_name) #'make-hydra_relational_relation_name)
(setf (symbol-function 'relation_name-value) #'hydra_relational_relation_name-value)
(setf (symbol-function 'copy-relation_name) #'copy-hydra_relational_relation_name)
(setf (symbol-function 'relation_name-p) #'hydra_relational_relation_name-p)

;; hydra_relational_relation_schema -> hydra_relational_relation_schema
(defun make-hydra_relational_relation_schema (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :columns :primary_keys :foreign_keys) args)))
(defun hydra_relational_relation_schema-name (rec) (cdr (assoc :name rec)))
(defun hydra_relational_relation_schema-columns (rec) (cdr (assoc :columns rec)))
(defun hydra_relational_relation_schema-primary_keys (rec) (cdr (assoc :primary_keys rec)))
(defun hydra_relational_relation_schema-foreign_keys (rec) (cdr (assoc :foreign_keys rec)))
(defun copy-hydra_relational_relation_schema (x) (copy-alist x))
(defun hydra_relational_relation_schema-p (x) (listp x))
(setf (symbol-function 'make-relation_schema) #'make-hydra_relational_relation_schema)
(setf (symbol-function 'relation_schema-name) #'hydra_relational_relation_schema-name)
(setf (symbol-function 'relation_schema-columns) #'hydra_relational_relation_schema-columns)
(setf (symbol-function 'relation_schema-primary_keys) #'hydra_relational_relation_schema-primary_keys)
(setf (symbol-function 'relation_schema-foreign_keys) #'hydra_relational_relation_schema-foreign_keys)
(setf (symbol-function 'copy-relation_schema) #'copy-hydra_relational_relation_schema)
(setf (symbol-function 'relation_schema-p) #'hydra_relational_relation_schema-p)

;; hydra_relational_relationship -> hydra_relational_relationship
(defun make-hydra_relational_relationship (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_relationship-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_relationship (x) (copy-alist x))
(defun hydra_relational_relationship-p (x) (listp x))
(setf (symbol-function 'make-relationship) #'make-hydra_relational_relationship)
(setf (symbol-function 'relationship-value) #'hydra_relational_relationship-value)
(setf (symbol-function 'copy-relationship) #'copy-hydra_relational_relationship)
(setf (symbol-function 'relationship-p) #'hydra_relational_relationship-p)

;; hydra_relational_row -> hydra_relational_row
(defun make-hydra_relational_row (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_relational_row-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_relational_row (x) (copy-alist x))
(defun hydra_relational_row-p (x) (listp x))
(setf (symbol-function 'make-row) #'make-hydra_relational_row)
(setf (symbol-function 'row-value) #'hydra_relational_row-value)
(setf (symbol-function 'copy-row) #'copy-hydra_relational_row)
(setf (symbol-function 'row-p) #'hydra_relational_row-p)

;; hydra_tabular_column_type -> hydra_tabular_column_type
(defun make-hydra_tabular_column_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :type) args)))
(defun hydra_tabular_column_type-name (rec) (cdr (assoc :name rec)))
(defun hydra_tabular_column_type-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_tabular_column_type (x) (copy-alist x))
(defun hydra_tabular_column_type-p (x) (listp x))
(setf (symbol-function 'make-column_type) #'make-hydra_tabular_column_type)
(setf (symbol-function 'column_type-name) #'hydra_tabular_column_type-name)
(setf (symbol-function 'column_type-type) #'hydra_tabular_column_type-type)
(setf (symbol-function 'copy-column_type) #'copy-hydra_tabular_column_type)
(setf (symbol-function 'column_type-p) #'hydra_tabular_column_type-p)

;; hydra_tabular_data_row -> hydra_tabular_data_row
(defun make-hydra_tabular_data_row (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_tabular_data_row-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_tabular_data_row (x) (copy-alist x))
(defun hydra_tabular_data_row-p (x) (listp x))
(setf (symbol-function 'make-data_row) #'make-hydra_tabular_data_row)
(setf (symbol-function 'data_row-value) #'hydra_tabular_data_row-value)
(setf (symbol-function 'copy-data_row) #'copy-hydra_tabular_data_row)
(setf (symbol-function 'data_row-p) #'hydra_tabular_data_row-p)

;; hydra_tabular_header_row -> hydra_tabular_header_row
(defun make-hydra_tabular_header_row (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_tabular_header_row-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_tabular_header_row (x) (copy-alist x))
(defun hydra_tabular_header_row-p (x) (listp x))
(setf (symbol-function 'make-header_row) #'make-hydra_tabular_header_row)
(setf (symbol-function 'header_row-value) #'hydra_tabular_header_row-value)
(setf (symbol-function 'copy-header_row) #'copy-hydra_tabular_header_row)
(setf (symbol-function 'header_row-p) #'hydra_tabular_header_row-p)

;; hydra_tabular_table -> hydra_tabular_table
(defun make-hydra_tabular_table (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :header :data) args)))
(defun hydra_tabular_table-header (rec) (cdr (assoc :header rec)))
(defun hydra_tabular_table-data (rec) (cdr (assoc :data rec)))
(defun copy-hydra_tabular_table (x) (copy-alist x))
(defun hydra_tabular_table-p (x) (listp x))
(setf (symbol-function 'make-table) #'make-hydra_tabular_table)
(setf (symbol-function 'table-header) #'hydra_tabular_table-header)
(setf (symbol-function 'table-data) #'hydra_tabular_table-data)
(setf (symbol-function 'copy-table) #'copy-hydra_tabular_table)
(setf (symbol-function 'table-p) #'hydra_tabular_table-p)

;; hydra_tabular_table_type -> hydra_tabular_table_type
(defun make-hydra_tabular_table_type (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :columns) args)))
(defun hydra_tabular_table_type-name (rec) (cdr (assoc :name rec)))
(defun hydra_tabular_table_type-columns (rec) (cdr (assoc :columns rec)))
(defun copy-hydra_tabular_table_type (x) (copy-alist x))
(defun hydra_tabular_table_type-p (x) (listp x))
(setf (symbol-function 'make-table_type) #'make-hydra_tabular_table_type)
(setf (symbol-function 'table_type-name) #'hydra_tabular_table_type-name)
(setf (symbol-function 'table_type-columns) #'hydra_tabular_table_type-columns)
(setf (symbol-function 'copy-table_type) #'copy-hydra_tabular_table_type)
(setf (symbol-function 'table_type-p) #'hydra_tabular_table_type-p)

;; hydra_testing_tag -> hydra_testing_tag
(defun make-hydra_testing_tag (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_testing_tag-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_testing_tag (x) (copy-alist x))
(defun hydra_testing_tag-p (x) (listp x))
(setf (symbol-function 'make-tag) #'make-hydra_testing_tag)
(setf (symbol-function 'tag-value) #'hydra_testing_tag-value)
(setf (symbol-function 'copy-tag) #'copy-hydra_testing_tag)
(setf (symbol-function 'tag-p) #'hydra_testing_tag-p)

;; hydra_testing_test_case_with_metadata -> hydra_testing_test_case_with_metadata
(defun make-hydra_testing_test_case_with_metadata (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :case :description :tags) args)))
(defun hydra_testing_test_case_with_metadata-name (rec) (cdr (assoc :name rec)))
(defun hydra_testing_test_case_with_metadata-case (rec) (cdr (assoc :case rec)))
(defun hydra_testing_test_case_with_metadata-description (rec) (cdr (assoc :description rec)))
(defun hydra_testing_test_case_with_metadata-tags (rec) (cdr (assoc :tags rec)))
(defun copy-hydra_testing_test_case_with_metadata (x) (copy-alist x))
(defun hydra_testing_test_case_with_metadata-p (x) (listp x))
(setf (symbol-function 'make-test_case_with_metadata) #'make-hydra_testing_test_case_with_metadata)
(setf (symbol-function 'test_case_with_metadata-name) #'hydra_testing_test_case_with_metadata-name)
(setf (symbol-function 'test_case_with_metadata-case) #'hydra_testing_test_case_with_metadata-case)
(setf (symbol-function 'test_case_with_metadata-description) #'hydra_testing_test_case_with_metadata-description)
(setf (symbol-function 'test_case_with_metadata-tags) #'hydra_testing_test_case_with_metadata-tags)
(setf (symbol-function 'copy-test_case_with_metadata) #'copy-hydra_testing_test_case_with_metadata)
(setf (symbol-function 'test_case_with_metadata-p) #'hydra_testing_test_case_with_metadata-p)

;; hydra_testing_test_group -> hydra_testing_test_group
(defun make-hydra_testing_test_group (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :description :subgroups :cases) args)))
(defun hydra_testing_test_group-name (rec) (cdr (assoc :name rec)))
(defun hydra_testing_test_group-description (rec) (cdr (assoc :description rec)))
(defun hydra_testing_test_group-subgroups (rec) (cdr (assoc :subgroups rec)))
(defun hydra_testing_test_group-cases (rec) (cdr (assoc :cases rec)))
(defun copy-hydra_testing_test_group (x) (copy-alist x))
(defun hydra_testing_test_group-p (x) (listp x))
(setf (symbol-function 'make-test_group) #'make-hydra_testing_test_group)
(setf (symbol-function 'test_group-name) #'hydra_testing_test_group-name)
(setf (symbol-function 'test_group-description) #'hydra_testing_test_group-description)
(setf (symbol-function 'test_group-subgroups) #'hydra_testing_test_group-subgroups)
(setf (symbol-function 'test_group-cases) #'hydra_testing_test_group-cases)
(setf (symbol-function 'copy-test_group) #'copy-hydra_testing_test_group)
(setf (symbol-function 'test_group-p) #'hydra_testing_test_group-p)

;; hydra_testing_universal_test_case -> hydra_testing_universal_test_case
(defun make-hydra_testing_universal_test_case (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :actual :expected) args)))
(defun hydra_testing_universal_test_case-actual (rec) (cdr (assoc :actual rec)))
(defun hydra_testing_universal_test_case-expected (rec) (cdr (assoc :expected rec)))
(defun copy-hydra_testing_universal_test_case (x) (copy-alist x))
(defun hydra_testing_universal_test_case-p (x) (listp x))
(setf (symbol-function 'make-universal_test_case) #'make-hydra_testing_universal_test_case)
(setf (symbol-function 'universal_test_case-actual) #'hydra_testing_universal_test_case-actual)
(setf (symbol-function 'universal_test_case-expected) #'hydra_testing_universal_test_case-expected)
(setf (symbol-function 'copy-universal_test_case) #'copy-hydra_testing_universal_test_case)
(setf (symbol-function 'universal_test_case-p) #'hydra_testing_universal_test_case-p)

;; hydra_topology_ordering_isomorphism -> hydra_topology_ordering_isomorphism
(defun make-hydra_topology_ordering_isomorphism (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :encode :decode) args)))
(defun hydra_topology_ordering_isomorphism-encode (rec) (cdr (assoc :encode rec)))
(defun hydra_topology_ordering_isomorphism-decode (rec) (cdr (assoc :decode rec)))
(defun copy-hydra_topology_ordering_isomorphism (x) (copy-alist x))
(defun hydra_topology_ordering_isomorphism-p (x) (listp x))
(setf (symbol-function 'make-ordering_isomorphism) #'make-hydra_topology_ordering_isomorphism)
(setf (symbol-function 'ordering_isomorphism-encode) #'hydra_topology_ordering_isomorphism-encode)
(setf (symbol-function 'ordering_isomorphism-decode) #'hydra_topology_ordering_isomorphism-decode)
(setf (symbol-function 'copy-ordering_isomorphism) #'copy-hydra_topology_ordering_isomorphism)
(setf (symbol-function 'ordering_isomorphism-p) #'hydra_topology_ordering_isomorphism-p)

;; hydra_topology_tarjan_state -> hydra_topology_tarjan_state
(defun make-hydra_topology_tarjan_state (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :counter :indices :low_links :stack :on_stack :sccs) args)))
(defun hydra_topology_tarjan_state-counter (rec) (cdr (assoc :counter rec)))
(defun hydra_topology_tarjan_state-indices (rec) (cdr (assoc :indices rec)))
(defun hydra_topology_tarjan_state-low_links (rec) (cdr (assoc :low_links rec)))
(defun hydra_topology_tarjan_state-stack (rec) (cdr (assoc :stack rec)))
(defun hydra_topology_tarjan_state-on_stack (rec) (cdr (assoc :on_stack rec)))
(defun hydra_topology_tarjan_state-sccs (rec) (cdr (assoc :sccs rec)))
(defun copy-hydra_topology_tarjan_state (x) (copy-alist x))
(defun hydra_topology_tarjan_state-p (x) (listp x))
(setf (symbol-function 'make-tarjan_state) #'make-hydra_topology_tarjan_state)
(setf (symbol-function 'tarjan_state-counter) #'hydra_topology_tarjan_state-counter)
(setf (symbol-function 'tarjan_state-indices) #'hydra_topology_tarjan_state-indices)
(setf (symbol-function 'tarjan_state-low_links) #'hydra_topology_tarjan_state-low_links)
(setf (symbol-function 'tarjan_state-stack) #'hydra_topology_tarjan_state-stack)
(setf (symbol-function 'tarjan_state-on_stack) #'hydra_topology_tarjan_state-on_stack)
(setf (symbol-function 'tarjan_state-sccs) #'hydra_topology_tarjan_state-sccs)
(setf (symbol-function 'copy-tarjan_state) #'copy-hydra_topology_tarjan_state)
(setf (symbol-function 'tarjan_state-p) #'hydra_topology_tarjan_state-p)

;; hydra_typing_function_structure -> hydra_typing_function_structure
(defun make-hydra_typing_function_structure (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_params :params :bindings :body :domains :codomain :environment) args)))
(defun hydra_typing_function_structure-type_params (rec) (cdr (assoc :type_params rec)))
(defun hydra_typing_function_structure-params (rec) (cdr (assoc :params rec)))
(defun hydra_typing_function_structure-bindings (rec) (cdr (assoc :bindings rec)))
(defun hydra_typing_function_structure-body (rec) (cdr (assoc :body rec)))
(defun hydra_typing_function_structure-domains (rec) (cdr (assoc :domains rec)))
(defun hydra_typing_function_structure-codomain (rec) (cdr (assoc :codomain rec)))
(defun hydra_typing_function_structure-environment (rec) (cdr (assoc :environment rec)))
(defun copy-hydra_typing_function_structure (x) (copy-alist x))
(defun hydra_typing_function_structure-p (x) (listp x))
(setf (symbol-function 'make-function_structure) #'make-hydra_typing_function_structure)
(setf (symbol-function 'function_structure-type_params) #'hydra_typing_function_structure-type_params)
(setf (symbol-function 'function_structure-params) #'hydra_typing_function_structure-params)
(setf (symbol-function 'function_structure-bindings) #'hydra_typing_function_structure-bindings)
(setf (symbol-function 'function_structure-body) #'hydra_typing_function_structure-body)
(setf (symbol-function 'function_structure-domains) #'hydra_typing_function_structure-domains)
(setf (symbol-function 'function_structure-codomain) #'hydra_typing_function_structure-codomain)
(setf (symbol-function 'function_structure-environment) #'hydra_typing_function_structure-environment)
(setf (symbol-function 'copy-function_structure) #'copy-hydra_typing_function_structure)
(setf (symbol-function 'function_structure-p) #'hydra_typing_function_structure-p)

;; hydra_typing_inference_context -> hydra_typing_inference_context
(defun make-hydra_typing_inference_context (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :fresh_type_variable_count :trace) args)))
(defun hydra_typing_inference_context-fresh_type_variable_count (rec) (cdr (assoc :fresh_type_variable_count rec)))
(defun hydra_typing_inference_context-trace (rec) (cdr (assoc :trace rec)))
(defun copy-hydra_typing_inference_context (x) (copy-alist x))
(defun hydra_typing_inference_context-p (x) (listp x))
(setf (symbol-function 'make-inference_context) #'make-hydra_typing_inference_context)
(setf (symbol-function 'inference_context-fresh_type_variable_count) #'hydra_typing_inference_context-fresh_type_variable_count)
(setf (symbol-function 'inference_context-trace) #'hydra_typing_inference_context-trace)
(setf (symbol-function 'copy-inference_context) #'copy-hydra_typing_inference_context)
(setf (symbol-function 'inference_context-p) #'hydra_typing_inference_context-p)

;; hydra_typing_inference_result -> hydra_typing_inference_result
(defun make-hydra_typing_inference_result (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :term :type :subst :class_constraints :context) args)))
(defun hydra_typing_inference_result-term (rec) (cdr (assoc :term rec)))
(defun hydra_typing_inference_result-type (rec) (cdr (assoc :type rec)))
(defun hydra_typing_inference_result-subst (rec) (cdr (assoc :subst rec)))
(defun hydra_typing_inference_result-class_constraints (rec) (cdr (assoc :class_constraints rec)))
(defun hydra_typing_inference_result-context (rec) (cdr (assoc :context rec)))
(defun copy-hydra_typing_inference_result (x) (copy-alist x))
(defun hydra_typing_inference_result-p (x) (listp x))
(setf (symbol-function 'make-inference_result) #'make-hydra_typing_inference_result)
(setf (symbol-function 'inference_result-term) #'hydra_typing_inference_result-term)
(setf (symbol-function 'inference_result-type) #'hydra_typing_inference_result-type)
(setf (symbol-function 'inference_result-subst) #'hydra_typing_inference_result-subst)
(setf (symbol-function 'inference_result-class_constraints) #'hydra_typing_inference_result-class_constraints)
(setf (symbol-function 'inference_result-context) #'hydra_typing_inference_result-context)
(setf (symbol-function 'copy-inference_result) #'copy-hydra_typing_inference_result)
(setf (symbol-function 'inference_result-p) #'hydra_typing_inference_result-p)

;; hydra_typing_parameter -> hydra_typing_parameter
(defun make-hydra_typing_parameter (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :description :type :is_lazy) args)))
(defun hydra_typing_parameter-name (rec) (cdr (assoc :name rec)))
(defun hydra_typing_parameter-description (rec) (cdr (assoc :description rec)))
(defun hydra_typing_parameter-type (rec) (cdr (assoc :type rec)))
(defun hydra_typing_parameter-is_lazy (rec) (cdr (assoc :is_lazy rec)))
(defun copy-hydra_typing_parameter (x) (copy-alist x))
(defun hydra_typing_parameter-p (x) (listp x))
(setf (symbol-function 'make-parameter) #'make-hydra_typing_parameter)
(setf (symbol-function 'parameter-name) #'hydra_typing_parameter-name)
(setf (symbol-function 'parameter-description) #'hydra_typing_parameter-description)
(setf (symbol-function 'parameter-type) #'hydra_typing_parameter-type)
(setf (symbol-function 'parameter-is_lazy) #'hydra_typing_parameter-is_lazy)
(setf (symbol-function 'copy-parameter) #'copy-hydra_typing_parameter)
(setf (symbol-function 'parameter-p) #'hydra_typing_parameter-p)

;; hydra_typing_result -> hydra_typing_result
(defun make-hydra_typing_result (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :description :type) args)))
(defun hydra_typing_result-description (rec) (cdr (assoc :description rec)))
(defun hydra_typing_result-type (rec) (cdr (assoc :type rec)))
(defun copy-hydra_typing_result (x) (copy-alist x))
(defun hydra_typing_result-p (x) (listp x))
(setf (symbol-function 'make-result) #'make-hydra_typing_result)
(setf (symbol-function 'result-description) #'hydra_typing_result-description)
(setf (symbol-function 'result-type) #'hydra_typing_result-type)
(setf (symbol-function 'copy-result) #'copy-hydra_typing_result)
(setf (symbol-function 'result-p) #'hydra_typing_result-p)

;; hydra_typing_term_signature -> hydra_typing_term_signature
(defun make-hydra_typing_term_signature (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :type_parameters :parameters :result) args)))
(defun hydra_typing_term_signature-type_parameters (rec) (cdr (assoc :type_parameters rec)))
(defun hydra_typing_term_signature-parameters (rec) (cdr (assoc :parameters rec)))
(defun hydra_typing_term_signature-result (rec) (cdr (assoc :result rec)))
(defun copy-hydra_typing_term_signature (x) (copy-alist x))
(defun hydra_typing_term_signature-p (x) (listp x))
(setf (symbol-function 'make-term_signature) #'make-hydra_typing_term_signature)
(setf (symbol-function 'term_signature-type_parameters) #'hydra_typing_term_signature-type_parameters)
(setf (symbol-function 'term_signature-parameters) #'hydra_typing_term_signature-parameters)
(setf (symbol-function 'term_signature-result) #'hydra_typing_term_signature-result)
(setf (symbol-function 'copy-term_signature) #'copy-hydra_typing_term_signature)
(setf (symbol-function 'term_signature-p) #'hydra_typing_term_signature-p)

;; hydra_typing_term_subst -> hydra_typing_term_subst
(defun make-hydra_typing_term_subst (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_typing_term_subst-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_typing_term_subst (x) (copy-alist x))
(defun hydra_typing_term_subst-p (x) (listp x))
(setf (symbol-function 'make-term_subst) #'make-hydra_typing_term_subst)
(setf (symbol-function 'term_subst-value) #'hydra_typing_term_subst-value)
(setf (symbol-function 'copy-term_subst) #'copy-hydra_typing_term_subst)
(setf (symbol-function 'term_subst-p) #'hydra_typing_term_subst-p)

;; hydra_typing_type_class -> hydra_typing_type_class
(defun make-hydra_typing_type_class (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :description) args)))
(defun hydra_typing_type_class-description (rec) (cdr (assoc :description rec)))
(defun copy-hydra_typing_type_class (x) (copy-alist x))
(defun hydra_typing_type_class-p (x) (listp x))
(setf (symbol-function 'make-type_class) #'make-hydra_typing_type_class)
(setf (symbol-function 'type_class-description) #'hydra_typing_type_class-description)
(setf (symbol-function 'copy-type_class) #'copy-hydra_typing_type_class)
(setf (symbol-function 'type_class-p) #'hydra_typing_type_class-p)

;; hydra_typing_type_constraint -> hydra_typing_type_constraint
(defun make-hydra_typing_type_constraint (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :left :right :comment) args)))
(defun hydra_typing_type_constraint-left (rec) (cdr (assoc :left rec)))
(defun hydra_typing_type_constraint-right (rec) (cdr (assoc :right rec)))
(defun hydra_typing_type_constraint-comment (rec) (cdr (assoc :comment rec)))
(defun copy-hydra_typing_type_constraint (x) (copy-alist x))
(defun hydra_typing_type_constraint-p (x) (listp x))
(setf (symbol-function 'make-type_constraint) #'make-hydra_typing_type_constraint)
(setf (symbol-function 'type_constraint-left) #'hydra_typing_type_constraint-left)
(setf (symbol-function 'type_constraint-right) #'hydra_typing_type_constraint-right)
(setf (symbol-function 'type_constraint-comment) #'hydra_typing_type_constraint-comment)
(setf (symbol-function 'copy-type_constraint) #'copy-hydra_typing_type_constraint)
(setf (symbol-function 'type_constraint-p) #'hydra_typing_type_constraint-p)

;; hydra_typing_type_parameter -> hydra_typing_type_parameter
(defun make-hydra_typing_type_parameter (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :name :constraints) args)))
(defun hydra_typing_type_parameter-name (rec) (cdr (assoc :name rec)))
(defun hydra_typing_type_parameter-constraints (rec) (cdr (assoc :constraints rec)))
(defun copy-hydra_typing_type_parameter (x) (copy-alist x))
(defun hydra_typing_type_parameter-p (x) (listp x))
(setf (symbol-function 'make-type_parameter) #'make-hydra_typing_type_parameter)
(setf (symbol-function 'type_parameter-name) #'hydra_typing_type_parameter-name)
(setf (symbol-function 'type_parameter-constraints) #'hydra_typing_type_parameter-constraints)
(setf (symbol-function 'copy-type_parameter) #'copy-hydra_typing_type_parameter)
(setf (symbol-function 'type_parameter-p) #'hydra_typing_type_parameter-p)

;; hydra_typing_type_subst -> hydra_typing_type_subst
(defun make-hydra_typing_type_subst (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :value) args)))
(defun hydra_typing_type_subst-value (rec) (cdr (assoc :value rec)))
(defun copy-hydra_typing_type_subst (x) (copy-alist x))
(defun hydra_typing_type_subst-p (x) (listp x))
(setf (symbol-function 'make-type_subst) #'make-hydra_typing_type_subst)
(setf (symbol-function 'type_subst-value) #'hydra_typing_type_subst-value)
(setf (symbol-function 'copy-type_subst) #'copy-hydra_typing_type_subst)
(setf (symbol-function 'type_subst-p) #'hydra_typing_type_subst-p)

;; hydra_util_namespaces -> hydra_util_namespaces
(defun make-hydra_util_namespaces (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :focus :mapping) args)))
(defun hydra_util_namespaces-focus (rec) (cdr (assoc :focus rec)))
(defun hydra_util_namespaces-mapping (rec) (cdr (assoc :mapping rec)))
(defun copy-hydra_util_namespaces (x) (copy-alist x))
(defun hydra_util_namespaces-p (x) (listp x))
(setf (symbol-function 'make-namespaces) #'make-hydra_util_namespaces)
(setf (symbol-function 'namespaces-focus) #'hydra_util_namespaces-focus)
(setf (symbol-function 'namespaces-mapping) #'hydra_util_namespaces-mapping)
(setf (symbol-function 'copy-namespaces) #'copy-hydra_util_namespaces)
(setf (symbol-function 'namespaces-p) #'hydra_util_namespaces-p)

;; hydra_validation_validation_profile -> hydra_validation_validation_profile
(defun make-hydra_validation_validation_profile (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :error_rules :warning_rules :max_errors :max_warnings) args)))
(defun hydra_validation_validation_profile-error_rules (rec) (cdr (assoc :error_rules rec)))
(defun hydra_validation_validation_profile-warning_rules (rec) (cdr (assoc :warning_rules rec)))
(defun hydra_validation_validation_profile-max_errors (rec) (cdr (assoc :max_errors rec)))
(defun hydra_validation_validation_profile-max_warnings (rec) (cdr (assoc :max_warnings rec)))
(defun copy-hydra_validation_validation_profile (x) (copy-alist x))
(defun hydra_validation_validation_profile-p (x) (listp x))
(setf (symbol-function 'make-validation_profile) #'make-hydra_validation_validation_profile)
(setf (symbol-function 'validation_profile-error_rules) #'hydra_validation_validation_profile-error_rules)
(setf (symbol-function 'validation_profile-warning_rules) #'hydra_validation_validation_profile-warning_rules)
(setf (symbol-function 'validation_profile-max_errors) #'hydra_validation_validation_profile-max_errors)
(setf (symbol-function 'validation_profile-max_warnings) #'hydra_validation_validation_profile-max_warnings)
(setf (symbol-function 'copy-validation_profile) #'copy-hydra_validation_validation_profile)
(setf (symbol-function 'validation_profile-p) #'hydra_validation_validation_profile-p)

;; hydra_validation_validation_result -> hydra_validation_validation_result
(defun make-hydra_validation_validation_result (&rest args)
  (if (and args (keywordp (first args)))
    (loop for (k v) on args by #'cddr collect (cons k v))
    (mapcar #'cons '( :errors :warnings) args)))
(defun hydra_validation_validation_result-errors (rec) (cdr (assoc :errors rec)))
(defun hydra_validation_validation_result-warnings (rec) (cdr (assoc :warnings rec)))
(defun copy-hydra_validation_validation_result (x) (copy-alist x))
(defun hydra_validation_validation_result-p (x) (listp x))
(setf (symbol-function 'make-validation_result) #'make-hydra_validation_validation_result)
(setf (symbol-function 'validation_result-errors) #'hydra_validation_validation_result-errors)
(setf (symbol-function 'validation_result-warnings) #'hydra_validation_validation_result-warnings)
(setf (symbol-function 'copy-validation_result) #'copy-hydra_validation_validation_result)
(setf (symbol-function 'validation_result-p) #'hydra_validation_validation_result-p)

;;; End of generated compatibility definitions
