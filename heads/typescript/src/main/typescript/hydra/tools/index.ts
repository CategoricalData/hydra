/**
 * Hydra tools and utilities for TypeScript.
 *
 * Placeholder for code generation tools, formatters, and other utilities.
 */

export function formatModuleName(ns: string): string {
  return ns.replace(/\./g, "/");
}
