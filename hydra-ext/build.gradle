plugins {
    id 'java-library'
}

dependencies {
    implementation project(':hydra-java')
}

// Avoids duplicate KqlWriter.java
tasks.named('sourcesJar') {
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
}