(require 'cl-lib)

(require 'hydra.annotations)

(require 'hydra.arity)

(require 'hydra.checking)

(require 'hydra.coders)

(require 'hydra.core)

(require 'hydra.formatting)

(require 'hydra.graph)

(require 'hydra.lib.eithers)

(require 'hydra.lib.equality)

(require 'hydra.lib.lists)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.math)

(require 'hydra.lib.maybes)

(require 'hydra.lib.pairs)

(require 'hydra.lib.sets)

(require 'hydra.lib.strings)

(require 'hydra.module)

(require 'hydra.names)

(require 'hydra.rewriting)

(require 'hydra.schemas)

(require 'hydra.typing)

(require 'hydra.util)

(defvar hydra_coder_utils_type_of_term (lambda (cx) (lambda (g) (lambda (term) (funcall (hydra_lib_eithers_map hydra_lib_pairs_first) (funcall (funcall (funcall (hydra_checking_type_of cx) g) (list)) term))))))

(defvar hydra_coder_utils_analyze_function_term_with_finish (lambda (cx) (lambda (get_t_c) (lambda (f_env) (lambda (tparams) (lambda (args) (lambda (bindings) (lambda (doms) (lambda (tapps) (lambda (body) (let ((body_with_tapps (funcall (funcall (hydra_lib_lists_foldl (lambda (trm) (lambda (typ) (list :type_application (make-hydra_core_type_application_term trm typ))))) body) tapps))) (let ((mcod (funcall (funcall (hydra_lib_eithers_either (lambda (_) (list :nothing))) (lambda (c) (list :just c))) (funcall (funcall (hydra_coder_utils_type_of_term cx) (get_t_c f_env)) body_with_tapps)))) (list :right (make-hydra_typing_function_structure (hydra_lib_lists_reverse tparams) (hydra_lib_lists_reverse args) bindings body_with_tapps (hydra_lib_lists_reverse doms) mcod f_env))))))))))))))

(defvar hydra_coder_utils_analyze_function_term_with_gather (lambda (cx) (lambda (for_binding) (lambda (get_t_c) (lambda (set_t_c) (lambda (arg_mode) (lambda (g_env) (lambda (tparams) (lambda (args) (lambda (bindings) (lambda (doms) (lambda (tapps) (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (lam) (if arg_mode (let ((v (funcall (lambda (v) (hydra_core_lambda-parameter v)) lam))) (let ((dom (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list :variable "_"))) (lambda (x_) x_)) (funcall (lambda (v) (hydra_core_lambda-domain v)) lam)))) (let ((body (funcall (lambda (v) (hydra_core_lambda-body v)) lam))) (let ((new_env (funcall (set_t_c (funcall (hydra_schemas_extend_graph_for_lambda (get_t_c g_env)) lam)) g_env))) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_gather cx) for_binding) get_t_c) set_t_c) arg_mode) new_env) tparams) (funcall (hydra_lib_lists_cons v) args)) bindings) (funcall (hydra_lib_lists_cons dom) doms)) tapps) body))))) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_finish cx) get_t_c) g_env) tparams) args) bindings) doms) tapps) t_))) match_value)) (t (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_finish cx) get_t_c) g_env) tparams) args) bindings) doms) tapps) t_)))) (cadr match_target))) f)) match_value)) ((equal (car match_target) :let) (funcall (lambda (lt) (let ((new_bindings (funcall (lambda (v) (hydra_core_let-bindings v)) lt))) (let ((body (funcall (lambda (v) (hydra_core_let-body v)) lt))) (let ((new_env (funcall (set_t_c (funcall (funcall (hydra_schemas_extend_graph_for_let for_binding) (get_t_c g_env)) lt)) g_env))) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_gather cx) for_binding) get_t_c) set_t_c) nil) new_env) tparams) args) (funcall (hydra_lib_lists_concat2 bindings) new_bindings)) doms) tapps) body))))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (let ((ta_body (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) (let ((typ (funcall (lambda (v) (hydra_core_type_application_term-type v)) ta))) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_gather cx) for_binding) get_t_c) set_t_c) arg_mode) g_env) tparams) args) bindings) doms) (funcall (hydra_lib_lists_cons typ) tapps)) ta_body)))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (let ((tvar (funcall (lambda (v) (hydra_core_type_lambda-parameter v)) tl))) (let ((tl_body (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) (let ((new_env (funcall (set_t_c (funcall (hydra_schemas_extend_graph_for_type_lambda (get_t_c g_env)) tl)) g_env))) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_gather cx) for_binding) get_t_c) set_t_c) arg_mode) new_env) (funcall (hydra_lib_lists_cons tvar) tparams)) args) bindings) doms) tapps) tl_body))))) match_value)) (t (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_finish cx) get_t_c) g_env) tparams) args) bindings) doms) tapps) t_)))) (cadr match_target))) (hydra_rewriting_deannotate_term t_)))))))))))))))

(defvar hydra_coder_utils_analyze_function_term_with (lambda (cx) (lambda (for_binding) (lambda (get_t_c) (lambda (set_t_c) (lambda (env) (lambda (term) (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with_gather cx) for_binding) get_t_c) set_t_c) t) env) (list)) (list)) (list)) (list)) (list)) term))))))))

(defvar hydra_coder_utils_is_complex_variable (lambda (tc) (lambda (name) (let ((meta_lookup (funcall (hydra_lib_maps_lookup name) (funcall (lambda (v) (hydra_graph_graph-metadata v)) tc)))) (if (hydra_lib_maybes_is_just meta_lookup) t (if (funcall (hydra_lib_sets_member name) (funcall (lambda (v) (hydra_graph_graph-lambda_variables v)) tc)) t (let ((type_lookup (funcall (hydra_lib_maps_lookup name) (funcall (lambda (v) (hydra_graph_graph-bound_types v)) tc)))) (funcall (funcall (hydra_lib_maybes_maybe (lambda () t)) (lambda (ts) (funcall (hydra_lib_equality_gt (hydra_arity_type_scheme_arity ts)) 0))) type_lookup))))))))

(defvar hydra_coder_utils_is_complex_term (lambda (tc) (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :let) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :variable) (funcall (lambda (name) (funcall (hydra_coder_utils_is_complex_variable tc) name)) match_value)) (t (funcall (funcall (hydra_lib_lists_foldl (lambda (b) (lambda (sub) (funcall (hydra_lib_logic_or b) (funcall (hydra_coder_utils_is_complex_term tc) sub))))) nil) (hydra_rewriting_subterms t_))))) (cadr match_target))) t_))))

(defvar hydra_coder_utils_is_complex_binding (lambda (tc) (lambda (b) (let ((term (funcall (lambda (v) (hydra_core_binding-term v)) b))) (let ((mts (funcall (lambda (v) (hydra_core_binding-type v)) b))) (funcall (funcall (hydra_lib_maybes_cases mts) (lambda () (funcall (hydra_coder_utils_is_complex_term tc) term))) (lambda (ts) (let ((is_polymorphic (hydra_lib_logic_not (hydra_lib_lists_null (funcall (lambda (v) (hydra_core_type_scheme-variables v)) ts))))) (let ((is_non_nullary (funcall (hydra_lib_equality_gt (hydra_arity_type_arity (funcall (lambda (v) (hydra_core_type_scheme-type v)) ts))) 0))) (let ((is_complex (funcall (hydra_coder_utils_is_complex_term tc) term))) (funcall (hydra_lib_logic_or (funcall (hydra_lib_logic_or is_polymorphic) is_non_nullary)) is_complex)))))))))))

(defvar hydra_coder_utils_binding_metadata (lambda (tc) (lambda (b) (if (funcall (hydra_coder_utils_is_complex_binding tc) b) (list :just (list :literal (list :boolean t))) (list :nothing)))))

(defvar hydra_coder_utils_analyze_function_term (lambda (cx) (lambda (get_t_c) (lambda (set_t_c) (lambda (env) (lambda (term) (funcall (funcall (funcall (funcall (funcall (hydra_coder_utils_analyze_function_term_with cx) hydra_coder_utils_binding_metadata) get_t_c) set_t_c) env) term)))))))

(defvar hydra_coder_utils_comments_from_element (lambda (cx) (lambda (g) (lambda (b) (funcall (funcall (hydra_annotations_get_term_description cx) g) (funcall (lambda (v) (hydra_core_binding-term v)) b))))))

(defvar hydra_coder_utils_comments_from_field_type (lambda (cx) (lambda (g) (lambda (ft) (funcall (funcall (hydra_annotations_get_type_description cx) g) (funcall (lambda (v) (hydra_core_field_type-type v)) ft))))))

(defvar hydra_coder_utils_gather_applications (lambda (term) (letrec ((go_ (lambda (args) (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app) (let ((lhs (funcall (lambda (v) (hydra_core_application-function v)) app))) (let ((rhs (funcall (lambda (v) (hydra_core_application-argument v)) app))) (funcall (go_ (funcall (hydra_lib_lists_cons rhs) args)) lhs)))) match_value)) (t (list args t_)))) (cadr match_target))) (hydra_rewriting_deannotate_term t_)))))) (funcall (go_ (list)) term))))

(defvar hydra_coder_utils_gather_args (lambda (term) (lambda (args) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app) (let ((lhs (funcall (lambda (v) (hydra_core_application-function v)) app))) (let ((rhs (funcall (lambda (v) (hydra_core_application-argument v)) app))) (funcall (hydra_coder_utils_gather_args lhs) (funcall (hydra_lib_lists_cons rhs) args))))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (let ((body (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) (funcall (hydra_coder_utils_gather_args body) args))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (let ((body (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) (funcall (hydra_coder_utils_gather_args body) args))) match_value)) (t (list term args)))) (cadr match_target))) (hydra_rewriting_deannotate_term term)))))

(defvar hydra_coder_utils_gather_args_with_type_apps (lambda (term) (lambda (args) (lambda (ty_args) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app) (let ((lhs (funcall (lambda (v) (hydra_core_application-function v)) app))) (let ((rhs (funcall (lambda (v) (hydra_core_application-argument v)) app))) (funcall (funcall (hydra_coder_utils_gather_args_with_type_apps lhs) (funcall (hydra_lib_lists_cons rhs) args)) ty_args)))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (let ((body (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) (funcall (funcall (hydra_coder_utils_gather_args_with_type_apps body) args) ty_args))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (let ((body (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) (let ((typ (funcall (lambda (v) (hydra_core_type_application_term-type v)) ta))) (funcall (funcall (hydra_coder_utils_gather_args_with_type_apps body) args) (funcall (hydra_lib_lists_cons typ) ty_args))))) match_value)) (t (list term (list args ty_args))))) (cadr match_target))) (hydra_rewriting_deannotate_term term))))))

(defvar hydra_coder_utils_is_tail_recursive_in_tail_position (lambda (func_name) (lambda (term) (let ((stripped (hydra_rewriting_deannotate_and_detype_term term))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app) (let ((gathered (hydra_coder_utils_gather_applications stripped))) (let ((gather_args (hydra_lib_pairs_first gathered))) (let ((gather_fun (hydra_lib_pairs_second gathered))) (let ((stripped_fun (hydra_rewriting_deannotate_and_detype_term gather_fun))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (vname) (if (funcall (hydra_lib_equality_equal vname) func_name) (let ((args_no_func (funcall (funcall (hydra_lib_lists_foldl (lambda (ok) (lambda (arg) (funcall (hydra_lib_logic_and ok) (funcall (hydra_rewriting_is_free_variable_in_term func_name) arg))))) t) gather_args))) (let ((args_no_lambda (funcall (funcall (hydra_lib_lists_foldl (lambda (ok) (lambda (arg) (funcall (hydra_lib_logic_and ok) (hydra_lib_logic_not (funcall (funcall (funcall (hydra_rewriting_fold_over_term (list :pre nil)) (lambda (found) (lambda (t_) (funcall (hydra_lib_logic_or found) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :function) (funcall (lambda (f2) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (lam) (let ((ignore (funcall (lambda (v) (hydra_core_lambda-body v)) lam))) t)) match_value)) (t nil))) (cadr match_target))) f2)) match_value)) (t nil))) (cadr match_target))) t_))))) nil) arg)))))) t) gather_args))) (funcall (hydra_lib_logic_and args_no_func) args_no_lambda))) (funcall (hydra_rewriting_is_free_variable_in_term func_name) term))) match_value)) ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :elimination) (funcall (lambda (e) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :union) (funcall (lambda (cs) (let ((cases_ (funcall (lambda (v) (hydra_core_case_statement-cases v)) cs))) (let ((dflt (funcall (lambda (v) (hydra_core_case_statement-default v)) cs))) (let ((branches_ok (funcall (funcall (hydra_lib_lists_foldl (lambda (ok) (lambda (field) (funcall (hydra_lib_logic_and ok) (funcall (hydra_coder_utils_is_tail_recursive_in_tail_position func_name) (funcall (lambda (v) (hydra_core_field-term v)) field)))))) t) cases_))) (let ((dflt_ok (funcall (funcall (hydra_lib_maybes_maybe (lambda () t)) (lambda (d) (funcall (hydra_coder_utils_is_tail_recursive_in_tail_position func_name) d))) dflt))) (let ((args_ok (funcall (funcall (hydra_lib_lists_foldl (lambda (ok) (lambda (arg) (funcall (hydra_lib_logic_and ok) (funcall (hydra_rewriting_is_free_variable_in_term func_name) arg))))) t) gather_args))) (funcall (hydra_lib_logic_and (funcall (hydra_lib_logic_and branches_ok) dflt_ok)) args_ok))))))) match_value)) (t (funcall (hydra_rewriting_is_free_variable_in_term func_name) term)))) (cadr match_target))) e)) match_value)) (t (funcall (hydra_rewriting_is_free_variable_in_term func_name) term)))) (cadr match_target))) f)) match_value)) (t (funcall (hydra_rewriting_is_free_variable_in_term func_name) term)))) (cadr match_target))) stripped_fun)))))) match_value)) ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (lam) (funcall (hydra_coder_utils_is_tail_recursive_in_tail_position func_name) (funcall (lambda (v) (hydra_core_lambda-body v)) lam))) match_value)) (t (funcall (hydra_rewriting_is_free_variable_in_term func_name) term)))) (cadr match_target))) f)) match_value)) ((equal (car match_target) :let) (funcall (lambda (lt) (let ((bindings_ok (funcall (funcall (hydra_lib_lists_foldl (lambda (ok) (lambda (b) (funcall (hydra_lib_logic_and ok) (funcall (hydra_rewriting_is_free_variable_in_term func_name) (funcall (lambda (v) (hydra_core_binding-term v)) b)))))) t) (funcall (lambda (v) (hydra_core_let-bindings v)) lt)))) (funcall (hydra_lib_logic_and bindings_ok) (funcall (hydra_coder_utils_is_tail_recursive_in_tail_position func_name) (funcall (lambda (v) (hydra_core_let-body v)) lt))))) match_value)) (t (funcall (hydra_rewriting_is_free_variable_in_term func_name) term)))) (cadr match_target))) stripped)))))

(defvar hydra_coder_utils_is_self_tail_recursive (lambda (func_name) (lambda (body) (let ((calls_self (hydra_lib_logic_not (funcall (hydra_rewriting_is_free_variable_in_term func_name) body)))) (if calls_self (funcall (hydra_coder_utils_is_tail_recursive_in_tail_position func_name) body) nil)))))

(defvar hydra_coder_utils_is_simple_assignment (lambda (term) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (hydra_coder_utils_is_simple_assignment (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) match_value)) ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (_) nil) match_value)) (t t))) (cadr match_target))) f)) match_value)) ((equal (car match_target) :let) (funcall (lambda (_) nil) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (_) nil) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (hydra_coder_utils_is_simple_assignment (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) match_value)) (t (let ((base_term (hydra_lib_pairs_first (funcall (hydra_coder_utils_gather_args term) (list))))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :elimination) (funcall (lambda (elim) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :union) (funcall (lambda (_) nil) match_value)) (t t))) (cadr match_target))) elim)) match_value)) (t t))) (cadr match_target))) f)) match_value)) (t t))) (cadr match_target))) base_term))))) (cadr match_target))) term)))

(defvar hydra_coder_utils_is_trivial_term (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :literal) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :variable) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :unit) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :application) (funcall (lambda (app) (let ((fun (funcall (lambda (v) (hydra_core_application-function v)) app))) (let ((arg (funcall (lambda (v) (hydra_core_application-argument v)) app))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :function) (funcall (lambda (f) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :elimination) (funcall (lambda (e) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :record) (funcall (lambda (_) (hydra_coder_utils_is_trivial_term arg)) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (_) (hydra_coder_utils_is_trivial_term arg)) match_value)) (t nil))) (cadr match_target))) e)) match_value)) (t nil))) (cadr match_target))) f)) match_value)) (t nil))) (cadr match_target))) fun)))) match_value)) ((equal (car match_target) :maybe) (funcall (lambda (opt) (funcall (funcall (hydra_lib_maybes_maybe (lambda () t)) (lambda (inner) (hydra_coder_utils_is_trivial_term inner))) opt)) match_value)) ((equal (car match_target) :record) (funcall (lambda (rec) (funcall (funcall (hydra_lib_lists_foldl (lambda (acc) (lambda (fld) (funcall (hydra_lib_logic_and acc) (hydra_coder_utils_is_trivial_term (funcall (lambda (v) (hydra_core_field-term v)) fld)))))) t) (funcall (lambda (v) (hydra_core_record-fields v)) rec))) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (wt) (hydra_coder_utils_is_trivial_term (funcall (lambda (v) (hydra_core_wrapped_term-body v)) wt))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (hydra_coder_utils_is_trivial_term (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (hydra_coder_utils_is_trivial_term (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) match_value)) (t nil))) (cadr match_target))) (hydra_rewriting_deannotate_term t_))))

(defvar hydra_coder_utils_name_to_file_path (lambda (ns_conv) (lambda (local_conv) (lambda (ext) (lambda (name) (let ((qual_name (hydra_names_qualify_name name))) (let ((ns_ (funcall (lambda (v) (hydra_module_qualified_name-namespace v)) qual_name))) (let ((local (funcall (lambda (v) (hydra_module_qualified_name-local v)) qual_name))) (let ((ns_to_file_path (lambda (ns_) (funcall (hydra_lib_strings_intercalate "/") (funcall (hydra_lib_lists_map (lambda (part) (funcall (funcall (hydra_formatting_convert_case (list :camel nil)) ns_conv) part))) (funcall (hydra_lib_strings_split_on ".") (funcall (lambda (v) v) ns_))))))) (let ((prefix (funcall (funcall (hydra_lib_maybes_maybe (lambda () "")) (lambda (n) (funcall (hydra_lib_strings_cat2 (ns_to_file_path n)) "/"))) ns_))) (let ((suffix (funcall (funcall (hydra_formatting_convert_case (list :pascal nil)) local_conv) local))) (hydra_lib_strings_cat (list prefix suffix "." (funcall (lambda (v) v) ext))))))))))))))

(defvar hydra_coder_utils_normalize_comment (lambda (s) (let ((stripped (hydra_formatting_strip_leading_and_trailing_whitespace s))) (if (hydra_lib_strings_null stripped) "" (let ((last_idx (funcall (hydra_lib_math_sub (hydra_lib_strings_length stripped)) 1))) (let ((last_char (funcall (hydra_lib_strings_char_at last_idx) stripped))) (if (funcall (hydra_lib_equality_equal last_char) 46) stripped (funcall (hydra_lib_strings_cat2 stripped) "."))))))))

(defvar hydra_coder_utils_union_type_to_record_type (lambda (rt) (let ((make_optional (lambda (f) (let ((fn_ (funcall (lambda (v) (hydra_core_field_type-name v)) f))) (let ((ft (funcall (lambda (v) (hydra_core_field_type-type v)) f))) (make-hydra_core_field_type fn_ (funcall (hydra_rewriting_map_beneath_type_annotations (lambda (x) (list :maybe x))) ft))))))) (funcall (hydra_lib_lists_map make_optional) rt))))

(provide 'hydra.coderUtils)
