(require 'cl-lib)

(require 'hydra.accessors)

(require 'hydra.core)

(require 'hydra.graph)

(require 'hydra.lexical)

(require 'hydra.lib.equality)

(require 'hydra.lib.lists)

(require 'hydra.lib.literals)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.math)

(require 'hydra.lib.maybes)

(require 'hydra.lib.pairs)

(require 'hydra.lib.sets)

(require 'hydra.lib.strings)

(require 'hydra.rewriting)

(require 'hydra.schemas)

(require 'hydra.sorting)

(require 'hydra.substitution)

(require 'hydra.typing)

(defvar hydra_hoisting_augment_bindings_with_new_free_vars (lambda (cx) (lambda (bound_vars) (lambda (bindings) (let ((types ((hydra_lib_maps_map hydra_rewriting_type_scheme_to_f_type) ((lambda (v) (hydra_graph_graph-bound_types v)) cx)))) (letrec ((wrap_after_type_lambdas (lambda (vars) (lambda (term) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :type_lambda) ((lambda (tl) (list :type_lambda (make-hydra_core_type_lambda ((lambda (v) (hydra_core_type_lambda-parameter v)) tl) ((wrap_after_type_lambdas vars) ((lambda (v) (hydra_core_type_lambda-body v)) tl))))) match_value)) (t (((hydra_lib_lists_foldl (lambda (t_) (lambda (p) (list :function (list :lambda (make-hydra_core_lambda (hydra_lib_pairs_first p) (hydra_lib_pairs_second p) t_)))))) term) (hydra_lib_lists_reverse vars))))) (cadr match_target))) term))))) (let ((augment (lambda (b) (let ((free_vars (hydra_lib_sets_to_list ((hydra_lib_sets_intersection bound_vars) (hydra_rewriting_free_variables_in_term ((lambda (v) (hydra_core_binding-term v)) b)))))) (let ((var_type_pairs ((hydra_lib_lists_map (lambda (v) (list v ((hydra_lib_maps_lookup v) types)))) free_vars))) (let ((var_types (hydra_lib_maybes_cat ((hydra_lib_lists_map hydra_lib_pairs_second) var_type_pairs)))) (if ((hydra_lib_logic_or (hydra_lib_lists_null free_vars)) (hydra_lib_logic_not ((hydra_lib_equality_equal (hydra_lib_lists_length var_types)) (hydra_lib_lists_length var_type_pairs)))) (list b nil) (list (make-hydra_core_binding ((lambda (v) (hydra_core_binding-name v)) b) ((wrap_after_type_lambdas var_type_pairs) ((lambda (v) (hydra_core_binding-term v)) b)) ((hydra_lib_maybes_map (lambda (ts) (make-hydra_core_type_scheme ((lambda (v) (hydra_core_type_scheme-variables v)) ts) (((hydra_lib_lists_foldl (lambda (acc) (lambda (t_) (list :function (make-hydra_core_function_type t_ acc))))) ((lambda (v) (hydra_core_type_scheme-type v)) ts)) (hydra_lib_lists_reverse var_types)) ((lambda (v) (hydra_core_type_scheme-constraints v)) ts)))) ((lambda (v) (hydra_core_binding-type v)) b))) (list ((lambda (v) (hydra_core_binding-name v)) b) (((hydra_lib_lists_foldl (lambda (t_) (lambda (v) (list :application (make-hydra_core_application t_ (list :variable v)))))) (list :variable ((lambda (v) (hydra_core_binding-name v)) b))) free_vars)))))))))) (let ((results ((hydra_lib_lists_map augment) bindings))) (list ((hydra_lib_lists_map hydra_lib_pairs_first) results) (hydra_lib_maps_from_list (hydra_lib_maybes_cat ((hydra_lib_lists_map hydra_lib_pairs_second) results))))))))))))

(defvar hydra_hoisting_binding_is_polymorphic (lambda (binding) (((hydra_lib_maybes_maybe nil) (lambda (ts) (hydra_lib_logic_not (hydra_lib_lists_null ((lambda (v) (hydra_core_type_scheme-variables v)) ts))))) ((lambda (v) (hydra_core_binding-type v)) binding))))

(defvar hydra_hoisting_binding_uses_context_type_vars (lambda (cx) (lambda (binding) (((hydra_lib_maybes_maybe nil) (lambda (ts) (let ((free_in_type (hydra_rewriting_free_variables_in_type ((lambda (v) (hydra_core_type_scheme-type v)) ts)))) (let ((context_type_vars ((lambda (v) (hydra_graph_graph-type_variables v)) cx))) (hydra_lib_logic_not (hydra_lib_sets_null ((hydra_lib_sets_intersection free_in_type) context_type_vars))))))) ((lambda (v) (hydra_core_binding-type v)) binding)))))

(defvar hydra_hoisting_count_var_occurrences (lambda (name) (lambda (term) (let ((child_count (((hydra_lib_lists_foldl (lambda (acc) (lambda (t_) ((hydra_lib_math_add acc) ((hydra_hoisting_count_var_occurrences name) t_))))) 0) (hydra_rewriting_subterms term)))) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :variable) ((lambda (v) (if ((hydra_lib_equality_equal v) name) ((hydra_lib_math_add 1) child_count) child_count)) match_value)) (t child_count))) (cadr match_target))) term)))))

(defvar hydra_hoisting_rewrite_and_fold_term_with_type_context (lambda (f) (lambda (cx0) (lambda (val0) (lambda (term0) (let ((wrapper (lambda (low_level_recurse) (lambda (val_and_cx) (lambda (term) (let ((val (hydra_lib_pairs_first val_and_cx))) (let ((cx (hydra_lib_pairs_second val_and_cx))) (let ((cx1 ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :function) ((lambda (fun) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :lambda) ((lambda (l) ((hydra_schemas_extend_graph_for_lambda cx) l)) match_value)) (t cx))) (cadr match_target))) fun)) match_value)) ((equal (car match_target) :let) ((lambda (l) (((hydra_schemas_extend_graph_for_let (lambda (_) (lambda (_) nil))) cx) l)) match_value)) ((equal (car match_target) :type_lambda) ((lambda (tl) ((hydra_schemas_extend_graph_for_type_lambda cx) tl)) match_value)) (t cx))) (cadr match_target))) term))) (let ((recurse_for_user (lambda (new_val) (lambda (subterm) (let ((result ((low_level_recurse (list new_val cx1)) subterm))) (list (hydra_lib_pairs_first (hydra_lib_pairs_first result)) (hydra_lib_pairs_second result))))))) (let ((f_result ((((f recurse_for_user) cx1) val) term))) (list (list (hydra_lib_pairs_first f_result) cx) (hydra_lib_pairs_second f_result)))))))))))) (let ((result (((hydra_rewriting_rewrite_and_fold_term wrapper) (list val0 cx0)) term0))) (list (hydra_lib_pairs_first (hydra_lib_pairs_first result)) (hydra_lib_pairs_second result)))))))))

(defvar hydra_hoisting_hoist_let_bindings_with_predicate (lambda (is_parent_binding) (lambda (should_hoist_binding) (lambda (cx0) (lambda (let0) (let ((hoist_one (lambda (prefix) (lambda (cx) (lambda (pair) (lambda (binding_with_captured_vars) (let ((binding_and_replacement_pairs (hydra_lib_pairs_first pair))) (let ((already_used_names (hydra_lib_pairs_second pair))) (let ((b (hydra_lib_pairs_first binding_with_captured_vars))) (let ((captured_term_vars (hydra_lib_pairs_second binding_with_captured_vars))) (let ((types ((hydra_lib_maps_map hydra_rewriting_type_scheme_to_f_type) ((lambda (v) (hydra_graph_graph-bound_types v)) cx)))) (let ((captured_term_var_type_pairs ((hydra_lib_lists_map (lambda (v) (list v ((hydra_lib_maps_lookup v) types)))) captured_term_vars))) (let ((captured_term_var_types ((hydra_lib_lists_map (lambda (typ) (hydra_rewriting_deannotate_type_parameters typ))) (hydra_lib_maybes_cat ((hydra_lib_lists_map hydra_lib_pairs_second) captured_term_var_type_pairs))))) (let ((free_in_binding_type (((hydra_lib_maybes_maybe hydra_lib_sets_empty) (lambda (ts) (hydra_rewriting_free_variables_in_type ((lambda (v) (hydra_core_type_scheme-type v)) ts)))) ((lambda (v) (hydra_core_binding-type v)) b)))) (let ((free_in_captured_var_types (hydra_lib_sets_unions ((hydra_lib_lists_map (lambda (t_) (hydra_rewriting_free_variables_in_type t_))) captured_term_var_types)))) (let ((captured_type_vars (hydra_lib_sets_to_list ((hydra_lib_sets_intersection ((lambda (v) (hydra_graph_graph-type_variables v)) cx)) ((hydra_lib_sets_union free_in_binding_type) free_in_captured_var_types))))) (let ((global_binding_name ((hydra_lexical_choose_unique_name already_used_names) ((hydra_lib_strings_cat2 prefix) ((lambda (v) v) ((lambda (v) (hydra_core_binding-name v)) b)))))) (let ((new_used_names ((hydra_lib_sets_insert global_binding_name) already_used_names))) (let ((new_type_scheme (if ((hydra_lib_equality_equal (hydra_lib_lists_length captured_term_var_types)) (hydra_lib_lists_length captured_term_var_type_pairs)) ((hydra_lib_maybes_map (lambda (ts) (make-hydra_core_type_scheme (hydra_lib_lists_nub ((hydra_lib_lists_concat2 captured_type_vars) ((lambda (v) (hydra_core_type_scheme-variables v)) ts))) (((hydra_lib_lists_foldl (lambda (t_) (lambda (a) (list :function (make-hydra_core_function_type a t_))))) ((lambda (v) (hydra_core_type_scheme-type v)) ts)) (hydra_lib_lists_reverse captured_term_var_types)) ((lambda (v) (hydra_core_type_scheme-constraints v)) ts)))) ((lambda (v) (hydra_core_binding-type v)) b)) nil))) (let ((stripped_term (hydra_rewriting_strip_type_lambdas ((lambda (v) (hydra_core_binding-term v)) b)))) (let ((term_with_lambdas (((hydra_lib_lists_foldl (lambda (t_) (lambda (p) (list :function (list :lambda (make-hydra_core_lambda (hydra_lib_pairs_first p) ((hydra_lib_maybes_map (lambda (dom) (hydra_rewriting_deannotate_type_parameters dom))) (hydra_lib_pairs_second p)) t_)))))) stripped_term) (hydra_lib_lists_reverse captured_term_var_type_pairs)))) (let ((term_with_type_lambdas (((hydra_lib_lists_foldl (lambda (t_) (lambda (v) (list :type_lambda (make-hydra_core_type_lambda v t_))))) term_with_lambdas) (hydra_lib_lists_reverse (((hydra_lib_maybes_maybe (list)) (lambda (v) (hydra_core_type_scheme-variables v))) new_type_scheme))))) (let ((with_type_apps (((hydra_lib_lists_foldl (lambda (t_) (lambda (v) (list :type_application (make-hydra_core_type_application_term t_ (list :variable v)))))) (list :variable global_binding_name)) captured_type_vars))) (let ((replacement (((hydra_lib_lists_foldl (lambda (t_) (lambda (v) (list :application (make-hydra_core_application t_ (list :variable v)))))) with_type_apps) captured_term_vars))) (let ((new_binding_and_replacement (list (make-hydra_core_binding global_binding_name term_with_type_lambdas new_type_scheme) replacement))) (let ((new_pairs ((hydra_lib_lists_cons new_binding_and_replacement) binding_and_replacement_pairs))) (list new_pairs new_used_names))))))))))))))))))))))))))) (let ((rewrite (lambda (prefix) (lambda (recurse) (lambda (cx) (lambda (bindings_and_names) (lambda (term) (let ((previously_finished_bindings (hydra_lib_pairs_first bindings_and_names))) (let ((empty_bindings_and_names (list (list) (hydra_lib_pairs_second bindings_and_names)))) (let ((result ((recurse empty_bindings_and_names) term))) (let ((new_bindings_and_names (hydra_lib_pairs_first result))) (let ((bindings_so_far (hydra_lib_pairs_first new_bindings_and_names))) (let ((already_used_names (hydra_lib_pairs_second new_bindings_and_names))) (let ((new_term (hydra_lib_pairs_second result))) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :let) ((lambda (l) (let ((body ((lambda (v) (hydra_core_let-body v)) l))) (let ((partition_pair ((hydra_lib_lists_partition (should_hoist_binding cx)) ((lambda (v) (hydra_core_let-bindings v)) l)))) (let ((hoist_us (hydra_lib_pairs_first partition_pair))) (let ((keep_us (hydra_lib_pairs_second partition_pair))) (let ((hoisted_binding_names ((hydra_lib_lists_map (lambda (v) (hydra_core_binding-name v))) hoist_us))) (let ((poly_let_variables (hydra_lib_sets_from_list ((hydra_lib_lists_filter (lambda (v) (((hydra_lib_maybes_maybe nil) hydra_schemas_f_type_is_polymorphic) ((hydra_lib_maybes_map hydra_rewriting_type_scheme_to_f_type) ((hydra_lib_maps_lookup v) ((lambda (v) (hydra_graph_graph-bound_types v)) cx)))))) (hydra_lib_sets_to_list ((hydra_lib_sets_difference (hydra_lib_sets_from_list (hydra_lib_maps_keys ((lambda (v) (hydra_graph_graph-bound_terms v)) cx)))) ((lambda (v) (hydra_graph_graph-lambda_variables v)) cx))))))) (let ((bound_term_variables ((hydra_lib_sets_union ((lambda (v) (hydra_graph_graph-lambda_variables v)) cx)) ((hydra_lib_sets_difference (hydra_lib_sets_from_list (hydra_lib_maps_keys ((lambda (v) (hydra_graph_graph-bound_terms v)) cx)))) ((lambda (v) (hydra_graph_graph-lambda_variables v)) cx))))) (let ((free_variables_in_each_binding ((hydra_lib_lists_map (lambda (b) (hydra_lib_sets_to_list ((hydra_lib_sets_intersection bound_term_variables) (hydra_rewriting_free_variables_in_term ((lambda (v) (hydra_core_binding-term v)) b)))))) hoist_us))) (let ((binding_dependencies ((hydra_lib_lists_map (lambda (vars) ((hydra_lib_lists_partition (lambda (v) ((hydra_lib_sets_member v) (hydra_lib_sets_from_list hoisted_binding_names)))) vars))) free_variables_in_each_binding))) (let ((binding_edges ((hydra_lib_lists_zip hoisted_binding_names) ((hydra_lib_lists_map hydra_lib_pairs_first) binding_dependencies)))) (let ((binding_immediate_captured_vars ((hydra_lib_lists_zip hoisted_binding_names) ((hydra_lib_lists_map hydra_lib_pairs_second) binding_dependencies)))) (let ((captured_vars_map (hydra_lib_maps_from_list ((hydra_sorting_propagate_tags binding_edges) binding_immediate_captured_vars)))) (let ((bindings_with_captured_vars ((hydra_lib_lists_map (lambda (b) (list b (((hydra_lib_maybes_maybe (list)) (lambda (vars) (hydra_lib_sets_to_list ((hydra_lib_sets_difference vars) poly_let_variables)))) ((hydra_lib_maps_lookup ((lambda (v) (hydra_core_binding-name v)) b)) captured_vars_map))))) hoist_us))) (let ((hoist_pairs_and_names (((hydra_lib_lists_foldl ((hoist_one prefix) cx)) (list (list) already_used_names)) bindings_with_captured_vars))) (let ((hoist_pairs (hydra_lib_lists_reverse (hydra_lib_pairs_first hoist_pairs_and_names)))) (let ((hoisted_bindings ((hydra_lib_lists_map hydra_lib_pairs_first) hoist_pairs))) (let ((replacements ((hydra_lib_lists_map hydra_lib_pairs_second) hoist_pairs))) (let ((final_used_names (hydra_lib_pairs_second hoist_pairs_and_names))) (let ((hoist_name_replacement_pairs ((hydra_lib_lists_zip ((hydra_lib_lists_map (lambda (v) (hydra_core_binding-name v))) hoist_us)) replacements))) (let ((hoist_binding_map (hydra_lib_maps_from_list ((hydra_lib_lists_map (lambda (b) (list ((lambda (v) (hydra_core_binding-name v)) b) b))) hoist_us)))) (let ((is_cacheable (lambda (name) (let ((multi_ref ((hydra_lib_equality_gte ((hydra_hoisting_count_var_occurrences name) body)) 2))) (let ((is_poly (((hydra_lib_maybes_maybe nil) (lambda (b) (hydra_hoisting_binding_is_polymorphic b))) ((hydra_lib_maps_lookup name) hoist_binding_map)))) ((hydra_lib_logic_and multi_ref) (hydra_lib_logic_not is_poly))))))) (let ((single_ref_pairs ((hydra_lib_lists_filter (lambda (p) (hydra_lib_logic_not (is_cacheable (hydra_lib_pairs_first p))))) hoist_name_replacement_pairs))) (let ((multi_ref_pairs ((hydra_lib_lists_filter (lambda (p) (is_cacheable (hydra_lib_pairs_first p)))) hoist_name_replacement_pairs))) (let ((full_subst (hydra_lib_maps_from_list hoist_name_replacement_pairs))) (let ((body_only_subst (hydra_lib_maps_from_list single_ref_pairs))) (let ((body_subst ((hydra_substitution_substitute_in_term body_only_subst) body))) (let ((cache_bindings ((hydra_lib_lists_map (lambda (p) (let ((orig_type (((hydra_lib_maybes_maybe nil) (lambda (b) ((lambda (v) (hydra_core_binding-type v)) b))) ((hydra_lib_maps_lookup (hydra_lib_pairs_first p)) hoist_binding_map)))) (make-hydra_core_binding (hydra_lib_pairs_first p) (hydra_lib_pairs_second p) orig_type)))) multi_ref_pairs))) (let ((body_with_cache (if (hydra_lib_lists_null cache_bindings) body_subst (list :let (make-hydra_core_let cache_bindings body_subst))))) (let ((keep_us_subst ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding full_subst)) keep_us))) (let ((hoisted_bindings_subst ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding full_subst)) hoisted_bindings))) (let ((bindings_so_far_subst ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding full_subst)) bindings_so_far))) (let ((augment_result (((hydra_hoisting_augment_bindings_with_new_free_vars cx) ((hydra_lib_sets_difference bound_term_variables) poly_let_variables)) bindings_so_far_subst))) (let ((bindings_so_far_augmented (hydra_lib_pairs_first augment_result))) (let ((augment_subst (hydra_lib_pairs_second augment_result))) (let ((hoisted_bindings_final ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding augment_subst)) hoisted_bindings_subst))) (let ((bindings_so_far_final ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding augment_subst)) bindings_so_far_augmented))) (let ((body_final ((hydra_substitution_substitute_in_term augment_subst) body_with_cache))) (let ((keep_us_final ((hydra_lib_lists_map (hydra_substitution_substitute_in_binding augment_subst)) keep_us_subst))) (let ((final_term (if (hydra_lib_lists_null keep_us_final) body_final (list :let (make-hydra_core_let keep_us_final body_final))))) (list (list (hydra_lib_lists_concat (list previously_finished_bindings hoisted_bindings_final bindings_so_far_final)) final_used_names) final_term))))))))))))))))))))))))))))))))))))))))) match_value)) (t (list (list ((hydra_lib_lists_concat2 previously_finished_bindings) bindings_so_far) already_used_names) new_term)))) (cadr match_target))) new_term))))))))))))))) (let ((cx1 (((hydra_schemas_extend_graph_for_let (lambda (c) (lambda (b) nil))) cx0) let0))) (let ((for_active_binding (lambda (b) (let ((prefix ((hydra_lib_strings_cat2 ((lambda (v) v) ((lambda (v) (hydra_core_binding-name v)) b))) "_"))) (let ((init (list (list) (hydra_lib_sets_singleton ((lambda (v) (hydra_core_binding-name v)) b))))) (let ((result_pair ((((hydra_hoisting_rewrite_and_fold_term_with_type_context (rewrite prefix)) cx1) init) ((lambda (v) (hydra_core_binding-term v)) b)))) (let ((result_bindings (hydra_lib_pairs_first (hydra_lib_pairs_first result_pair)))) (let ((result_term (hydra_lib_pairs_second result_pair))) ((hydra_lib_lists_cons (make-hydra_core_binding ((lambda (v) (hydra_core_binding-name v)) b) result_term ((lambda (v) (hydra_core_binding-type v)) b))) result_bindings))))))))) (let ((for_binding (lambda (b) (if (is_parent_binding b) (for_active_binding b) (list b))))) (make-hydra_core_let (hydra_lib_lists_concat ((hydra_lib_lists_map for_binding) ((lambda (v) (hydra_core_let-bindings v)) let0))) ((lambda (v) (hydra_core_let-body v)) let0))))))))))))

(defvar hydra_hoisting_should_hoist_all (lambda (_) (lambda (_) t)))

(defvar hydra_hoisting_hoist_all_let_bindings (lambda (let0) (let ((empty_cx (make-hydra_graph_graph hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty))) ((((hydra_hoisting_hoist_let_bindings_with_predicate (lambda (_) t)) hydra_hoisting_should_hoist_all) empty_cx) let0))))

(defvar hydra_hoisting_rewrite_and_fold_term_with_type_context_and_path (lambda (f) (lambda (cx0) (lambda (val0) (lambda (term0) (let ((wrapper (lambda (recurse) (lambda (path) (lambda (cx_and_val) (lambda (term) (let ((cx (hydra_lib_pairs_first cx_and_val))) (let ((val (hydra_lib_pairs_second cx_and_val))) (let ((cx1 ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :function) ((lambda (fun) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :lambda) ((lambda (l) ((hydra_schemas_extend_graph_for_lambda cx) l)) match_value)) (t cx))) (cadr match_target))) fun)) match_value)) ((equal (car match_target) :let) ((lambda (l) (((hydra_schemas_extend_graph_for_let (lambda (_) (lambda (_) nil))) cx) l)) match_value)) ((equal (car match_target) :type_lambda) ((lambda (tl) ((hydra_schemas_extend_graph_for_type_lambda cx) tl)) match_value)) (t cx))) (cadr match_target))) term))) (let ((recurse_for_user (lambda (val_in) (lambda (term_in) (let ((result (((recurse path) (list cx1 val_in)) term_in))) (list (hydra_lib_pairs_second (hydra_lib_pairs_first result)) (hydra_lib_pairs_second result))))))) (let ((f_result (((((f recurse_for_user) path) cx1) val) term))) (list (list cx (hydra_lib_pairs_first f_result)) (hydra_lib_pairs_second f_result))))))))))))) (let ((result (((hydra_rewriting_rewrite_and_fold_term_with_path wrapper) (list cx0 val0)) term0))) (list (hydra_lib_pairs_second (hydra_lib_pairs_first result)) (hydra_lib_pairs_second result)))))))))

(defvar hydra_hoisting_hoist_subterms (lambda (should_hoist) (lambda (cx0) (lambda (term0) (let ((process_immediate_subterm (lambda (cx) (lambda (counter) (lambda (name_prefix) (lambda (path_prefix) (lambda (subterm) (let ((baseline_lambda_vars ((lambda (v) (hydra_graph_graph-lambda_variables v)) cx))) (let ((collect_and_replace (lambda (recurse) (lambda (path) (lambda (cx_inner) (lambda (acc) (lambda (term) (let ((current_counter (hydra_lib_pairs_first acc))) (let ((collected_bindings (hydra_lib_pairs_second acc))) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :let) ((lambda (_) (list acc term)) match_value)) ((equal (car match_target) :type_lambda) ((lambda (_) (list acc term)) match_value)) (t (let ((result ((recurse acc) term))) (let ((new_acc (hydra_lib_pairs_first result))) (let ((processed_term (hydra_lib_pairs_second result))) (let ((new_counter (hydra_lib_pairs_first new_acc))) (let ((new_bindings (hydra_lib_pairs_second new_acc))) (let ((full_path ((hydra_lib_lists_concat2 path_prefix) path))) (if (should_hoist (list full_path processed_term)) (let ((binding_name (hydra_lib_strings_cat (list "_hoist_" name_prefix "_" (hydra_lib_literals_show_int32 new_counter))))) (let ((all_lambda_vars ((lambda (v) (hydra_graph_graph-lambda_variables v)) cx_inner))) (let ((new_lambda_vars ((hydra_lib_sets_difference all_lambda_vars) baseline_lambda_vars))) (let ((free_vars (hydra_rewriting_free_variables_in_term processed_term))) (let ((captured_vars (hydra_lib_sets_to_list ((hydra_lib_sets_intersection new_lambda_vars) free_vars)))) (let ((type_map ((hydra_lib_maps_map hydra_rewriting_type_scheme_to_f_type) ((lambda (v) (hydra_graph_graph-bound_types v)) cx_inner)))) (let ((wrapped_term (((hydra_lib_lists_foldl (lambda (body) (lambda (var_name) (list :function (list :lambda (make-hydra_core_lambda var_name ((hydra_lib_maps_lookup var_name) type_map) body)))))) processed_term) (hydra_lib_lists_reverse captured_vars)))) (let ((reference (((hydra_lib_lists_foldl (lambda (fn_) (lambda (var_name) (list :application (make-hydra_core_application fn_ (list :variable var_name)))))) (list :variable binding_name)) captured_vars))) (let ((new_binding (make-hydra_core_binding binding_name wrapped_term nil))) (list (list ((hydra_lib_math_add new_counter) 1) ((hydra_lib_lists_cons new_binding) new_bindings)) reference)))))))))) (list new_acc processed_term))))))))))) (cadr match_target))) term)))))))))) (let ((result ((((hydra_hoisting_rewrite_and_fold_term_with_type_context_and_path collect_and_replace) cx) (list counter (list))) subterm))) (let ((final_acc (hydra_lib_pairs_first result))) (let ((transformed_subterm (hydra_lib_pairs_second result))) (let ((final_counter (hydra_lib_pairs_first final_acc))) (let ((bindings (hydra_lib_pairs_second final_acc))) (if (hydra_lib_lists_null bindings) (list final_counter transformed_subterm) (let ((local_let (list :let (make-hydra_core_let (hydra_lib_lists_reverse bindings) transformed_subterm)))) (list final_counter local_let))))))))))))))))) (let ((process_let_term (lambda (cx) (lambda (counter) (lambda (path) (lambda (lt) (let ((bindings ((lambda (v) (hydra_core_let-bindings v)) lt))) (let ((body ((lambda (v) (hydra_core_let-body v)) lt))) (let ((process_binding (lambda (acc) (lambda (binding) (let ((name_prefix ((hydra_lib_strings_intercalate "_") ((hydra_lib_strings_split_on ".") ((lambda (v) v) ((lambda (v) (hydra_core_binding-name v)) binding)))))) (let ((binding_path_prefix ((hydra_lib_lists_concat2 path) (list (list :let_binding ((lambda (v) (hydra_core_binding-name v)) binding)))))) (let ((result (((((process_immediate_subterm cx) 1) name_prefix) binding_path_prefix) ((lambda (v) (hydra_core_binding-term v)) binding)))) (let ((new_value (hydra_lib_pairs_second result))) (let ((new_binding (make-hydra_core_binding ((lambda (v) (hydra_core_binding-name v)) binding) new_value ((lambda (v) (hydra_core_binding-type v)) binding)))) ((hydra_lib_lists_cons new_binding) acc)))))))))) (let ((new_bindings_reversed (((hydra_lib_lists_foldl process_binding) (list)) bindings))) (let ((new_bindings (hydra_lib_lists_reverse new_bindings_reversed))) (let ((body_path_prefix ((hydra_lib_lists_concat2 path) (list (list :let_body nil))))) (let ((body_result (((((process_immediate_subterm cx) 1) "_body") body_path_prefix) body))) (let ((new_body (hydra_lib_pairs_second body_result))) (list counter (list :let (make-hydra_core_let new_bindings new_body))))))))))))))))) (let ((rewrite (lambda (recurse) (lambda (path) (lambda (cx) (lambda (counter) (lambda (term) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :let) ((lambda (lt) (let ((recursed ((recurse counter) term))) (let ((new_counter (hydra_lib_pairs_first recursed))) (let ((recursed_term (hydra_lib_pairs_second recursed))) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :let) ((lambda (lt2) ((((process_let_term cx) new_counter) path) lt2)) match_value)) (t (list new_counter recursed_term)))) (cadr match_target))) recursed_term))))) match_value)) (t ((recurse counter) term)))) (cadr match_target))) term)))))))) (hydra_lib_pairs_second ((((hydra_hoisting_rewrite_and_fold_term_with_type_context_and_path rewrite) cx0) 1) term0)))))))))

(defvar hydra_hoisting_is_elimination_union (lambda (f) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :elimination) ((lambda (e) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :union) ((lambda (_) t) match_value)) (t nil))) (cadr match_target))) e)) match_value)) (t nil))) (cadr match_target))) f)))

(defvar hydra_hoisting_is_union_elimination (lambda (term) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :function) ((lambda (f) (hydra_hoisting_is_elimination_union f)) match_value)) (t nil))) (cadr match_target))) term)))

(defvar hydra_hoisting_is_union_elimination_application (lambda (term) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :application) ((lambda (app) (hydra_hoisting_is_union_elimination (hydra_rewriting_deannotate_and_detype_term ((lambda (v) (hydra_core_application-function v)) app)))) match_value)) (t nil))) (cadr match_target))) term)))

(defvar hydra_hoisting_update_hoist_state (lambda (accessor) (lambda (state) (let ((at_top (hydra_lib_pairs_first state))) (let ((used_app (hydra_lib_pairs_second state))) (if (hydra_lib_logic_not at_top) (list nil used_app) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :annotated_body) ((lambda (_) (list t used_app)) match_value)) ((equal (car match_target) :let_body) ((lambda (_) (list t used_app)) match_value)) ((equal (car match_target) :let_binding) ((lambda (_) (list t used_app)) match_value)) ((equal (car match_target) :lambda_body) ((lambda (_) (if used_app (list nil t) (list t nil))) match_value)) ((equal (car match_target) :union_cases_branch) ((lambda (_) (if used_app (list nil t) (list t nil))) match_value)) ((equal (car match_target) :union_cases_default) ((lambda (_) (if used_app (list nil t) (list t nil))) match_value)) ((equal (car match_target) :application_function) ((lambda (_) (if used_app (list nil t) (list t t))) match_value)) ((equal (car match_target) :application_argument) ((lambda (_) (list nil used_app)) match_value)) (t (list nil used_app)))) (cadr match_target))) accessor)))))))

(defvar hydra_hoisting_should_hoist_case_statement (lambda (path_and_term) (let ((path (hydra_lib_pairs_first path_and_term))) (let ((term (hydra_lib_pairs_second path_and_term))) (if (hydra_lib_logic_not ((hydra_lib_logic_or (hydra_hoisting_is_union_elimination term)) (hydra_hoisting_is_union_elimination_application term))) nil (let ((final_state (((hydra_lib_lists_foldl (lambda (st) (lambda (acc) ((hydra_hoisting_update_hoist_state acc) st)))) (list t nil)) path))) (hydra_lib_logic_not (hydra_lib_pairs_first final_state))))))))

(defvar hydra_hoisting_hoist_case_statements (hydra_hoisting_hoist_subterms hydra_hoisting_should_hoist_case_statement))

(defvar hydra_hoisting_hoist_case_statements_in_graph (lambda (bindings) (let ((empty_tx (make-hydra_graph_graph hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty))) (let ((term0 (list :let (make-hydra_core_let bindings (list :unit nil))))) (let ((term1 ((hydra_hoisting_hoist_case_statements empty_tx) term0))) (hydra_schemas_term_as_bindings term1))))))

(defvar hydra_hoisting_should_hoist_polymorphic (lambda (cx) (lambda (binding) ((hydra_lib_logic_or (hydra_hoisting_binding_is_polymorphic binding)) ((hydra_hoisting_binding_uses_context_type_vars cx) binding)))))

(defvar hydra_hoisting_hoist_let_bindings_with_context (lambda (is_parent_binding) (lambda (cx) (lambda (let0) ((((hydra_hoisting_hoist_let_bindings_with_predicate is_parent_binding) hydra_hoisting_should_hoist_polymorphic) cx) let0)))))

(defvar hydra_hoisting_hoist_polymorphic_let_bindings (lambda (is_parent_binding) (lambda (let0) (let ((empty_cx (make-hydra_graph_graph hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_maps_empty hydra_lib_sets_empty))) ((((hydra_hoisting_hoist_let_bindings_with_predicate is_parent_binding) hydra_hoisting_should_hoist_polymorphic) empty_cx) let0)))))

(defvar hydra_hoisting_is_application_function (lambda (acc) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :application_function) ((lambda (_) t) match_value)) (t nil))) (cadr match_target))) acc)))

(defvar hydra_hoisting_is_lambda_body (lambda (acc) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :lambda_body) ((lambda (_) t) match_value)) (t nil))) (cadr match_target))) acc)))

(defvar hydra_hoisting_normalize_path_for_hoisting (lambda (path) (letrec ((go_ (lambda (remaining) (if ((hydra_lib_logic_or (hydra_lib_lists_null remaining)) (hydra_lib_lists_null (hydra_lib_lists_tail remaining))) remaining (let ((first (hydra_lib_lists_head remaining))) (let ((second (hydra_lib_lists_head (hydra_lib_lists_tail remaining)))) (let ((rest (hydra_lib_lists_tail (hydra_lib_lists_tail remaining)))) (if ((hydra_lib_logic_and (hydra_hoisting_is_application_function first)) (hydra_hoisting_is_lambda_body second)) ((hydra_lib_lists_cons (list :let_body nil)) (go_ rest)) ((hydra_lib_lists_cons first) (go_ (hydra_lib_lists_tail remaining))))))))))) (go_ path))))

(defvar hydra_hoisting_rewrite_term_with_type_context (lambda (f) (lambda (cx0) (lambda (term0) (let ((f2 (lambda (recurse) (lambda (cx) (lambda (term) (let ((recurse1 (lambda (term) ((recurse cx) term)))) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :function) ((lambda (fun) ((lambda (match_target) ((lambda (match_value) (cond ((equal (car match_target) :lambda) ((lambda (l) (let ((cx1 ((hydra_schemas_extend_graph_for_lambda cx) l))) (let ((recurse2 (lambda (term) ((recurse cx1) term)))) (((f recurse2) cx1) term)))) match_value)) (t (((f recurse1) cx) term)))) (cadr match_target))) fun)) match_value)) ((equal (car match_target) :let) ((lambda (l) (let ((cx1 (((hydra_schemas_extend_graph_for_let (lambda (_) (lambda (_) nil))) cx) l))) (let ((recurse2 (lambda (term) ((recurse cx1) term)))) (((f recurse2) cx1) term)))) match_value)) ((equal (car match_target) :type_lambda) ((lambda (tl) (let ((cx1 ((hydra_schemas_extend_graph_for_type_lambda cx) tl))) (let ((recurse2 (lambda (term) ((recurse cx1) term)))) (((f recurse2) cx1) term)))) match_value)) (t (((f recurse1) cx) term)))) (cadr match_target))) term))))))) (letrec ((rewrite (lambda (cx) (lambda (term) (((f2 rewrite) cx) term))))) ((rewrite cx0) term0)))))))

(provide 'hydra.hoisting)
