(require 'cl-lib)

(require 'hydra.coders)

(require 'hydra.context)

(require 'hydra.core)

(require 'hydra.error)

(require 'hydra.lib.eithers)

(require 'hydra.lib.equality)

(require 'hydra.lib.lists)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.maybes)

(require 'hydra.lib.strings)

(require 'hydra.rewriting)

(require 'hydra.show.core)

(require 'hydra.substitution)

(require 'hydra.typing)

(defvar hydra_unification_join_types (lambda (cx) (lambda (left) (lambda (right) (lambda (comment) (let ((sleft (hydra_rewriting_deannotate_type left))) (let ((sright (hydra_rewriting_deannotate_type right))) (let ((join_one (lambda (l) (lambda (r) (make-hydra_typing_type_constraint l r (funcall (hydra_lib_strings_cat2 "join types; ") comment)))))) (let ((cannot_unify (list :left (make-hydra_context_in_context (make-hydra_error_unification_error sleft sright (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 "cannot unify ") (hydra_show_core_type sleft))) " with ")) (hydra_show_core_type sright))) cx)))) (let ((assert_equal (if (funcall (hydra_lib_equality_equal sleft) sright) (list :right (list)) cannot_unify))) (let ((join_list (lambda (lefts) (lambda (rights) (if (funcall (hydra_lib_equality_equal (hydra_lib_lists_length lefts)) (hydra_lib_lists_length rights)) (list :right (funcall (funcall (hydra_lib_lists_zip_with join_one) lefts) rights)) cannot_unify))))) (let ((join_row_types (lambda (left) (lambda (right) (if (funcall (hydra_lib_logic_and (funcall (hydra_lib_equality_equal (hydra_lib_lists_length (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-name v))) left))) (hydra_lib_lists_length (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-name v))) right)))) (funcall (funcall (hydra_lib_lists_foldl hydra_lib_logic_and) t) (funcall (funcall (hydra_lib_lists_zip_with (lambda (left) (lambda (right) (funcall (hydra_lib_equality_equal (funcall (lambda (v) v) left)) (funcall (lambda (v) v) right))))) (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-name v))) left)) (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-name v))) right)))) (funcall (join_list (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-type v))) left)) (funcall (hydra_lib_lists_map (lambda (v) (hydra_core_field_type-type v))) right)) cannot_unify))))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (r) (list :right (list (funcall (join_one (funcall (lambda (v) (hydra_core_application_type-function v)) l)) (funcall (lambda (v) (hydra_core_application_type-function v)) r)) (funcall (join_one (funcall (lambda (v) (hydra_core_application_type-argument v)) l)) (funcall (lambda (v) (hydra_core_application_type-argument v)) r))))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :either) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :either) (funcall (lambda (r) (list :right (list (funcall (join_one (funcall (lambda (v) (hydra_core_either_type-left v)) l)) (funcall (lambda (v) (hydra_core_either_type-left v)) r)) (funcall (join_one (funcall (lambda (v) (hydra_core_either_type-right v)) l)) (funcall (lambda (v) (hydra_core_either_type-right v)) r))))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :function) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :function) (funcall (lambda (r) (list :right (list (funcall (join_one (funcall (lambda (v) (hydra_core_function_type-domain v)) l)) (funcall (lambda (v) (hydra_core_function_type-domain v)) r)) (funcall (join_one (funcall (lambda (v) (hydra_core_function_type-codomain v)) l)) (funcall (lambda (v) (hydra_core_function_type-codomain v)) r))))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :list) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :list) (funcall (lambda (r) (list :right (list (funcall (join_one l) r)))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :literal) (funcall (lambda (_) assert_equal) match_value)) ((equal (car match_target) :map) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :map) (funcall (lambda (r) (list :right (list (funcall (join_one (funcall (lambda (v) (hydra_core_map_type-keys v)) l)) (funcall (lambda (v) (hydra_core_map_type-keys v)) r)) (funcall (join_one (funcall (lambda (v) (hydra_core_map_type-values v)) l)) (funcall (lambda (v) (hydra_core_map_type-values v)) r))))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :maybe) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :maybe) (funcall (lambda (r) (list :right (list (funcall (join_one l) r)))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :pair) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :pair) (funcall (lambda (r) (list :right (list (funcall (join_one (funcall (lambda (v) (hydra_core_pair_type-first v)) l)) (funcall (lambda (v) (hydra_core_pair_type-first v)) r)) (funcall (join_one (funcall (lambda (v) (hydra_core_pair_type-second v)) l)) (funcall (lambda (v) (hydra_core_pair_type-second v)) r))))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :record) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :record) (funcall (lambda (r) (funcall (join_row_types l) r)) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :set) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :set) (funcall (lambda (r) (list :right (list (funcall (join_one l) r)))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :union) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :union) (funcall (lambda (r) (funcall (join_row_types l) r)) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :unit) (funcall (lambda (_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :unit) (funcall (lambda (_) (list :right (list))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (l) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :wrap) (funcall (lambda (r) (list :right (list (funcall (join_one l) r)))) match_value)) (t cannot_unify))) (cadr match_target))) sright)) match_value)) (t cannot_unify))) (cadr match_target))) sleft)))))))))))))

(defvar hydra_unification_variable_occurs_in_type (lambda (var_) (lambda (typ0) (let ((try_type (lambda (b) (lambda (typ) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (v) (funcall (hydra_lib_logic_or b) (funcall (hydra_lib_equality_equal (funcall (lambda (v) v) v)) (funcall (lambda (v) v) var_)))) match_value)) (t b))) (cadr match_target))) typ))))) (funcall (funcall (funcall (hydra_rewriting_fold_over_type (list :pre nil)) try_type) nil) typ0)))))

(defvar hydra_unification_unify_type_constraints (lambda (cx) (lambda (schema_types) (lambda (constraints) (let ((with_constraint (lambda (c) (lambda (rest) (let ((sleft (hydra_rewriting_deannotate_type (funcall (lambda (v) (hydra_typing_type_constraint-left v)) c)))) (let ((sright (hydra_rewriting_deannotate_type (funcall (lambda (v) (hydra_typing_type_constraint-right v)) c)))) (let ((comment (funcall (lambda (v) (hydra_typing_type_constraint-comment v)) c))) (let ((bind (lambda (v) (lambda (t_) (let ((subst (funcall (hydra_substitution_singleton_type_subst v) t_))) (let ((with_result (lambda (s) (funcall (hydra_substitution_compose_type_subst subst) s)))) (funcall (hydra_lib_eithers_map with_result) (funcall (funcall (hydra_unification_unify_type_constraints cx) schema_types) (funcall (hydra_substitution_substitute_in_constraints subst) rest))))))))) (let ((try_binding (lambda (v) (lambda (t_) (if (funcall (hydra_unification_variable_occurs_in_type v) t_) (list :left (make-hydra_context_in_context (make-hydra_error_unification_error sleft sright (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 "Variable ") (funcall (lambda (v) v) v))) " appears free in type ")) (hydra_show_core_type t_))) " (")) comment)) ")")) cx)) (funcall (bind v) t_)))))) (let ((no_vars (let ((with_constraints (lambda (constraints2) (funcall (funcall (hydra_unification_unify_type_constraints cx) schema_types) (funcall (hydra_lib_lists_concat2 constraints2) rest))))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_unification_join_types cx) sleft) sright) comment)) with_constraints)))) (let ((dflt (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (name) (funcall (try_binding name) sleft)) match_value)) (t no_vars))) (cadr match_target))) sright))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (name) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (name2) (if (funcall (hydra_lib_equality_equal (funcall (lambda (v) v) name)) (funcall (lambda (v) v) name2)) (funcall (funcall (hydra_unification_unify_type_constraints cx) schema_types) rest) (if (hydra_lib_maybes_is_just (funcall (hydra_lib_maps_lookup name) schema_types)) (if (hydra_lib_maybes_is_just (funcall (hydra_lib_maps_lookup name2) schema_types)) (list :left (make-hydra_context_in_context (make-hydra_error_unification_error sleft sright (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 "Attempted to unify schema names ") (funcall (lambda (v) v) name))) " and ")) (funcall (lambda (v) v) name2))) " (")) comment)) ")")) cx)) (funcall (bind name2) sleft)) (funcall (bind name) sright)))) match_value)) (t (funcall (try_binding name) sright)))) (cadr match_target))) sright)) match_value)) (t dflt))) (cadr match_target))) sleft)))))))))))) (if (hydra_lib_lists_null constraints) (list :right hydra_substitution_id_type_subst) (funcall (with_constraint (hydra_lib_lists_head constraints)) (hydra_lib_lists_tail constraints))))))))

(defvar hydra_unification_unify_type_lists (lambda (cx) (lambda (schema_types) (lambda (l) (lambda (r) (lambda (comment) (let ((to_constraint (lambda (l) (lambda (r) (make-hydra_typing_type_constraint l r comment))))) (funcall (funcall (hydra_unification_unify_type_constraints cx) schema_types) (funcall (funcall (hydra_lib_lists_zip_with to_constraint) l) r)))))))))

(defvar hydra_unification_unify_types (lambda (cx) (lambda (schema_types) (lambda (l) (lambda (r) (lambda (comment) (funcall (funcall (hydra_unification_unify_type_constraints cx) schema_types) (list (make-hydra_typing_type_constraint l r comment)))))))))

(provide 'hydra.unification)
