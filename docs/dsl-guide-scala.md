# Hydra DSL Guide (Scala)

This guide explains Hydra's domain-specific language (DSL) utilities for constructing types and terms in Scala.

**Note**: Hydra provides DSLs in all five implementation languages (Haskell, Java, Python, Scala, and Lisp).
This guide focuses on the Scala DSLs.
For the comprehensive Haskell DSL guide (including kernel development context),
see [DSL Guide (Haskell)](dsl-guide.md).
For the Java DSLs, see [DSL Guide (Java)](dsl-guide-java.md).
For the Python DSLs, see [DSL Guide (Python)](dsl-guide-python.md).

The Scala DSL was introduced in 0.17 (#509) so that the `hydra-scala` coder package
could be authored natively in Scala instead of via a Haskell DSL,
parallelling the Java (#346) and Python (#346) migrations.
The legacy Haskell DSL copy under `packages/hydra-scala/src/main/haskell/` has been deleted.

## Prerequisites

**Before using the DSL utilities**, you should:
- Understand Hydra's core concepts: [Concepts](https://github.com/CategoricalData/hydra/wiki/Concepts)
- Know basic Scala 3 syntax
- Have set up Hydra-Scala locally
  (see [Hydra-Scala README](https://github.com/CategoricalData/hydra/tree/main/packages/hydra-scala))
- Scala 3.3+ required (the kernel uses Scala 3 `match` syntax, opaque type aliases, and `inline def`)

## Layout

Scala DSL utilities live in two trees:

- **`overlay/scala/hydra-kernel/src/main/scala/hydra/overlay/scala/dsl/`** — the hand-written
  DSL infrastructure (term/type constructors, phantom-typed helpers, library wrappers,
  Helpers utilities). Copied into `dist/scala/hydra-kernel/` by the assembler and consumed
  by every Scala DSL-authoring file.
- **`packages/hydra-scala/src/main/scala/hydra/sources/scala/`** — the `hydra-scala` coder
  DSL sources themselves (Syntax.scala, Language.scala, Utils.scala, Serde.scala, Coder.scala,
  Manifest.scala). These are the canonical example of a non-trivial Scala-authored Hydra
  package; consult them for patterns.

The driver `heads/scala/src/main/scala/hydra/UpdateScalaJson.scala` loads these sources
plus the kernel universe, runs Hydra type inference over each module, and writes
`dist/json/hydra-scala/`. The convenience wrapper is `bin/generate-hydra-scala-from-scala.sh`;
the main `bin/sync.sh` invokes it in Phase 5.

## DSL variants

Mirrors the Java/Python layering:

| Layer | Module | Purpose |
|-------|--------|---------|
| **Direct DSLs** | `hydra.overlay.scala.dsl.{Terms, Types}` | Raw construction of `Term` and `Type` instances |
| **Phantom-typed DSL** | `hydra.overlay.scala.dsl.Phantoms` | Type safety via `TypedTerm[A]` phantom alias (`TypedTerm[A] = Term`) |
| **Domain-specific helpers** | `hydra.overlay.scala.dsl.Helpers` | Typed accessors (`typeref`, `typeDef`, `termDef`, `unqualifiedDep`) |
| **Generated DSL wrappers** | `hydra.dsl.<x>.*` (under `dist/scala/`) | Typed wrappers around every kernel/package primitive (lists, sets, maps, core, graph, ...) |

Domain-specific helpers and library wrappers are generated by the same sync pipeline that
generates the rest of the kernel — they are pure consumers of the kernel module definitions
in `dist/json/`.

## Conventions

- **Imports**: every Scala DSL file imports `hydra.overlay.scala.dsl.Phantoms.{...}`
  selectively (renaming `var` to `v` since `var` is a Scala reserved word) and brings in
  `applyP`, `applyN`, `lambda`, `let`, `field`, `string`, `int32`, `bool`, `list`, `nothing`,
  `just`, `doc`, `constant`, `cases`, `casesWithDefault`, `project`, `unwrap`, `wrap`,
  `inject`.
- **`var` reference**: use the imported alias `v("name")` to construct a `TypedTerm` variable
  reference. Equivalent to the Haskell `var "name"`.
- **Function application**: use `applyP("hydra.lib.foo.bar", arg1, arg2, …)` for primitive
  application (eta-expanded by the Scala codegen). For curried application of a `TypedTerm`
  function value, use `applyN(fn, arg1, arg2)`.
- **Let bindings**: prefer the multi-binding form
  `let(Seq(field("a", expr1), field("b", expr2), …), body)`. Hydra's let is letrec by default,
  so a binding's value can reference its own name and its siblings'.
- **Recursive helpers**: bind via `field("collectFoo", lambda("t", lambda("acc", …)))` and
  call them with `applyN(v("collectFoo"), …)`. The recursive call goes through `v(name)`
  rather than the Scala `private val` so the Hydra inference engine sees a single
  let-bound recursive function.
- **Pattern matching on a union**: use `casesWithDefault("hydra.core.X", arg, defaultBranch,
  field("variantA", lambda("v", …)), field("variantB", lambda("v", …)), …)`. Field ordering
  is preserved into the generated Scala source's `match` cases — match the Haskell DSL's
  order to keep generated output structurally aligned.

## Inferring opaque `TypedTerm[Term]` values

The phantom type alias is `type TypedTerm[A] = hydra.core.Term`. Some kernel APIs expect a
`TypedTerm[Term]` (an opaque term value), but `Phantoms.bool(true)` returns
`TypedTerm[Boolean]` — which the Hydra inference engine refines to `boolean` and refuses to
unify with `Term`. Use `Phantoms.metaBool(b)` instead: it constructs the meta-encoded
`Inject(Term, "literal", Inject(Literal, "boolean", boolean(b)))` whose outer typeName is
`hydra.core.Term`, matching the Haskell DSL's `MetaTerms.true`.

## Generation pipeline

1. Edit the Scala source under `packages/hydra-scala/src/main/scala/hydra/sources/scala/`.
2. Run `bin/generate-hydra-scala-from-scala.sh` (or just `bin/sync.sh --hosts scala` —
   Phase 5 invokes the same driver). The driver compiles the package, runs inference,
   and writes the new JSON to `dist/json/hydra-scala/src/main/json/`.
3. `dist/scala/hydra-scala/src/main/scala/hydra/scala/*.scala` is regenerated downstream
   from that JSON by `heads/scala/bin/assemble-distribution.sh hydra-scala`.

The driver currently runs only against the local sbt build — there is no published-host probe
yet (compare hydra-java/hydra-python which default to the published host with a `--local-host`
escape).

## See also

- [DSL Guide (Haskell)](dsl-guide.md) — the comprehensive cross-language DSL reference.
- [DSL Guide (Java)](dsl-guide-java.md) — parallel guide for the Java DSL.
- [DSL Guide (Python)](dsl-guide-python.md) — parallel guide for the Python DSL.
- [hydra-lexicon.txt](hydra-lexicon.txt) — the kernel API surface (types + primitive signatures).
- The Scala coder sources themselves
  (`packages/hydra-scala/src/main/scala/hydra/sources/scala/`) — canonical worked examples,
  covering pattern matching, let-rec, partial application, and primitive references.
