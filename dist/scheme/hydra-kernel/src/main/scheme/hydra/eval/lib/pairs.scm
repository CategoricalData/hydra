(define-library (hydra eval lib pairs)
(export hydra_eval_lib_pairs_bimap hydra_eval_lib_pairs_first hydra_eval_lib_pairs_second)
(import (scheme base) (hydra core) (hydra errors) (hydra lib pairs) (hydra show core))
(begin
(define hydra_eval_lib_pairs_bimap (lambda (cx) (lambda (g) (lambda (first_fun) (lambda (second_fun) (lambda (pair_term) ((lambda (match_target) ((lambda (match_value) (cond ((equal? (car match_target) 'pair) ((lambda (p) (let ((fst (hydra_lib_pairs_first p))) (let ((snd (hydra_lib_pairs_second p))) (list 'right (list 'pair (list (list 'application (make-hydra_core_application first_fun fst)) (list 'application (make-hydra_core_application second_fun snd)))))))) match_value)) (else (list 'left (list 'extraction (list 'unexpected_shape (make-hydra_errors_unexpected_shape_error "pair value" (hydra_show_core_term pair_term)))))))) (cadr match_target))) pair_term)))))))
(define hydra_eval_lib_pairs_first (lambda (cx) (lambda (g) (lambda (pair_term) ((lambda (match_target) ((lambda (match_value) (cond ((equal? (car match_target) 'pair) ((lambda (p) (list 'right (hydra_lib_pairs_first p))) match_value)) (else (list 'left (list 'extraction (list 'unexpected_shape (make-hydra_errors_unexpected_shape_error "pair value" (hydra_show_core_term pair_term)))))))) (cadr match_target))) pair_term)))))
(define hydra_eval_lib_pairs_second (lambda (cx) (lambda (g) (lambda (pair_term) ((lambda (match_target) ((lambda (match_value) (cond ((equal? (car match_target) 'pair) ((lambda (p) (list 'right (hydra_lib_pairs_second p))) match_value)) (else (list 'left (list 'extraction (list 'unexpected_shape (make-hydra_errors_unexpected_shape_error "pair value" (hydra_show_core_term pair_term)))))))) (cadr match_target))) pair_term)))))))
