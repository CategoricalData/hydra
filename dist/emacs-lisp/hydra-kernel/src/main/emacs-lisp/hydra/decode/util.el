(require 'cl-lib)

(require 'hydra.core)

(require 'hydra.errors)

(require 'hydra.extract.core)

(require 'hydra.lib.eithers)

(require 'hydra.lib.maps)

(require 'hydra.lib.maybes)

(require 'hydra.lib.strings)

(require 'hydra.util)

(defvar hydra_decode_util_case_convention (lambda (cx) (lambda (raw) (funcall (funcall (hydra_lib_eithers_either (lambda (err) (list :left err))) (lambda (stripped) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :inject) (funcall (lambda (inj) (let* ((field (funcall (lambda (v) (hydra_core_injection-field v)) inj)) (fname (funcall (lambda (v) (hydra_core_field-name v)) field)) (fterm (funcall (lambda (v) (hydra_core_field-term v)) field)) (variant_map (hydra_lib_maps_from_list (list (list "camel" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :camel t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "pascal" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :pascal t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "lowerSnake" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :lower_snake t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "upperSnake" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :upper_snake t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))))))) (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list :left (hydra_lib_strings_cat (list "no such field " (funcall (lambda (v) v) fname) " in union"))))) (lambda (f) (f fterm))) (funcall (hydra_lib_maps_lookup fname) variant_map)))) match_value)) (t (list :left "expected union")))) (cadr match_target))) stripped))) (funcall (hydra_extract_core_strip_with_decoding_error cx) raw)))))

(defvar hydra_decode_util_comparison (lambda (cx) (lambda (raw) (funcall (funcall (hydra_lib_eithers_either (lambda (err) (list :left err))) (lambda (stripped) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :inject) (funcall (lambda (inj) (let* ((field (funcall (lambda (v) (hydra_core_injection-field v)) inj)) (fname (funcall (lambda (v) (hydra_core_field-name v)) field)) (fterm (funcall (lambda (v) (hydra_core_field-term v)) field)) (variant_map (hydra_lib_maps_from_list (list (list "lessThan" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :less_than t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "equalTo" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :equal_to t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "greaterThan" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :greater_than t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))))))) (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list :left (hydra_lib_strings_cat (list "no such field " (funcall (lambda (v) v) fname) " in union"))))) (lambda (f) (f fterm))) (funcall (hydra_lib_maps_lookup fname) variant_map)))) match_value)) (t (list :left "expected union")))) (cadr match_target))) stripped))) (funcall (hydra_extract_core_strip_with_decoding_error cx) raw)))))

(defvar hydra_decode_util_precision (lambda (cx) (lambda (raw) (funcall (funcall (hydra_lib_eithers_either (lambda (err) (list :left err))) (lambda (stripped) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :inject) (funcall (lambda (inj) (let* ((field (funcall (lambda (v) (hydra_core_injection-field v)) inj)) (fname (funcall (lambda (v) (hydra_core_field-name v)) field)) (fterm (funcall (lambda (v) (hydra_core_field-term v)) field)) (variant_map (hydra_lib_maps_from_list (list (list "arbitrary" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :arbitrary t_))) (funcall (funcall (lambda (cx2) (lambda (t_) (funcall (hydra_extract_core_decode_unit cx2) t_))) cx) input)))) (list "bits" (lambda (input) (funcall (hydra_lib_eithers_map (lambda (t_) (list :bits t_))) (funcall (funcall (lambda (cx2) (lambda (raw2) (funcall (funcall (hydra_lib_eithers_either (lambda (err) (list :left err))) (lambda (stripped2) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :literal) (funcall (lambda (v) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :integer) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :int32) (funcall (lambda (i) (list :right i)) match_value)) (t (list :left "expected int32 value")))) (cadr match_target))) match_value)) (t (list :left "expected int32 literal")))) (cadr match_target))) v)) match_value)) (t (list :left "expected literal")))) (cadr match_target))) stripped2))) (funcall (hydra_extract_core_strip_with_decoding_error cx2) raw2)))) cx) input)))))))) (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list :left (hydra_lib_strings_cat (list "no such field " (funcall (lambda (v) v) fname) " in union"))))) (lambda (f) (f fterm))) (funcall (hydra_lib_maps_lookup fname) variant_map)))) match_value)) (t (list :left "expected union")))) (cadr match_target))) stripped))) (funcall (hydra_extract_core_strip_with_decoding_error cx) raw)))))

(provide 'hydra.decode.util)
