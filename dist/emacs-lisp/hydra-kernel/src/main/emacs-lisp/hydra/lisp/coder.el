(require 'cl-lib)

(require 'hydra.analysis)

(require 'hydra.core)

(require 'hydra.environment)

(require 'hydra.formatting)

(require 'hydra.lib.eithers)

(require 'hydra.lib.equality)

(require 'hydra.lib.lists)

(require 'hydra.lib.literals)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.maybes)

(require 'hydra.lib.pairs)

(require 'hydra.lib.sets)

(require 'hydra.lib.strings)

(require 'hydra.lisp.language)

(require 'hydra.lisp.syntax)

(require 'hydra.names)

(require 'hydra.packaging)

(require 'hydra.predicates)

(require 'hydra.show.core)

(require 'hydra.sorting)

(require 'hydra.strip)

(require 'hydra.variables)

(defvar hydra_lisp_coder_dialect_cadr (lambda (d) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) "second") match_value)) (t "cadr"))) (cadr match_target))) d)))

(defvar hydra_lisp_coder_dialect_car (lambda (d) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) "first") match_value)) (t "car"))) (cadr match_target))) d)))

(defvar hydra_lisp_coder_dialect_constructor_prefix (lambda (d) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) "->") match_value)) (t "make-"))) (cadr match_target))) d)))

(defvar hydra_lisp_coder_dialect_equal (lambda (d) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) "=") match_value)) ((equal (car match_target) :common_lisp) (funcall (lambda (_) "equal") match_value)) ((equal (car match_target) :emacs_lisp) (funcall (lambda (_) "equal") match_value)) (t "equal?"))) (cadr match_target))) d)))

(defvar hydra_lisp_coder_lisp_lambda_expr (lambda (params) (lambda (body) (list :lambda (make-hydra_lisp_syntax_lambda (list :nothing) (funcall (hydra_lib_lists_map (lambda (p) p)) params) (list :nothing) (list body))))))

(defvar hydra_lisp_coder_lisp_app (lambda (fun) (lambda (args) (list :application (make-hydra_lisp_syntax_application fun args)))))

(defvar hydra_lisp_coder_lisp_named_lambda_expr (lambda (name) (lambda (params) (lambda (body) (list :lambda (make-hydra_lisp_syntax_lambda (list :just name) (funcall (hydra_lib_lists_map (lambda (p) p)) params) (list :nothing) (list body)))))))

(defvar hydra_lisp_coder_lisp_var (lambda (name) (list :variable (make-hydra_lisp_syntax_variable_reference name nil))))

(defvar hydra_lisp_coder_encode_literal (lambda (lit) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :boolean) (funcall (lambda (b) (list :literal (list :boolean b))) match_value)) ((equal (car match_target) :string) (funcall (lambda (s) (list :literal (list :string s))) match_value)) ((equal (car match_target) :float) (funcall (lambda (fv) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :float32) (funcall (lambda (f) (list :literal (list :float (make-hydra_lisp_syntax_float_literal (hydra_lib_literals_float32_to_bigfloat f) (list :nothing))))) match_value)) ((equal (car match_target) :float64) (funcall (lambda (f) (list :literal (list :float (make-hydra_lisp_syntax_float_literal (hydra_lib_literals_float64_to_bigfloat f) (list :nothing))))) match_value)) ((equal (car match_target) :bigfloat) (funcall (lambda (f) (list :literal (list :float (make-hydra_lisp_syntax_float_literal f (list :nothing))))) match_value)))) (cadr match_target))) fv)) match_value)) ((equal (car match_target) :integer) (funcall (lambda (iv) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :int8) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_int8_to_bigint i) nil)))) match_value)) ((equal (car match_target) :int16) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_int16_to_bigint i) nil)))) match_value)) ((equal (car match_target) :int32) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_int32_to_bigint i) nil)))) match_value)) ((equal (car match_target) :int64) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_int64_to_bigint i) nil)))) match_value)) ((equal (car match_target) :uint8) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_uint8_to_bigint i) nil)))) match_value)) ((equal (car match_target) :uint16) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_uint16_to_bigint i) nil)))) match_value)) ((equal (car match_target) :uint32) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_uint32_to_bigint i) nil)))) match_value)) ((equal (car match_target) :uint64) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_uint64_to_bigint i) nil)))) match_value)) ((equal (car match_target) :bigint) (funcall (lambda (i) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal i t)))) match_value)))) (cadr match_target))) iv)) match_value)) ((equal (car match_target) :binary) (funcall (lambda (b) (let ((byte_values (hydra_lib_literals_binary_to_bytes b))) (list :vector (make-hydra_lisp_syntax_vector_literal (funcall (hydra_lib_lists_map (lambda (bv) (list :literal (list :integer (make-hydra_lisp_syntax_integer_literal (hydra_lib_literals_int32_to_bigint bv) nil))))) byte_values))))) match_value)))) (cadr match_target))) lit)))

(defvar hydra_lisp_coder_qualified_snake_name (lambda (name) (let ((raw (funcall (lambda (v) v) name))) (let ((parts (funcall (hydra_lib_strings_split_on ".") raw))) (let ((snake_parts (funcall (hydra_lib_lists_map (lambda (p) (hydra_formatting_convert_case_camel_or_underscore_to_lower_snake p))) parts))) (let ((joined (funcall (hydra_lib_strings_intercalate "_") snake_parts))) (funcall (hydra_formatting_sanitize_with_underscores hydra_lisp_language_lisp_reserved_words) joined)))))))

(defvar hydra_lisp_coder_lisp_keyword (lambda (name) (list :literal (list :keyword (make-hydra_lisp_syntax_keyword name (list :nothing))))))

(defvar hydra_lisp_coder_lisp_list_expr (lambda (elements) (list :list (make-hydra_lisp_syntax_list_literal elements nil))))

(defvar hydra_lisp_coder_lisp_nil_expr (list :literal (list :nil nil)))

(defvar hydra_lisp_coder_is_primitive_ref (lambda (prim_name) (lambda (term) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (name) (funcall (hydra_lib_equality_equal (funcall (lambda (v) v) name)) prim_name)) match_value)) ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (hydra_lisp_coder_is_primitive_ref prim_name) (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (funcall (hydra_lisp_coder_is_primitive_ref prim_name) (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (funcall (hydra_lisp_coder_is_primitive_ref prim_name) (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) match_value)) (t nil))) (cadr match_target))) term))))

(defvar hydra_lisp_coder_wrap_in_thunk (lambda (expr) (list :lambda (make-hydra_lisp_syntax_lambda (list :nothing) (list) (list :nothing) (list expr)))))

(defvar hydra_lisp_coder_encode_application (lambda (dialect) (lambda (cx) (lambda (g) (lambda (raw_fun) (lambda (raw_arg) (let ((d_fun (hydra_strip_deannotate_term raw_fun))) (let ((normal (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) raw_fun)) (lambda (fun) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) raw_arg)) (lambda (arg) (list :right (funcall (hydra_lisp_coder_lisp_app fun) (list arg))))))))) (let ((enc (lambda (t_) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) t_)))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app2) (let ((mid_fun (funcall (lambda (v) (hydra_core_application-function v)) app2))) (let ((mid_arg (funcall (lambda (v) (hydra_core_application-argument v)) app2))) (let ((d_mid_fun (hydra_strip_deannotate_term mid_fun))) (let ((is_lazy2 (funcall (hydra_lib_logic_or (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.eithers.fromLeft") d_mid_fun)) (funcall (hydra_lib_logic_or (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.eithers.fromRight") d_mid_fun)) (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.maybes.fromMaybe") d_mid_fun))))) (if is_lazy2 (funcall (hydra_lib_eithers_bind (enc mid_fun)) (lambda (e_prim) (funcall (hydra_lib_eithers_bind (enc mid_arg)) (lambda (e_def) (funcall (hydra_lib_eithers_bind (enc raw_arg)) (lambda (e_arg) (list :right (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_app e_prim) (list (hydra_lisp_coder_wrap_in_thunk e_def)))) (list e_arg))))))))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :application) (funcall (lambda (app3) (let ((inner_fun (funcall (lambda (v) (hydra_core_application-function v)) app3))) (let ((inner_arg (funcall (lambda (v) (hydra_core_application-argument v)) app3))) (let ((d_inner_fun (hydra_strip_deannotate_term inner_fun))) (if (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.logic.ifElse") d_inner_fun) (funcall (hydra_lib_eithers_bind (enc inner_arg)) (lambda (e_c) (funcall (hydra_lib_eithers_bind (enc mid_arg)) (lambda (e_t) (funcall (hydra_lib_eithers_bind (enc raw_arg)) (lambda (e_e) (list :right (list :if (make-hydra_lisp_syntax_if_expression e_c e_t (list :just e_e)))))))))) (if (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.maybes.maybe") d_inner_fun) (funcall (hydra_lib_eithers_bind (enc inner_fun)) (lambda (e_p) (funcall (hydra_lib_eithers_bind (enc inner_arg)) (lambda (e_def) (funcall (hydra_lib_eithers_bind (enc mid_arg)) (lambda (e_f) (funcall (hydra_lib_eithers_bind (enc raw_arg)) (lambda (e_m) (list :right (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_app e_p) (list (hydra_lisp_coder_wrap_in_thunk e_def)))) (list e_f))) (list e_m))))))))))) (if (funcall (hydra_lisp_coder_is_primitive_ref "hydra.lib.maybes.cases") d_inner_fun) (funcall (hydra_lib_eithers_bind (enc inner_fun)) (lambda (e_p) (funcall (hydra_lib_eithers_bind (enc inner_arg)) (lambda (e_m) (funcall (hydra_lib_eithers_bind (enc mid_arg)) (lambda (e_n) (funcall (hydra_lib_eithers_bind (enc raw_arg)) (lambda (e_j) (list :right (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_app e_p) (list e_m))) (list (hydra_lisp_coder_wrap_in_thunk e_n)))) (list e_j))))))))))) normal))))))) match_value)) (t normal))) (cadr match_target))) d_mid_fun))))))) match_value)) (t normal))) (cadr match_target))) d_fun))))))))))

(defvar hydra_lisp_coder_encode_lambda_term (lambda (dialect) (lambda (cx) (lambda (g) (lambda (lam) (let ((param (hydra_formatting_convert_case_camel_or_underscore_to_lower_snake (funcall (hydra_formatting_sanitize_with_underscores hydra_lisp_language_lisp_reserved_words) (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_lambda-parameter v)) lam)))))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_lambda-body v)) lam))) (lambda (body) (list :right (funcall (hydra_lisp_coder_lisp_lambda_expr (list param)) body))))))))))

(defvar hydra_lisp_coder_encode_let_as_native (lambda (dialect) (lambda (cx) (lambda (g) (lambda (bindings) (lambda (body) (let ((is_clojure_top (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) t) match_value)) (t nil))) (cadr match_target))) dialect))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) body)) (lambda (body_expr) (let ((sorted_bindings (if t (let* ((all_names (hydra_lib_sets_from_list (funcall (hydra_lib_lists_map (lambda (b) (funcall (lambda (v) (hydra_core_binding-name v)) b))) bindings))) (adj_list (funcall (hydra_lib_lists_map (lambda (b) (list (funcall (lambda (v) (hydra_core_binding-name v)) b) (hydra_lib_sets_to_list (funcall (hydra_lib_sets_intersection all_names) (hydra_variables_free_variables_in_term (funcall (lambda (v) (hydra_core_binding-term v)) b))))))) bindings)) (name_to_binding (hydra_lib_maps_from_list (funcall (hydra_lib_lists_map (lambda (b) (list (funcall (lambda (v) (hydra_core_binding-name v)) b) b))) bindings))) (sort_result (hydra_sorting_topological_sort adj_list))) (funcall (funcall (hydra_lib_eithers_either (lambda (_) bindings)) (lambda (sorted) (funcall (hydra_lib_lists_map (lambda (name) (funcall (hydra_lib_maybes_from_maybe (lambda () (hydra_lib_lists_head bindings))) (funcall (hydra_lib_maps_lookup name) name_to_binding)))) sorted))) sort_result)) bindings))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (lambda (b) (let ((bname (hydra_formatting_convert_case_camel_or_underscore_to_lower_snake (funcall (hydra_formatting_sanitize_with_underscores hydra_lisp_language_lisp_reserved_words) (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_binding-name v)) b)))))) (let ((is_self_ref (funcall (hydra_lib_sets_member (funcall (lambda (v) (hydra_core_binding-name v)) b)) (hydra_variables_free_variables_in_term (funcall (lambda (v) (hydra_core_binding-term v)) b))))) (let ((is_lambda (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (_) t) match_value)) (t nil))) (cadr match_target))) (hydra_strip_deannotate_term (funcall (lambda (v) (hydra_core_binding-term v)) b))))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_binding-term v)) b))) (lambda (bval) (let ((is_clojure (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :clojure) (funcall (lambda (_) t) match_value)) (t nil))) (cadr match_target))) dialect))) (let ((wrapped_val (if is_clojure (if is_self_ref (if is_lambda (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (lam) (list :lambda (make-hydra_lisp_syntax_lambda (list :just bname) (funcall (lambda (v) (hydra_lisp_syntax_lambda-params v)) lam) (funcall (lambda (v) (hydra_lisp_syntax_lambda-rest_param v)) lam) (funcall (lambda (v) (hydra_lisp_syntax_lambda-body v)) lam)))) match_value)) (t bval))) (cadr match_target))) bval) (funcall (funcall (hydra_lisp_coder_lisp_named_lambda_expr bname) (list "_arg")) (funcall (hydra_lisp_coder_lisp_app bval) (list (hydra_lisp_coder_lisp_var "_arg"))))) bval) (if (funcall (hydra_lib_logic_and is_self_ref) (hydra_lib_logic_not is_lambda)) (funcall (hydra_lisp_coder_lisp_lambda_expr (list "_arg")) (funcall (hydra_lisp_coder_lisp_app bval) (list (hydra_lisp_coder_lisp_var "_arg")))) bval)))) (list :right (list bname wrapped_val))))))))))) sorted_bindings)) (lambda (encoded_bindings) (let ((all_binding_names (hydra_lib_sets_from_list (funcall (hydra_lib_lists_map (lambda (b) (funcall (lambda (v) (hydra_core_binding-name v)) b))) bindings)))) (let ((has_cross_refs (funcall (funcall (hydra_lib_lists_foldl (lambda (acc) (lambda (b) (funcall (hydra_lib_logic_or acc) (hydra_lib_logic_not (hydra_lib_sets_null (funcall (hydra_lib_sets_intersection all_binding_names) (hydra_variables_free_variables_in_term (funcall (lambda (v) (hydra_core_binding-term v)) b))))))))) nil) bindings))) (let ((has_self_ref (funcall (funcall (hydra_lib_lists_foldl (lambda (acc) (lambda (b) (funcall (hydra_lib_logic_or acc) (funcall (hydra_lib_sets_member (funcall (lambda (v) (hydra_core_binding-name v)) b)) (hydra_variables_free_variables_in_term (funcall (lambda (v) (hydra_core_binding-term v)) b))))))) nil) bindings))) (let ((is_recursive has_self_ref)) (let ((let_kind (if is_recursive (list :recursive nil) (if (hydra_lib_lists_null (hydra_lib_lists_tail bindings)) (list :parallel nil) (list :sequential nil))))) (let ((lisp_bindings (funcall (hydra_lib_lists_map (lambda (eb) (list :simple (make-hydra_lisp_syntax_simple_binding (hydra_lib_pairs_first eb) (hydra_lib_pairs_second eb))))) encoded_bindings))) (list :right (list :let (make-hydra_lisp_syntax_let_expression let_kind lisp_bindings (list body_expr))))))))))))))))))))))

(defvar hydra_lisp_coder_encode_projection_elim (lambda (dialect) (lambda (cx) (lambda (g) (lambda (proj) (lambda (marg) (let ((fname (hydra_formatting_convert_case_camel_to_lower_snake (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_projection-field v)) proj))))) (let ((tname (hydra_lisp_coder_qualified_snake_name (funcall (lambda (v) (hydra_core_projection-type_name v)) proj)))) (funcall (funcall (hydra_lib_maybes_cases marg) (lambda () (list :right (funcall (hydra_lisp_coder_lisp_lambda_expr (list "v")) (list :field_access (make-hydra_lisp_syntax_field_access tname fname (hydra_lisp_coder_lisp_var "v"))))))) (lambda (arg) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) arg)) (lambda (sarg) (list :right (list :field_access (make-hydra_lisp_syntax_field_access tname fname sarg)))))))))))))))

(defvar hydra_lisp_coder_encode_term (lambda (dialect) (lambda (cx) (lambda (g) (lambda (term) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) match_value)) ((equal (car match_target) :application) (funcall (lambda (app) (let ((raw_fun (funcall (lambda (v) (hydra_core_application-function v)) app))) (let ((raw_arg (funcall (lambda (v) (hydra_core_application-argument v)) app))) (funcall (funcall (funcall (funcall (hydra_lisp_coder_encode_application dialect) cx) g) raw_fun) raw_arg)))) match_value)) ((equal (car match_target) :either) (funcall (lambda (e) (funcall (funcall (hydra_lib_eithers_either (lambda (l) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) l)) (lambda (sl) (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword "left") sl))))))) (lambda (r) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) r)) (lambda (sr) (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword "right") sr))))))) e)) match_value)) ((equal (car match_target) :lambda) (funcall (lambda (lam) (funcall (funcall (funcall (hydra_lisp_coder_encode_lambda_term dialect) cx) g) lam)) match_value)) ((equal (car match_target) :project) (funcall (lambda (proj) (funcall (funcall (funcall (funcall (hydra_lisp_coder_encode_projection_elim dialect) cx) g) proj) (list :nothing))) match_value)) ((equal (car match_target) :cases) (funcall (lambda (cs) (funcall (funcall (funcall (funcall (hydra_lisp_coder_encode_union_elim dialect) cx) g) cs) (list :nothing))) match_value)) ((equal (car match_target) :unwrap) (funcall (lambda (name) (funcall (funcall (funcall (funcall (hydra_lisp_coder_encode_unwrap_elim dialect) cx) g) name) (list :nothing))) match_value)) ((equal (car match_target) :let) (funcall (lambda (lt) (let ((bindings (funcall (lambda (v) (hydra_core_let-bindings v)) lt))) (let ((body (funcall (lambda (v) (hydra_core_let-body v)) lt))) (funcall (funcall (funcall (funcall (hydra_lisp_coder_encode_let_as_native dialect) cx) g) bindings) body)))) match_value)) ((equal (car match_target) :list) (funcall (lambda (els) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g)) els)) (lambda (sels) (list :right (hydra_lisp_coder_lisp_list_expr sels))))) match_value)) ((equal (car match_target) :literal) (funcall (lambda (lit) (list :right (hydra_lisp_coder_encode_literal lit))) match_value)) ((equal (car match_target) :map) (funcall (lambda (m) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (lambda (entry) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (hydra_lib_pairs_first entry))) (lambda (k) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (hydra_lib_pairs_second entry))) (lambda (v) (list :right (make-hydra_lisp_syntax_map_entry k v)))))))) (hydra_lib_maps_to_list m))) (lambda (pairs) (list :right (list :map (make-hydra_lisp_syntax_map_literal pairs)))))) match_value)) ((equal (car match_target) :maybe) (funcall (lambda (mt) (funcall (funcall (hydra_lib_maybes_cases mt) (lambda () (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword "nothing")))))) (lambda (val) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) val)) (lambda (sval) (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword "just") sval)))))))) match_value)) ((equal (car match_target) :pair) (funcall (lambda (p) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (hydra_lib_pairs_first p))) (lambda (f) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (hydra_lib_pairs_second p))) (lambda (s) (list :right (hydra_lisp_coder_lisp_list_expr (list f s)))))))) match_value)) ((equal (car match_target) :record) (funcall (lambda (rec) (let ((rname (funcall (lambda (v) (hydra_core_record-type_name v)) rec))) (let ((fields (funcall (lambda (v) (hydra_core_record-fields v)) rec))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (lambda (f) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_field-term v)) f)))) fields)) (lambda (sfields) (let ((constructor_name (funcall (hydra_lib_strings_cat2 (hydra_lisp_coder_dialect_constructor_prefix dialect)) (hydra_lisp_coder_qualified_snake_name rname)))) (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var constructor_name)) sfields)))))))) match_value)) ((equal (car match_target) :set) (funcall (lambda (s) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g)) (hydra_lib_sets_to_list s))) (lambda (sels) (list :right (list :set (make-hydra_lisp_syntax_set_literal sels)))))) match_value)) ((equal (car match_target) :inject) (funcall (lambda (inj) (let ((tname (hydra_names_local_name_of (funcall (lambda (v) (hydra_core_injection-type_name v)) inj)))) (let ((field (funcall (lambda (v) (hydra_core_injection-field v)) inj))) (let ((fname (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_field-name v)) field)))) (let ((fterm (funcall (lambda (v) (hydra_core_field-term v)) field))) (let ((dterm (hydra_strip_deannotate_term fterm))) (let ((is_unit (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :unit) (funcall (lambda (_) t) match_value)) ((equal (car match_target) :record) (funcall (lambda (rt) (hydra_lib_lists_null (funcall (lambda (v) (hydra_core_record-fields v)) rt))) match_value)) (t nil))) (cadr match_target))) dterm))) (if is_unit (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword (hydra_formatting_convert_case_camel_to_lower_snake fname)) hydra_lisp_coder_lisp_nil_expr))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) fterm)) (lambda (sval) (list :right (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var "list")) (list (hydra_lisp_coder_lisp_keyword (hydra_formatting_convert_case_camel_to_lower_snake fname)) sval))))))))))))) match_value)) ((equal (car match_target) :unit) (funcall (lambda (_) (list :right hydra_lisp_coder_lisp_nil_expr)) match_value)) ((equal (car match_target) :variable) (funcall (lambda (name) (list :right (hydra_lisp_coder_lisp_var (hydra_formatting_convert_case_camel_or_underscore_to_lower_snake (funcall (hydra_formatting_sanitize_with_underscores hydra_lisp_language_lisp_reserved_words) (funcall (lambda (v) v) name)))))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (ta) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_type_application_term-body v)) ta))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (tl) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_type_lambda-body v)) tl))) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (wt) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_wrapped_term-body v)) wt))) match_value)))) (cadr match_target))) term))))))

(defvar hydra_lisp_coder_encode_union_elim (lambda (dialect) (lambda (cx) (lambda (g) (lambda (cs) (lambda (marg) (let ((tname (hydra_names_local_name_of (funcall (lambda (v) (hydra_core_case_statement-type_name v)) cs)))) (let ((case_fields (funcall (lambda (v) (hydra_core_case_statement-cases v)) cs))) (let ((def_case (funcall (lambda (v) (hydra_core_case_statement-default v)) cs))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (lambda (cf) (let ((cfname (hydra_formatting_convert_case_camel_to_lower_snake (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_field-name v)) cf))))) (let ((cfterm (funcall (lambda (v) (hydra_core_field-term v)) cf))) (let ((cond_expr (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var (hydra_lisp_coder_dialect_equal dialect))) (list (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var (hydra_lisp_coder_dialect_car dialect))) (list (hydra_lisp_coder_lisp_var "match_target"))) (hydra_lisp_coder_lisp_keyword cfname))))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (list :application (make-hydra_core_application cfterm (list :variable "match_value"))))) (lambda (body_expr) (list :right (make-hydra_lisp_syntax_cond_clause cond_expr body_expr))))))))) case_fields)) (lambda (clauses) (funcall (hydra_lib_eithers_bind (funcall (funcall (hydra_lib_maybes_cases def_case) (lambda () (list :right (list :nothing)))) (lambda (dt) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) dt)) (lambda (def_body) (list :right (list :just def_body))))))) (lambda (def_expr) (let ((cond_expr (list :cond (make-hydra_lisp_syntax_cond_expression clauses def_expr)))) (let ((inner_expr (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_lambda_expr (list "match_value")) cond_expr)) (list (funcall (hydra_lisp_coder_lisp_app (hydra_lisp_coder_lisp_var (hydra_lisp_coder_dialect_cadr dialect))) (list (hydra_lisp_coder_lisp_var "match_target"))))))) (funcall (funcall (hydra_lib_maybes_cases marg) (lambda () (list :right (funcall (hydra_lisp_coder_lisp_lambda_expr (list "match_target")) inner_expr)))) (lambda (arg) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) arg)) (lambda (sarg) (list :right (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_lambda_expr (list "match_target")) inner_expr)) (list sarg))))))))))))))))))))))

(defvar hydra_lisp_coder_encode_unwrap_elim (lambda (dialect) (lambda (cx) (lambda (g) (lambda (name) (lambda (marg) (funcall (funcall (hydra_lib_maybes_cases marg) (lambda () (list :right (funcall (hydra_lisp_coder_lisp_lambda_expr (list "v")) (hydra_lisp_coder_lisp_var "v"))))) (lambda (arg) (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) arg)))))))))

(defvar hydra_lisp_coder_encode_field_def (lambda (ft) (let ((fname (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_field_type-name v)) ft)))) (make-hydra_lisp_syntax_field_definition (hydra_formatting_convert_case_camel_to_lower_snake fname) (list :nothing)))))

(defvar hydra_lisp_coder_encode_let_as_lambda_app (lambda (dialect) (lambda (cx) (lambda (g) (lambda (bindings) (lambda (body) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) body)) (lambda (body_expr) (funcall (funcall (hydra_lib_eithers_foldl (lambda (acc) (lambda (b) (let ((bname (hydra_formatting_convert_case_camel_or_underscore_to_lower_snake (funcall (hydra_formatting_sanitize_with_underscores hydra_lisp_language_lisp_reserved_words) (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_binding-name v)) b)))))) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) (funcall (lambda (v) (hydra_core_binding-term v)) b))) (lambda (bval) (list :right (funcall (hydra_lisp_coder_lisp_app (funcall (hydra_lisp_coder_lisp_lambda_expr (list bname)) acc)) (list bval))))))))) body_expr) (hydra_lib_lists_reverse bindings))))))))))

(defvar hydra_lisp_coder_lisp_top_form (lambda (form) (make-hydra_lisp_syntax_top_level_form_with_comments (list :nothing) (list :nothing) form)))

(defvar hydra_lisp_coder_encode_term_definition (lambda (dialect) (lambda (cx) (lambda (g) (lambda (tdef) (let ((name (funcall (lambda (v) (hydra_packaging_term_definition-name v)) tdef))) (let ((term (funcall (lambda (v) (hydra_packaging_term_definition-term v)) tdef))) (let ((lname (hydra_lisp_coder_qualified_snake_name name))) (let ((dterm (hydra_strip_deannotate_term term))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (lam) (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) term)) (lambda (sterm) (list :right (hydra_lisp_coder_lisp_top_form (list :variable (make-hydra_lisp_syntax_variable_definition lname sterm (list :nothing)))))))) match_value)) (t (funcall (hydra_lib_eithers_bind (funcall (funcall (funcall (hydra_lisp_coder_encode_term dialect) cx) g) term)) (lambda (sterm) (list :right (hydra_lisp_coder_lisp_top_form (list :variable (make-hydra_lisp_syntax_variable_definition lname sterm (list :nothing)))))))))) (cadr match_target))) dterm))))))))))

(defvar hydra_lisp_coder_encode_type (lambda (cx) (lambda (g) (lambda (t_) (let ((typ (hydra_strip_deannotate_type t_))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) (funcall (lambda (v) (hydra_core_annotated_type-body v)) at))) match_value)) ((equal (car match_target) :application) (funcall (lambda (at) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) (funcall (lambda (v) (hydra_core_application_type-function v)) at))) match_value)) ((equal (car match_target) :unit) (funcall (lambda (_) (list :right (list :unit nil))) match_value)) ((equal (car match_target) :literal) (funcall (lambda (lt) (list :right (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :binary) (funcall (lambda (_) (list :named "ByteArray")) match_value)) ((equal (car match_target) :boolean) (funcall (lambda (_) (list :named "Boolean")) match_value)) ((equal (car match_target) :float) (funcall (lambda (_) (list :named "Float")) match_value)) ((equal (car match_target) :integer) (funcall (lambda (_) (list :named "Integer")) match_value)) ((equal (car match_target) :string) (funcall (lambda (_) (list :named "String")) match_value)))) (cadr match_target))) lt))) match_value)) ((equal (car match_target) :list) (funcall (lambda (inner) (funcall (hydra_lib_eithers_map (lambda (enc) (list :list enc))) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) inner))) match_value)) ((equal (car match_target) :set) (funcall (lambda (inner) (funcall (hydra_lib_eithers_map (lambda (enc) (list :set enc))) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) inner))) match_value)) ((equal (car match_target) :map) (funcall (lambda (mt) (list :right (list :named "Map"))) match_value)) ((equal (car match_target) :maybe) (funcall (lambda (inner) (funcall (hydra_lib_eithers_map (lambda (enc) (list :maybe enc))) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) inner))) match_value)) ((equal (car match_target) :either) (funcall (lambda (et) (list :right (list :named "Either"))) match_value)) ((equal (car match_target) :pair) (funcall (lambda (pt) (list :right (list :named "Pair"))) match_value)) ((equal (car match_target) :function) (funcall (lambda (ft) (list :right (list :named "Function"))) match_value)) ((equal (car match_target) :record) (funcall (lambda (_) (list :right (list :named "Record"))) match_value)) ((equal (car match_target) :union) (funcall (lambda (_) (list :right (list :named "Union"))) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (_) (list :right (list :named "Wrapper"))) match_value)) ((equal (car match_target) :variable) (funcall (lambda (name) (list :right (list :named (funcall (lambda (v) v) name)))) match_value)) ((equal (car match_target) :forall) (funcall (lambda (fa) (funcall (funcall (hydra_lisp_coder_encode_type cx) g) (funcall (lambda (v) (hydra_core_forall_type-body v)) fa))) match_value)) (t (list :right (list :named "Any"))))) (cadr match_target))) typ))))))

(defvar hydra_lisp_coder_encode_type_body (lambda (lname) (lambda (orig_typ) (lambda (typ) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :forall) (funcall (lambda (ft) (funcall (funcall (hydra_lisp_coder_encode_type_body lname) orig_typ) (funcall (lambda (v) (hydra_core_forall_type-body v)) ft))) match_value)) ((equal (car match_target) :record) (funcall (lambda (rt) (let ((fields (funcall (hydra_lib_lists_map hydra_lisp_coder_encode_field_def) rt))) (list :right (hydra_lisp_coder_lisp_top_form (list :record_type (make-hydra_lisp_syntax_record_type_definition lname fields (list :nothing))))))) match_value)) ((equal (car match_target) :union) (funcall (lambda (rt) (let ((variant_names (funcall (hydra_lib_lists_map (lambda (f) (list :literal (list :keyword (make-hydra_lisp_syntax_keyword (hydra_formatting_convert_case_camel_to_lower_snake (funcall (lambda (v) v) (funcall (lambda (v) (hydra_core_field_type-name v)) f))) (list :nothing)))))) rt))) (list :right (hydra_lisp_coder_lisp_top_form (list :variable (make-hydra_lisp_syntax_variable_definition (funcall (hydra_lib_strings_cat2 lname) "-variants") (hydra_lisp_coder_lisp_list_expr variant_names) (list :just (funcall (hydra_lib_strings_cat2 "Variants of the ") lname)))))))) match_value)) ((equal (car match_target) :wrap) (funcall (lambda (wt) (list :right (hydra_lisp_coder_lisp_top_form (list :record_type (make-hydra_lisp_syntax_record_type_definition lname (list (make-hydra_lisp_syntax_field_definition "value" (list :nothing))) (list :nothing)))))) match_value)) (t (list :right (make-hydra_lisp_syntax_top_level_form_with_comments (list :nothing) (list :just (make-hydra_lisp_syntax_comment (list :line nil) (funcall (hydra_lib_strings_cat2 (funcall (hydra_lib_strings_cat2 lname) " = ")) (hydra_show_core_type orig_typ)))) (list :expression (list :literal (list :nil nil)))))))) (cadr match_target))) typ)))))

(defvar hydra_lisp_coder_encode_type_definition (lambda (cx) (lambda (g) (lambda (tdef) (let ((name (funcall (lambda (v) (hydra_packaging_type_definition-name v)) tdef))) (let ((typ (funcall (lambda (v) (hydra_core_type_scheme-type v)) (funcall (lambda (v) (hydra_packaging_type_definition-type v)) tdef)))) (let ((lname (hydra_lisp_coder_qualified_snake_name name))) (let ((dtyp (hydra_strip_deannotate_type typ))) (funcall (funcall (hydra_lisp_coder_encode_type_body lname) typ) dtyp)))))))))

(defvar hydra_lisp_coder_is_cases_primitive (lambda (name) (funcall (hydra_lib_equality_equal name) "hydra.lib.maybes.cases")))

(defvar hydra_lisp_coder_is_lazy2_arg_primitive (lambda (name) (funcall (hydra_lib_logic_or (funcall (hydra_lib_equality_equal name) "hydra.lib.eithers.fromLeft")) (funcall (hydra_lib_logic_or (funcall (hydra_lib_equality_equal name) "hydra.lib.eithers.fromRight")) (funcall (hydra_lib_equality_equal name) "hydra.lib.maybes.fromMaybe")))))

(defvar hydra_lisp_coder_is_lazy3_arg_primitive (lambda (name) (funcall (hydra_lib_equality_equal name) "hydra.lib.maybes.maybe")))

(defvar hydra_lisp_coder_lisp_lit_expr (lambda (lit) (list :literal lit)))

(defvar hydra_lisp_coder_lisp_symbol (lambda (name) name))

(defvar hydra_lisp_coder_lisp_top_form_with_comments (lambda (mdoc) (lambda (form) (make-hydra_lisp_syntax_top_level_form_with_comments (funcall (hydra_lib_maybes_map (lambda (d) d)) mdoc) (list :nothing) form))))

(defvar hydra_lisp_coder_module_exports (lambda (forms) (let ((symbols (hydra_lib_lists_concat (funcall (hydra_lib_lists_map (lambda (fwc) (let ((form (funcall (lambda (v) (hydra_lisp_syntax_top_level_form_with_comments-form v)) fwc))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :variable) (funcall (lambda (vd) (list (funcall (lambda (v) (hydra_lisp_syntax_variable_definition-name v)) vd))) match_value)) ((equal (car match_target) :record_type) (funcall (lambda (rdef) (let ((rname (funcall (lambda (v) v) (funcall (lambda (v) (hydra_lisp_syntax_record_type_definition-name v)) rdef)))) (let ((fields (funcall (lambda (v) (hydra_lisp_syntax_record_type_definition-fields v)) rdef))) (let ((field_syms (funcall (hydra_lib_lists_map (lambda (f) (let ((fn_ (funcall (lambda (v) v) (funcall (lambda (v) (hydra_lisp_syntax_field_definition-name v)) f)))) (hydra_lib_strings_cat (list rname "-" fn_))))) fields))) (hydra_lib_lists_concat (list (list (funcall (hydra_lib_strings_cat2 "make-") rname) (funcall (hydra_lib_strings_cat2 rname) "?")) field_syms)))))) match_value)) (t (list)))) (cadr match_target))) form)))) forms)))) (if (hydra_lib_lists_null symbols) (list) (list (make-hydra_lisp_syntax_export_declaration symbols))))))

(defvar hydra_lisp_coder_module_imports (lambda (focus_ns) (lambda (defs) (let ((dep_nss (hydra_lib_sets_to_list (funcall (hydra_lib_sets_delete focus_ns) (hydra_analysis_definition_dependency_namespaces defs))))) (funcall (hydra_lib_lists_map (lambda (ns_) (make-hydra_lisp_syntax_import_declaration (funcall (lambda (v) v) ns_) (list :all nil)))) dep_nss)))))

(defvar hydra_lisp_coder_module_to_lisp (lambda (dialect) (lambda (mod) (lambda (defs0) (lambda (cx) (lambda (g) (let ((defs (hydra_environment_reorder_defs defs0))) (let ((partitioned (hydra_environment_partition_definitions defs))) (let ((all_type_defs (hydra_lib_pairs_first partitioned))) (let ((term_defs (hydra_lib_pairs_second partitioned))) (let ((type_defs (funcall (hydra_lib_lists_filter (lambda (td) (hydra_predicates_is_nominal_type (funcall (lambda (v) (hydra_core_type_scheme-type v)) (funcall (lambda (v) (hydra_packaging_type_definition-type v)) td))))) all_type_defs))) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (funcall (hydra_lisp_coder_encode_type_definition cx) g)) type_defs)) (lambda (type_items) (funcall (hydra_lib_eithers_bind (funcall (hydra_lib_eithers_map_list (funcall (funcall (hydra_lisp_coder_encode_term_definition dialect) cx) g)) term_defs)) (lambda (term_items) (let ((all_items (funcall (hydra_lib_lists_concat2 type_items) term_items))) (let ((ns_name (funcall (lambda (v) v) (funcall (lambda (v) (hydra_packaging_module-namespace v)) mod)))) (let ((focus_ns (funcall (lambda (v) (hydra_packaging_module-namespace v)) mod))) (let ((imports (funcall (hydra_lisp_coder_module_imports focus_ns) defs))) (let ((exports (hydra_lisp_coder_module_exports all_items))) (list :right (make-hydra_lisp_syntax_program dialect (list :just (make-hydra_lisp_syntax_module_declaration ns_name (list :nothing))) imports exports all_items))))))))))))))))))))))

(defvar hydra_lisp_coder_qualified_type_name (lambda (name) (hydra_formatting_capitalize (hydra_names_local_name_of name))))

(provide 'hydra.lisp.coder)
