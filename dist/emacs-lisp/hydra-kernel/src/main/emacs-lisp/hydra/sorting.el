(require 'cl-lib)

(require 'hydra.constants)

(require 'hydra.lib.equality)

(require 'hydra.lib.lists)

(require 'hydra.lib.logic)

(require 'hydra.lib.maps)

(require 'hydra.lib.math)

(require 'hydra.lib.maybes)

(require 'hydra.lib.pairs)

(require 'hydra.lib.sets)

(require 'hydra.topology)

(defvar hydra_sorting_adjacency_list_to_map (lambda (pairs) (funcall (funcall (hydra_lib_lists_foldl (lambda (mp) (lambda (p) (let ((k (hydra_lib_pairs_first p))) (let ((vs (hydra_lib_pairs_second p))) (let ((existing (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list))) hydra_lib_equality_identity) (funcall (hydra_lib_maps_lookup k) mp)))) (funcall (funcall (hydra_lib_maps_insert k) (funcall (hydra_lib_lists_concat2 existing) vs)) mp))))))) hydra_lib_maps_empty) pairs)))

(defvar hydra_sorting_adjacency_lists_to_graph (lambda (edges0) (let ((sorted_edges (funcall (hydra_lib_lists_sort_on hydra_lib_pairs_first) edges0))) (let ((indexed_edges (funcall (hydra_lib_lists_zip (funcall (hydra_lib_math_range 0) (hydra_lib_lists_length sorted_edges))) sorted_edges))) (let ((key_to_vertex (hydra_lib_maps_from_list (funcall (hydra_lib_lists_map (lambda (vk_neighbors) (let ((v (hydra_lib_pairs_first vk_neighbors))) (let ((k_neighbors (hydra_lib_pairs_second vk_neighbors))) (let ((k (hydra_lib_pairs_first k_neighbors))) (list k v)))))) indexed_edges)))) (let ((vertex_map (hydra_lib_maps_from_list (funcall (hydra_lib_lists_map (lambda (vk_neighbors) (let ((v (hydra_lib_pairs_first vk_neighbors))) (let ((k_neighbors (hydra_lib_pairs_second vk_neighbors))) (let ((k (hydra_lib_pairs_first k_neighbors))) (list v k)))))) indexed_edges)))) (let ((graph (hydra_lib_maps_from_list (funcall (hydra_lib_lists_map (lambda (vk_neighbors) (let ((v (hydra_lib_pairs_first vk_neighbors))) (let ((k_neighbors (hydra_lib_pairs_second vk_neighbors))) (let ((neighbors (hydra_lib_pairs_second k_neighbors))) (list v (funcall (hydra_lib_maybes_map_maybe (lambda (k) (funcall (hydra_lib_maps_lookup k) key_to_vertex))) neighbors))))))) indexed_edges)))) (let ((vertex_to_key (lambda (v) (funcall (hydra_lib_maps_lookup v) vertex_map)))) (list graph vertex_to_key)))))))))

(defvar hydra_sorting_create_ordering_isomorphism (lambda (source_ord) (lambda (target_ord) (let ((source_to_target_mapping (lambda (els) (let ((mp (hydra_lib_maps_from_list (funcall (hydra_lib_lists_zip source_ord) els)))) (hydra_lib_maybes_cat (funcall (hydra_lib_lists_map (lambda (n) (funcall (hydra_lib_maps_lookup n) mp))) target_ord)))))) (let ((target_to_source_mapping (lambda (els) (let ((mp (hydra_lib_maps_from_list (funcall (hydra_lib_lists_zip target_ord) els)))) (hydra_lib_maybes_cat (funcall (hydra_lib_lists_map (lambda (n) (funcall (hydra_lib_maps_lookup n) mp))) source_ord)))))) (make-hydra_topology_ordering_isomorphism source_to_target_mapping target_to_source_mapping))))))

(defvar hydra_sorting_find_reachable_nodes (lambda (adj) (lambda (root) (letrec ((visit (lambda (visited) (lambda (node) (let ((to_visit (funcall (hydra_lib_sets_difference (adj node)) visited))) (if (hydra_lib_sets_null to_visit) visited (funcall (funcall (hydra_lib_lists_foldl (lambda (v) (lambda (n) (funcall (visit (funcall (hydra_lib_sets_insert n) v)) n)))) visited) (hydra_lib_sets_to_list to_visit)))))))) (funcall (visit (hydra_lib_sets_singleton root)) root)))))

(defvar hydra_sorting_initial_state (make-hydra_topology_tarjan_state 0 hydra_lib_maps_empty hydra_lib_maps_empty (list) hydra_lib_sets_empty (list)))

(defvar hydra_sorting_pop_stack_until (lambda (v) (lambda (st0) (letrec ((go_ (lambda (acc) (lambda (st) (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list (hydra_lib_lists_reverse acc) st))) (lambda (uc) (let ((x (hydra_lib_pairs_first uc))) (let ((xs (hydra_lib_pairs_second uc))) (let ((new_st (make-hydra_topology_tarjan_state (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) st) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st) xs (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) st)))) (let ((new_st2 (make-hydra_topology_tarjan_state (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) new_st) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) new_st) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) new_st) (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) new_st) (funcall (hydra_lib_sets_delete x) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st)) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) new_st)))) (let ((acc_ (funcall (hydra_lib_lists_cons x) acc))) (if (funcall (hydra_lib_equality_equal x) v) (list (hydra_lib_lists_reverse acc_) new_st2) (funcall (go_ acc_) new_st2))))))))) (hydra_lib_lists_uncons (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) st))))))) (funcall (go_ (list)) st0)))))

(defvar hydra_sorting_propagate_tags (lambda (edges) (lambda (node_tags) (let ((adj_map (hydra_sorting_adjacency_list_to_map edges))) (let ((tag_map (funcall (hydra_lib_maps_map hydra_lib_sets_from_list) (hydra_sorting_adjacency_list_to_map node_tags)))) (let ((all_nodes (hydra_lib_sets_to_list (hydra_lib_sets_from_list (funcall (hydra_lib_lists_concat2 (funcall (hydra_lib_lists_map hydra_lib_pairs_first) edges)) (funcall (hydra_lib_lists_map hydra_lib_pairs_first) node_tags)))))) (let ((get_tags_for_node (lambda (node) (let ((reachable (funcall (hydra_sorting_find_reachable_nodes (lambda (n) (hydra_lib_sets_from_list (funcall (funcall (hydra_lib_maybes_maybe (lambda () (list))) hydra_lib_equality_identity) (funcall (hydra_lib_maps_lookup n) adj_map))))) node))) (hydra_lib_sets_unions (funcall (hydra_lib_lists_map (lambda (n) (funcall (funcall (hydra_lib_maybes_maybe (lambda () hydra_lib_sets_empty)) hydra_lib_equality_identity) (funcall (hydra_lib_maps_lookup n) tag_map)))) (hydra_lib_sets_to_list reachable))))))) (funcall (hydra_lib_lists_map (lambda (n) (list n (get_tags_for_node n)))) all_nodes))))))))

(defvar hydra_sorting_strong_connect (lambda (graph) (lambda (v) (lambda (st) (let ((i (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) st))) (let ((new_st (make-hydra_topology_tarjan_state (funcall (hydra_lib_math_add i) 1) (funcall (funcall (hydra_lib_maps_insert v) i) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st)) (funcall (funcall (hydra_lib_maps_insert v) i) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st)) (funcall (hydra_lib_lists_cons v) (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) st)) (funcall (hydra_lib_sets_insert v) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st)) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) st)))) (let ((neighbors (funcall (funcall (hydra_lib_maps_find_with_default (list)) v) graph))) (let ((process_neighbor (lambda (st_) (lambda (w) (let ((low_link (lambda (s) (let ((low_v1 (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) v) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) s)))) (let ((idx_w (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) w) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) s)))) (make-hydra_topology_tarjan_state (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) s) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) s) (funcall (funcall (hydra_lib_maps_insert v) (funcall (hydra_lib_equality_min low_v1) idx_w)) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) s)) (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) s) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) s) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) s))))))) (if (hydra_lib_logic_not (funcall (hydra_lib_maps_member w) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st_))) (let ((st_after (funcall (funcall (hydra_sorting_strong_connect graph) w) st_))) (let ((low_v2 (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) v) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st_after)))) (let ((low_w (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) w) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st_after)))) (make-hydra_topology_tarjan_state (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) st_after) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st_after) (funcall (funcall (hydra_lib_maps_insert v) (funcall (hydra_lib_equality_min low_v2) low_w)) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st_after)) (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) st_after) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st_after) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) st_after))))) (if (funcall (hydra_lib_sets_member w) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st_)) (low_link st_) st_))))))) (let ((st_after_neighbors (funcall (funcall (hydra_lib_lists_foldl process_neighbor) new_st) neighbors))) (let ((low_v (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) v) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st_after_neighbors)))) (let ((idx_v (funcall (funcall (hydra_lib_maps_find_with_default hydra_constants_max_int32) v) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st_after_neighbors)))) (if (funcall (hydra_lib_equality_equal low_v) idx_v) (let ((comp_result (funcall (hydra_sorting_pop_stack_until v) st_after_neighbors))) (let ((comp (hydra_lib_pairs_first comp_result))) (let ((st_popped (hydra_lib_pairs_second comp_result))) (make-hydra_topology_tarjan_state (funcall (lambda (v) (hydra_topology_tarjan_state-counter v)) st_popped) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st_popped) (funcall (lambda (v) (hydra_topology_tarjan_state-low_links v)) st_popped) (funcall (lambda (v) (hydra_topology_tarjan_state-stack v)) st_popped) (funcall (lambda (v) (hydra_topology_tarjan_state-on_stack v)) st_popped) (funcall (hydra_lib_lists_cons comp) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) st_popped)))))) st_after_neighbors))))))))))))

(defvar hydra_sorting_strongly_connected_components (lambda (graph) (let ((verts (hydra_lib_maps_keys graph))) (let ((final_state (funcall (funcall (hydra_lib_lists_foldl (lambda (st) (lambda (v) (if (funcall (hydra_lib_maps_member v) (funcall (lambda (v) (hydra_topology_tarjan_state-indices v)) st)) st (funcall (funcall (hydra_sorting_strong_connect graph) v) st))))) hydra_sorting_initial_state) verts))) (hydra_lib_lists_reverse (funcall (hydra_lib_lists_map hydra_lib_lists_sort) (funcall (lambda (v) (hydra_topology_tarjan_state-sccs v)) final_state)))))))

(defvar hydra_sorting_topological_sort_components (lambda (pairs) (let ((graph_result (hydra_sorting_adjacency_lists_to_graph pairs))) (let ((g (hydra_lib_pairs_first graph_result))) (funcall (hydra_lib_lists_map (lambda (comp) (funcall (hydra_lib_maybes_map_maybe (hydra_lib_pairs_second graph_result)) comp))) (hydra_sorting_strongly_connected_components g))))))

(defvar hydra_sorting_topological_sort (lambda (pairs) (let ((sccs (hydra_sorting_topological_sort_components pairs))) (let ((is_cycle (lambda (scc) (funcall (hydra_lib_equality_gt (hydra_lib_lists_length scc)) 1)))) (let ((with_cycles (funcall (hydra_lib_lists_filter is_cycle) sccs))) (if (hydra_lib_lists_null with_cycles) (list :right (hydra_lib_lists_concat sccs)) (list :left with_cycles)))))))

(defvar hydra_sorting_topological_sort_nodes (lambda (get_key) (lambda (get_adj) (lambda (nodes) (let ((nodes_by_key (hydra_lib_maps_from_list (funcall (hydra_lib_lists_map (lambda (n) (list (get_key n) n))) nodes)))) (let ((pairs (funcall (hydra_lib_lists_map (lambda (n) (list (get_key n) (get_adj n)))) nodes))) (let ((comps (hydra_sorting_topological_sort_components pairs))) (funcall (hydra_lib_lists_map (lambda (c) (hydra_lib_maybes_cat (funcall (hydra_lib_lists_map (lambda (k) (funcall (hydra_lib_maps_lookup k) nodes_by_key))) c)))) comps))))))))

(provide 'hydra.sorting)
