(require 'cl-lib)

(require 'hydra.core)

(require 'hydra.lib.lists)

(require 'hydra.rewriting)

(defvar hydra_strip_deannotate_and_detype_term (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (hydra_strip_deannotate_and_detype_term (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (tt) (hydra_strip_deannotate_and_detype_term (funcall (lambda (v) (hydra_core_type_application_term-body v)) tt))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (ta) (hydra_strip_deannotate_and_detype_term (funcall (lambda (v) (hydra_core_type_lambda-body v)) ta))) match_value)) (t t_))) (cadr match_target))) t_)))

(defvar hydra_strip_deannotate_term (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (hydra_strip_deannotate_term (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) match_value)) (t t_))) (cadr match_target))) t_)))

(defvar hydra_strip_deannotate_type (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (arg_) (hydra_strip_deannotate_type (funcall (lambda (v) (hydra_core_annotated_type-body v)) arg_))) match_value)) (t t_))) (cadr match_target))) t_)))

(defvar hydra_strip_deannotate_type_parameters (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :forall) (funcall (lambda (lt) (hydra_strip_deannotate_type_parameters (funcall (lambda (v) (hydra_core_forall_type-body v)) lt))) match_value)) (t t_))) (cadr match_target))) (hydra_strip_deannotate_type t_))))

(defvar hydra_strip_deannotate_type_recursive (lambda (typ) (let ((strip (lambda (recurse) (lambda (typ2) (let ((rewritten (recurse typ2))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (lambda (v) (hydra_core_annotated_type-body v)) at)) match_value)) (t rewritten))) (cadr match_target))) rewritten)))))) (funcall (hydra_rewriting_rewrite_type strip) typ))))

(defvar hydra_strip_deannotate_type_scheme_recursive (lambda (ts) (let ((vars (funcall (lambda (v) (hydra_core_type_scheme-variables v)) ts))) (let ((typ (funcall (lambda (v) (hydra_core_type_scheme-type v)) ts))) (let ((constraints (funcall (lambda (v) (hydra_core_type_scheme-constraints v)) ts))) (make-hydra_core_type_scheme vars (hydra_strip_deannotate_type_recursive typ) constraints))))))

(defvar hydra_strip_detype_term (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (let ((subj (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) (let ((ann (funcall (lambda (v) (hydra_core_annotated_term-annotation v)) at))) (list :annotated (make-hydra_core_annotated_term (hydra_strip_detype_term subj) ann))))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (tt) (hydra_strip_deannotate_and_detype_term (funcall (lambda (v) (hydra_core_type_application_term-body v)) tt))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (ta) (hydra_strip_deannotate_and_detype_term (funcall (lambda (v) (hydra_core_type_lambda-body v)) ta))) match_value)) (t t_))) (cadr match_target))) t_)))

(defvar hydra_strip_remove_term_annotations (lambda (term) (let ((remove (lambda (recurse) (lambda (term2) (let ((rewritten (recurse term2))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (lambda (v) (hydra_core_annotated_term-body v)) at)) match_value)) (t rewritten))) (cadr match_target))) term2)))))) (funcall (hydra_rewriting_rewrite_term remove) term))))

(defvar hydra_strip_remove_type_annotations (lambda (typ) (let ((remove (lambda (recurse) (lambda (typ2) (let ((rewritten (recurse typ2))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (funcall (lambda (v) (hydra_core_annotated_type-body v)) at)) match_value)) (t rewritten))) (cadr match_target))) rewritten)))))) (funcall (hydra_rewriting_rewrite_type remove) typ))))

(defvar hydra_strip_remove_type_annotations_from_term (lambda (term) (let ((strip (lambda (recurse) (lambda (term2) (let ((rewritten (recurse term2))) (let ((strip_binding (lambda (b) (make-hydra_core_binding (funcall (lambda (v) (hydra_core_binding-name v)) b) (funcall (lambda (v) (hydra_core_binding-term v)) b) (list :nothing))))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :let) (funcall (lambda (lt) (list :let (make-hydra_core_let (funcall (hydra_lib_lists_map strip_binding) (funcall (lambda (v) (hydra_core_let-bindings v)) lt)) (funcall (lambda (v) (hydra_core_let-body v)) lt)))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (tt) (funcall (lambda (v) (hydra_core_type_application_term-body v)) tt)) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (ta) (funcall (lambda (v) (hydra_core_type_lambda-body v)) ta)) match_value)) (t rewritten))) (cadr match_target))) rewritten))))))) (funcall (hydra_rewriting_rewrite_term strip) term))))

(defvar hydra_strip_remove_types_from_term (lambda (term) (let ((strip (lambda (recurse) (lambda (term2) (let ((rewritten (recurse term2))) (let ((strip_binding (lambda (b) (make-hydra_core_binding (funcall (lambda (v) (hydra_core_binding-name v)) b) (funcall (lambda (v) (hydra_core_binding-term v)) b) (list :nothing))))) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :lambda) (funcall (lambda (l) (list :lambda (make-hydra_core_lambda (funcall (lambda (v) (hydra_core_lambda-parameter v)) l) (list :nothing) (funcall (lambda (v) (hydra_core_lambda-body v)) l)))) match_value)) ((equal (car match_target) :let) (funcall (lambda (lt) (list :let (make-hydra_core_let (funcall (hydra_lib_lists_map strip_binding) (funcall (lambda (v) (hydra_core_let-bindings v)) lt)) (funcall (lambda (v) (hydra_core_let-body v)) lt)))) match_value)) ((equal (car match_target) :type_application) (funcall (lambda (tt) (funcall (lambda (v) (hydra_core_type_application_term-body v)) tt)) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (ta) (funcall (lambda (v) (hydra_core_type_lambda-body v)) ta)) match_value)) (t rewritten))) (cadr match_target))) rewritten))))))) (funcall (hydra_rewriting_rewrite_term strip) term))))

(defvar hydra_strip_strip_type_lambdas (lambda (t_) (funcall (lambda (match_target) (funcall (lambda (match_value) (cond ((equal (car match_target) :annotated) (funcall (lambda (at) (let ((subj (funcall (lambda (v) (hydra_core_annotated_term-body v)) at))) (let ((ann (funcall (lambda (v) (hydra_core_annotated_term-annotation v)) at))) (list :annotated (make-hydra_core_annotated_term (hydra_strip_strip_type_lambdas subj) ann))))) match_value)) ((equal (car match_target) :type_lambda) (funcall (lambda (ta) (hydra_strip_strip_type_lambdas (funcall (lambda (v) (hydra_core_type_lambda-body v)) ta))) match_value)) (t t_))) (cadr match_target))) t_)))

(provide 'hydra.strip)
