(ns hydra.protobuf.environment
  (:require [hydra.core :refer :all]
))

(declare hydra_protobuf_environment_structural_type_ref-variants)

(def hydra_protobuf_environment_structural_type_ref-variants (list :either :pair))
