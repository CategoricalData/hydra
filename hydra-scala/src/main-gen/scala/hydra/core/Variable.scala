/**
 * A symbol which stands in for a term
 * 
 * @type string
 */
package hydra.core

/**
 * A symbol which stands in for a term
 * 
 * @type string
 */
type Variable = String

val _Variable: String = "hydra/core.Variable"
