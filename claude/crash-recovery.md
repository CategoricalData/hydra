# crash-recovery — moved to hydra-agents

This harness doc is now maintained in the **hydra-agents** repository, not in Hydra.

- **Canonical (local checkout):** `../../external/agents/docs/crash-recovery.md` (from a worktree; `external/` is the submodule dir, a sibling of `worktrees/`)
- **Pinned revision:** 4857d95 (see `hydra-agents.json` → `agentsVersion`)
- **GitHub:** https://github.com/CategoricalData/hydra-agents/blob/main/docs/crash-recovery.md

The content is generic agent-fleet-harness guidance shared across projects. Hydra keeps this
one-line pointer so briefings, memories, and in-flight sessions that reference the old
`claude/crash-recovery.md` path still resolve. Edit the canonical copy in hydra-agents, not here.
